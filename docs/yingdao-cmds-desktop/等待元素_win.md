# 等待元素(win)

等待元素（win）
💡

等待窗口元素出现或消失，再执行接下来的流程

🎬 视频课程：影刀RPA中级课程：桌面软件＋键鼠自动化

窗口对象

根据操作目标自动匹配或选择一个之前通过【获取窗口对象】指令创建的窗口对象

操作目标

从「元素库」中选择一个已捕获的元素或通过「捕获新元素」来捕获新的窗口元素作为操作目标

等待状态

等待元素出现/消失

此流程执行逻辑：执行【获取窗口对象】指令获取指定窗口 --> 使用【关闭软件窗口】指令关闭指定窗口对象

设置超时时间，超时后流程将自动往下继续执行，单位为秒

等待结果

在超时时间内，等待状态符合则返回值为True,否则返回值为False

使用示例

此流程执行逻辑：获取"钉钉"窗口对象 --> 使用【等待元素(win)】指令一直等待"新消息"元素直至出现 --> 等待出现后执行【点击元素(win)】指令

## 参数截图
![assets/等待元素_win/img0.png](assets/等待元素_win/img0.png)
![assets/等待元素_win/img1.png](assets/等待元素_win/img1.png)
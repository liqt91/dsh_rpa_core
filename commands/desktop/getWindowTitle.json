{
  "id": "desktop.getWindowTitle",
  "version": "1.0.0",
  "executor": "desktop.uia",
  "kind": "query",
  "risk": "read",
  "capabilities": ["desktop.control"],
  "resources": ["desktop.session", "desktop.window"],
  "stability": "stable",
  "effect": {"kind": "read", "replay": "safe", "idempotency": "none"},
  "input_schema": {"type": "object", "required": ["sessionId"], "properties": {"sessionId": {"type": "string", "minLength": 1}}, "additionalProperties": false},
  "output_schema": {"type": "object", "required": ["title", "className", "processId"], "properties": {"title": {"type": "string"}, "className": {"type": "string"}, "processId": {"type": "integer"}}, "additionalProperties": false},
  "x-outputs": {
    "title": {"label": "窗口标题", "primary": true},
    "className": {"label": "窗口类名"},
    "processId": {"label": "进程ID"}
  },
  "errors": ["SESSION_NOT_FOUND", "SESSION_LOST", "EXECUTOR_FAILED"],
  "implementation": {"handler": "desktop.uia:get_window_title"},
  "default_timeout_seconds": 15,
  "retryable": false
}

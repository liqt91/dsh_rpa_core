{
  "id": "browser.close",
  "version": "1.0.0",
  "executor": "browser.playwright",
  "kind": "lifecycle",
  "risk": "browser-control",
  "capabilities": ["browser.control"],
  "resources": ["browser.session"],
  "stability": "stable",
  "effect": {"kind": "session", "replay": "unsafe", "idempotency": "none"},
  "input_schema": {"type": "object", "required": [], "properties": {"sessionId": {"type": "string"}, "forceKill": {"type": "boolean", "description": "强制终止浏览器进程及其所有子进程", "default": false}, "ignoreUnload": {"type": "boolean", "description": "忽略「确认离开页面」对话框", "default": true}}, "additionalProperties": false},
  "output_schema": {"type": "object", "additionalProperties": false},
  "errors": ["SESSION_NOT_FOUND", "CANCELLED", "EXECUTOR_FAILED"],
  "implementation": {"handler": "browser.playwright:close"},
  "default_timeout_seconds": 15,
  "retryable": false
}

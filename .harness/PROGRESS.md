# Progress

Format: `YYYY-MM-DD | feature | status | evidence`

2026-08-28 | project-harness | done | architecture/invariant docs + full mechanical gate
2026-08-28 | protocol-models | done | typed workflow, command, result, error, event, and run models
2026-08-28 | catalog-compiler | done | immutable 9-command catalog with digest and static reference/capability checks
2026-08-28 | orchestrator | done | timeout/retry/cancel/event/terminal-result ownership verified
2026-08-28 | browser-playwright | done | explicit UUID browser session and real Chromium contract verified
2026-08-28 | python-worker | done | subprocess JSON worker with workspace containment and UTF-8 path regression
2026-08-28 | vertical-slice | done | deterministic search-and-save CLI and Chromium E2E passed
2026-08-28 | orchestrator-hardening | done | lexical catch scope, async run evidence I/O, bounded foreach, explicit result guards, workflow timeout; 11 tests + full gate passed
2026-08-28 | resolver-contract | done | whole-token references, fail-fast malformed references, mapping-only paths, condition tests, and nested compiler scope propagation; 15 tests + full gate passed
2026-08-28 | runtime-correctness | done | stable errors/context, timeout and subprocess cleanup, bounded retry/deadline, persistence failure semantics; 24 tests + full gate passed
2026-08-28 | effect-contract | done | typed effect/replay/idempotency policy, unsafe retry rejection, committed and unknown evidence, browser/worker E2E; 30 tests + full gate passed
2026-08-31 | M2-desktop | active | 新增 desktop.win32 独立后端与记事本 Win32 垂直切片，桌面 locator 扩展 backend/className/controlId/foundIndex/menuPath，Win32 记事本 E2E 与合同测试已通过；UIA 主线保留待后续 WinForms 验证
2026-08-31 | M2.1-legacy-element-importer | done | 旧元素静态盘点文档、LegacyElementImporter、provenance/diagnostic 模型与确定性导入测试已补齐；导入器仅处理静态数据，不触及 rpa_script runtime
2026-09-01 | M3-checkpoint-recovery | done | ADR 0004 状态机契约、版本化原子 checkpoint + 路径键完成集合、resume(plan, run_id) 恢复门槛、indeterminate/recovery_required 终态与人工恢复入口（含 CLI resume）、7 项崩溃注入测试；48 tests + full gate passed
2026-09-01 | M4-pause-resume | done | ADR 0005 暂停契约（action 入口 + attempt 前生效、取消>暂停>超时）、RunStatus.PAUSED 驻留状态、pauseRequested/runPaused 事件、resume 复用 M3 五道门、规则 8 修订；7 项边界测试；55 tests + full gate passed
2026-09-01 | M2.2-uia-winforms | done | WinForms 测试应用全链路 UIA E2E（输入/提交/读回 + 对话框双 session，3 连跑一致）、timeoutMs 轮询与 EnumWindows 兜底修复 owned 弹窗枚举盲区、manifest 对称合同测试、docs/desktop_backends.md；60 tests + full gate passed
2026-09-01 | baidu-news-example | added | 真实站点示例 examples/baidu-news-top10（百度搜索"新闻"保存首页结果标题到 run_artifacts/baidu-top10.txt）；browser.launch 扩展 userAgent 参数并移除 automation 标志（headless 被百度安全验证拦截，headed + 正常 UA 放行）；python worker 修复相对 path 按 workspace 解析（补越界拒绝测试）；61 tests + full gate passed
2026-09-01 | M4.5-data-commands | done | data.writeText（text/lines 二选一，workspace 包含校验，UTF-8）与 data.limit（前 N 条截断，pure/safe）入册，catalog 25 条；百度示例升级为纯文本产物（limit 10 + 按行输出）；65 tests + full gate passed
2026-09-01 | M5-api-decision | done | ADR 0006：进程内 Python API 冻结为 v1（不建 facade）、HTTP 推迟并附重估条件、排除清单结论、三个待定问题全部结案；公开签名契约测试 4 项；工作区分批提交完成（M3+M4 / M2.2 / baidu / harness / M4.5）；69 tests + full gate passed
2026-09-01 | M6-api-usage | done | docs/api-usage.md（四步模式、状态机消费指引、恢复/暂停语义）+ examples/api-usage 可运行示例（run → 读证据 → pause → resume 实测）+ README 链接；70 tests + full gate passed
2026-09-01 | M7-command-surface | done | win32 timeoutMs 对称（findElement 声明未实现一并修复）、data.format（{name} 占位符 fail-fast）入册 catalog 26 条、UIA COM 繁忙轮询容错；S0 实验：Chrome 152 封锁默认 profile CDP 端口 → 捕获传输定为扩展为主/持久 profile 为辅（决策并入 ADR 0007）；72 tests × 3 轮 + full gate passed
2026-09-01 | capture-transport-scheme | added | 调研 Panerelay（chrome.debugger+Native Messaging→CDP 兼容端点）与 Playwright MCP（持久 profile 默认方案印证）；实测 chrome://inspect 授权开关（9222 监听、/json/* 404、需显式 WebSocket URL）；传输方案定稿 docs/capture-transport.md（持久 profile 主 + chrome://inspect/Panerelay/MCP 扩展/自研扩展降级链），S1 验证协议与 M8 计划更新
2026-09-02 | M11-editor-tree | done | 维护者确认切片 1 拖拽体验后完成切片 2-5：控制节点属性表单（if/forEach/try/return）、多选+批量移动/删除（同列表连续性校验、容器连带确认）、Ctrl+C/V 子树复制粘贴（全树 id 重映射）、快照式撤销/重做（50 步上限）、选中集以节点身份重寻；93 tests + full gate passed
2026-09-02 | M12-editor-polish | done | docs/editor-design.md 方案落地：i18n.js 展示层（26 命令中文名 + kind/effect/ops/字段/术语表）+ 防漂移合同门禁、命令面板两行卡片根除横向滚动条、画布节点卡片中文重构 + 分支标签中文、if 操作符中文下拉、页脚术语表、悬浮操作条；修复 413 未排空 body 的 RST 健壮性缺口；99 tests + full gate passed
2026-09-02 | M10-capture | done | 桌面 UIA 窗口作用域 hit-test（免疫安全软件覆盖层）+ 浏览器 persistent/chrome-inspect-ws 双传输 + picker 注入与 selector 回验、ElementDescriptor 落库（/api/elements）、browser.launch userDataDir；真实 E2E ×2（浏览器合成点击 + 桌面执行器回验命中）；修复 agent stdout 编码/注册表死锁/413 排空；105 tests + full gate passed
2026-09-01 | M8-devserver | done | ADR 0007 定稿（devserver 隔离边界/捕获契约/两个待定问题结案）、rpa_core.devserver 骨架（catalog/compile/workflows CRUD + 捕获 501 占位，stdlib 零新依赖）、CLI devserver 子命令、架构检查新增 devserver 隔离断言、11 项合同测试；curl 全流程实测通过；full gate 83 tests 全绿；S1 验证移入 M10 计划单（M10 前置，需登录态 Chrome 人工协作）
2026-09-01 | M9-editor | done | ADR 0008 放行编辑器 UI（零构建硬边界）、devserver/static/index.html 单页（命令面板 + 线性画布 + schema 表单 + 编译回显 + 打开/保存闭环）、GET / 唯一静态路由；Playwright Chromium E2E 全流程 3 连跑稳定 + 3 项合同测试；E2E 首跑即捕获并修复编译成功分支漏设 .ok class 缺陷；87 tests + full gate 全绿
2026-09-01 | S1-verification | done | 用户日常 Edge 152 实测：DevToolsActivePort 文件读 WS URL（免 UI 交互）、/json/* 404 防扫描确认、connect_over_cdp 枚举真实标签页 + 新标签页操作（example.com picker 注入回验 + 百度搜索 URL 直达）+ 原生 CDP session；user-browser 子类型定案 chrome-inspect-ws 补录 ADR 0007 §4；遗留：开关持久性待重启确认、登录态断言顺延 M10b
2026-09-01 | capture-extension-research | added | 复核调研 chrome-relay（Native Messaging 免弹窗、Python host 可行）与 Playwright MCP --extension（核实默认每连接批准 + profile 唯一 token 绕过 + 标签组隔离）；修正"扩展=零弹窗"误判；降级链定稿：M10a 持久 profile → M10b chrome-inspect-ws（S1 通过）→ M10c 自研扩展（token 配对，立项不实装）；Panerelay/MCP 传输降记录备查，capture-transport.md §2.3-§2.5/§3 与 ADR 0007 §4 已更新
2026-09-01 | M9.1-editor-dnd | done | 编辑器拖拽：命令面板拖入画布任意位置插入（上/下半区落点指示）+ 画布节点拖拽排序，HTML5 DnD 零依赖；E2E 扩展拖入插入与重排断言 3 连跑稳定；87 tests + full gate 全绿
2026-09-01 | M11-editor-tree | active | 结构化树形画布立项：范式决策（树形非 DAG、快照撤销 50 步、静态拆分 allowlist）、拖拽先行验证片为显式确认门；实现由独立 agent 按 M11 任务单执行
2026-09-02 | M11-editor-tree | slice0+1 done | 切片 0（/static/{name} 硬编码 allowlist 路由 + 面板分组数据源）与切片 1（路径寻址树模型/全树唯一 id/禁止移入自身子树、递归缩进色带渲染、控制流分组、DnD v2 子树拖拽+空容器落点+48px 边缘自动滚动、schema default 构造）完成；静态文件拆分 index.html/app.js/styles.css；嵌套拖入保存读回 E2E ×3 稳定 + /static 合同测试 2 项；90 tests + full gate 全绿；**暂停等待维护者确认拖拽体验**
2026-09-02 | plan-adjustment | done | 维护者决策：M13 编辑器元素库扩展为捕获→编辑→回验→保存三段式；chrome-inspect-ws 每次连接弹窗成本不可接受，M10c 自研捕获扩展提前为 M14 实装（token 配对免弹窗），chrome-inspect-ws 降为备选；混合 auto 捕获顺延 M15
2026-09-02 | M13-element-library | done | 元素库面板+插入到节点+删除+结构校验(verify)+selector 捕获按钮；POST/DELETE/verify 端点与 do_DELETE handler；合同+3、E2E+2；110 tests + full gate passed
2026-09-02 | M13.1-flow-dir-assets | done | 每流程一个目录(WorkflowDirStore <name>/workflow.json) + 元素资产 <name>/elements/*.json 入版本库；元素端点嵌套 /api/workflows/{name}/elements[/{el}[/verify]]；capture pick saveAs 需 flow；DevServer 去 elements_root、CLI 去 --elements；前端元素库按当前流程(命名/打开即刷新)；ADR 0007 §2 修订；contract/e2e flow 化 + store 单测；122 tests + full gate passed
2026-09-02 | browserskill-spike | added | Tencent/BrowserSkill 调研 + S2 部分实测(Edge 152,bsk 0.1.11):selector 直通 click/fill/press/evaluate 全链路通过,evaluate 注入 picker + 合成点击 + 回验通过;发现 ControlOverlay 全屏遮罩拦截并以 elementsFromPoint 变体解出;record 语义 trace 无 selector 实证(范式不符);M14 调整为 bsk 单扩展路线,自研扩展降兜底;真实点选/小红书/iframe 验证待补;docs/capture-browserskill.md
2026-09-03 | M14-bsk-capture | slice done | bsk CLI/daemon 0.1.11 安装 + Edge/Chrome 扩展连接确认；probe 验证 evaluate 注入 picker（elementsFromPoint 变体绕过 ControlOverlay）→ Ctrl+Click 手势（普通点击穿透）→ 轮询读回 → 回验命中；BrowserBskCaptureSession 实装（session start/stop 映射、取消强制 stop、runner 注入可测）；devserver transport 白名单加 bsk + cli 工厂分发；合同测试 8 项 + transport 契约 1 项；实机验收：Edge 必应搜索框 Ctrl+Click 捕获 #sb_form_q 落库 workflows/demo/elements/bingSearchBox.json + 重新导航回验命中 1；131 tests + full gate passed
2026-09-03 | editor-host-adr | done | ADR 0010：编辑器保持 web 页面，遮挡实证矩阵（bsk Agent Window 独立 / 窗口作用域 hit-test 免疫 / 仅裸屏幕兜底路径会遮挡）；桌面客户端薄壳入 BACKLOG 远期（重估条件三条）；M14 desktop 捕获分支补先 attach 目标窗口
2026-09-03 | M14.5-cli-parity | done | CLI 通道对齐（ADR 0006 §6 落地）：catalog（digest+清单与 load_catalog 同源）/ capture browser|desktop（一次性会话，bsk 默认 + Ctrl+Click 合成、desktop F9/point，save-as+flow 落库 flow 元素资产，cancel+close 强制）/ elements list|show|verify；元素校验下沉 model/capture.py（devserver 薄封装）；cli_parity 合同 7 项；138 tests + full gate passed
2026-09-03 | M14a-bsk-executor | done | browser.launch v1.1.0 新增 transport:bsk（+browserInstanceId，能力差异声明 CSS only/仅主 frame）；executors/browser_bsk.py BskSession（运行期会话，session start/stop 映射、取消检查、close 强制 stop）；抽能力层 bsk_client.py（devserver 捕获与执行器共用，协议单实现）；executor 合同 10 项（launch/分发/click/input/getText/queryAll/waitFor 超时与取消/会话丢失映射/close 强制 stop）；i18n 补 transport/browserInstanceId 中文标签；148 tests + full gate passed
2026-09-03 | bsk-borrow-mode | done | 捕获用户已打开标签页：bsk tab list/borrow/return 链路 probe 实测（用户 7 个标签页枚举、借用进 Agent Window、evaluate 读回、归还原窗口原位置）；BrowserBskCaptureSession 加 page_url 借用模式（子串匹配用户标签页 → picker 注入该页 --tab-id → close 先归还再 stop）；bsk_client 补 tab_* 封装 + 数组载荷错误映射修复；devserver bsk transport 透传 pageUrl；合同 +3；151 tests + full gate passed
2026-09-03 | desktop-hover-capture | done | 桌面 hover 捕获模式：悬浮高亮框（layered 顶层点击穿透窗口 + region 边框裁剪）随鼠标移动实时框选 UIA 命中元素，作用域取悬停元素根窗口（免疫覆盖层）；F9 或 Ctrl+Click 捕获、Esc 取消；agent --hover / DesktopCaptureSession(hover) / devserver hover=true / CLI --hover 四通道一致；自检 probe 合成鼠标移动+F9 捕获 submitButton verifyCount=1；合同 +2；153 tests + full gate passed
2026-09-03 | desktop-hover-fixes | done | hover 捕获实机反馈修复：region API 用 pywin32 不存在的 CreateRectRgn（改 gdi32 ctypes）导致悬浮框首移即崩且被静默吞掉；Ctrl+C 慢取消——desktop pick 长 timeout queue.get 在 Windows 吞键盘中断，改 0.5s 短切片轮询 + CLI KeyboardInterrupt 即时 cancel/close 返回 130；hover 捕获瞬间以当前点重命中取根窗口（防 hover last_root 滞后打偏）；hover probe 合成移动+F9 复测通过；153 tests + full gate passed
2026-09-03 | hover-fine-drill | done | hover 捕获细粒度：_drill_to_leaf 从 ElementFromPoint 向下钻取最深包含点叶子（面积最小、max_depth 12、max_children 200 防浏览器 Document 爆炸、children/rect 异常容错、同 handle 防环）；细粒度 E2E 实证小 Label（controlType=Text automationId=nameLabel verifyCount=1）；钻取纯逻辑单测 6 项（替身树）；159 tests + full gate passed
2026-09-03 | hover-blindspots | done | hover 捕获盲区修复：UIA 虚拟元素（无 win32 handle）钻取断链——XAML（Terminal 标签）/桌面 ListItem 由 win32 窗口链（WindowFromPoint→最深子窗口→GA_ROOT）定位根窗口 + 窗口作用域最小包含枚举兜底；_hover_hit 双通道取更细 rect；浏览器窗口类跳过窗口枚举（属 bsk 页内领域）；Terminal 标签实测命中 HeaderTextBlock（rect 比整 tab 小 3 倍）；浏览器 UI 区域（标签栏/工具栏）确认为 Chromium 设计不进 UIA（非缺陷）；159 tests + full gate passed
2026-09-03 | hover-perf-browser-ui | done | hover 性能+浏览器 UI：根因定位（Chrome UIA 树 handle 全 0，drill 同 handle 防环误杀第一层；窗口枚举改含点优先 DFS，DOM 宿主 Chrome_RenderWidgetHostHWND/RootWebArea 跳子树但 UI 骨架照走）；浏览器 UI 骨架（TabStrip/Omnibox 地址栏/工具栏按钮）UIA 原生可达实证；DFS 节流 150ms（粗命中区域），帧率 avg 49ms→14ms、p95 81ms→41ms；_smallest_containing_wrapper/_describe_wrapper 旧全树枚举路径删除；159 tests + full gate passed
2026-09-03 | hover-virtual-handle-fix | done | 桌面图标 hover 卡顿根因：虚拟元素 handle=None 被快路径整个跳过 → 每帧强制 Progman 全树 DFS（340ms/帧）；修复为 handle 只用于根窗口定位、rect 不依赖 handle（ElementFromPoint 直接给细粒度 ListItem）；hover_hit 图标区 340ms→12ms avg；full gate 159 tests passed
2026-09-03 | hover-frame-pacing | done | hover 残留微卡修复：GDI region inner 句柄每帧泄漏补 DeleteObject；show_rect rect 未变时跳过重建+SetWindowPos；hit 节流 60ms→30ms、帧间隔 30ms→15ms（高亮跟随 16fps→~30fps）；细粒度 hover probe 复测通过；full gate passed
2026-09-03 | hover-sweep-skip-fix | done | 横扫漏框根因：图标间隙命中大背景 Pane 每帧触发 Progman 全树 DFS（300ms 卡顿帧=整图标滑过）；改为 DFS 仅在快路径完全失败（rect=None）时兜底，粗容器 hover 期间直接框粗 rect（响应优先），捕获瞬间仍全量 DFS 保精度；Terminal 置顶时快路径实证可达 TextBlock/分裂 Image；合成横扫 5/5 图标全中；159 tests + full gate passed
2026-09-03 | capture-latency-reuse | done | 捕获瞬间延迟优化：hover 循环缓存最后命中元素（last_leaf+last_rect+last_root），F9/Ctrl+Click 时鼠标仍在该 rect 内则直接 describe 复用，省一次 hit-test+DFS（桌面图标区 capture_with_retry 全量 DFS 500-1000ms → 复用后 68ms 实测）；hover_hit 返回 4 元组带 leaf；全门禁 159 tests（记事本 E2E 门禁内又抖一次、单独重跑过，老毛病非本次引入）
2026-09-03 | element-edit-confirm | done | 元素编辑确认切片：捕获后编辑对话框（改名/改 selector/metadata 只读/捕获时命中数展示/同名覆盖保护 confirm）→ 确认入库；元素库面板点元素名打开同一对话框编辑（改名=删旧存新）；捕获会话 pick 后未 cancel 的资源泄漏修复；E2E +3（对话框打开保存/取消不覆盖/改名删旧）；162 tests + full gate passed
2026-09-03 | capture-extension-selfbuilt | done | 自研 content-script 捕获扩展实装（无缝场景：Chrome/Edge 各装一份、全页面 hover+Ctrl+Click、零逐次授权）：extension/ MV3 零构建（manifest+content picker 复用已验证 elementsFromPoint 变体+background 短轮询+popup token 配对）；devserver 三个端点（token 配对/pending/result）+ transport=extension + ExtensionCaptureSession（threading.Event，无子进程）；不走 WS（stdlib 无 WS 服务端）；CLI --transport extension；合同 7 项（配对/未配对 501/错 token 403/pending/result 回传/落库 flow/取消）；169 tests + full gate passed；实机验收（装扩展+配对+跨浏览器捕获）待人工
2026-09-03 | capture-extension-e2e | done | 自研扩展实机验收通过：Edge 装扩展+token 配对（一次）→ 小红书搜索框 Ctrl+Click 捕获落库；同一会话切 Chrome → 煎蛋导航条再捕获——跨浏览器/跨页无缝衔接、零弹窗；demo 流程元素资产新增 extFirst/extSecond；pending 会话正确撤防
2026-09-03 | capture-ext-tofu-hover-cleanup | done | 扩展两个 UX 修复：(1) token 配对免手动——扩展自动生成 token + devserver TOFU 首次接触采纳持久化（workflows/.capture-extension-token），popup 改为只读展示/重新生成；合同测试同步 TOFU 语义（缺 token 403/首次采纳/错 token 403）；(2) 鼠标离开网页红框残留——content.js 加 mouseleave/blur/scroll 时 hideBox；169 tests + full gate passed
2026-09-03 | hybrid-capture | done | 混合捕获 auto（装了扩展网页走插件、没装走 UIA）：desktop_agent --hybrid（浏览器内容区 Chrome_RenderWidgetHostHWND 抑制高亮+忽略捕获手势让位扩展）；HybridCaptureSession 双通道先回传者胜（扩展回传→取消桌面 agent；桌面先回→正常返回）；devserver desktop hover 在已配对扩展时自动升级 hybrid（可 hybrid:false 关闭）；desktop pick 落库放开 kind==browser（混合会话扩展结果）；合同 5 项（未配对不 hybrid/配对自动 hybrid+pending 可见/扩展先赢/桌面先赢/混合落库）；174 tests + full gate passed
2026-09-03 | hybrid-acceptance | done | 混合捕获实机验收：插件腿（网页正文 Ctrl+Click 捕获小红书 AI 搜索框 #search-input-ai verifyCount=1，kind=browser 带 url）+ UIA 腿（桌面图标/终端控件）+ 让位（正文区桌面红框让位页内红框）双通道通过；修复 stale-root bug（无 handle 虚拟元素的根窗口用 win32 链解析，metadata 不再陈旧）；full gate 174 tests
2026-09-03 | uia-web-fallback-disproven | done | 实测：没装扩展时 UIA 兜底捕获 Chrome 网页正文命中渲染层 Pane（Intermediate D3D Window），verifyCount=29 不唯一不可用——网页正文无可靠 UIA 退路，网页捕获只有扩展/bsk 两条正路；UIA 正确用途=桌面应用+浏览器 UI 骨架；结论记入 capture-transport.md §2.6 防翻案
2026-09-03 | element-capture-entry | done | 元素库「＋捕获」入口：面板头部按钮 → 网页（extension 无缝，默认）/ 桌面（hover，已配对自动 hybrid）二选菜单；captureIntoField 重构为通用 captureElement + captureBrowserElement/captureDesktopElement + _confirmAndSave 复用编辑确认对话框；E2E +1（菜单开合）；full gate 175 tests（门禁内 uia/win32 桌面 E2E 各抖一次，单独重跑均过——环境敏感老毛病）
2026-09-03 | M14-extension | done | M14 浏览器捕获收口：bsk 执行传输（M14a）+ 自研 content-script 扩展（无缝捕获主路线）+ 混合捕获（HybridCaptureSession 先回传者胜）+ 桌面 hover（细粒度钻取+性能+盲区）+ 元素编辑确认 + 「＋捕获」入口 + 借用模式（用户已开页面）+ token TOFU 免配对；实机验收全过；175 tests；feature capture-extension 通过。active → M15 WorkBuddy 连接器
2026-09-03 | M15-workbuddy-connector | done | WorkBuddy 连接器入驻（CLI+Skill）：CLI 补 auth/status/unauth 三件套（无认证固定 ready + statusMatch 契约）；workbuddy-connector/ 目录（connector-meta.json type:cli + cli.json runtime python + icon.svg + skills/rpa-automation/SKILL.md + references 三件套）；CLI 入口统一 UTF-8（修 cp936 导致 statusMatch 解码失败）；修真实部署缺口：commands/ 打进 wheel（hatch force-include）+ 包内优先解析；合同 6 项 + 检查清单脚本化全过 + 干净 venv 本地路径安装验证；181 tests + full gate passed
2026-09-03 | pypi-publish | done | rpa-core-runtime 0.1.0 发布 PyPI（rpa-core 撞名被拒，改名 rpa-core-runtime）；wheel 含 26 条命令清单（hatch force-include）；干净 venv pip install rpa-core-runtime → status/validate/catalog 实测可用；WorkBuddy cli.json init 换成 PyPI 名（市场提交前置就绪）
2026-09-03 | M17-editor-advanced | done | 编辑器进阶：变量补全（${ 触发引用路径下拉，inputs/steps.outputs/loop/error）；全屏画布切换；运行状态高亮（devserver /api/runs/latest-events 读 run_artifacts 最近 events.jsonl，画布节点按 node_id 标 succeeded/failed/running）；E2E +3；184 tests + full gate passed；M17 收口
2026-09-03 | M18-run-control | done | 运行控制（ADR 0011 子进程 run host）：RunManager spawn `rpa-core run`（stdout 解析真实 run_id → run_artifacts/<uuid>/ 读证据，cancel=终止子进程）；/api/runs start/status/events/cancel；CLI run 新增 --inputs；前端 ▶运行/■取消/事件流面板；合同 5 + E2E 1；190 tests + full gate passed；架构检查确认 devserver 不 import runtime 隔离不破
2026-09-03 | bsk-keepopen | done | browser.launch 新增 keepOpen（bsk 传输专用）：true 时 executor.close() 不 session.stop（Agent Window 留给 bsk daemon 持有，空闲超时兜底，可人工继续）；显式 browser.close 命令仍停；manifest v1.1.0 输入 + i18n 标签 + 合同 2 项（close 跳过 keepOpen/显式 close 仍停）；实机验证 run 结束后 bsk session 仍在 list、Agent Window 保留；192 tests + full gate passed
2026-09-05 | workbuddy-connector-compliance | done | 对照 open.workbuddy.cn/docs/connector 合规审查：修 cli.json win32 入口 rpa-core.cmd→rpa-core.exe（干净 venv 实证 pip 只生成 .exe 无 .cmd，否则 Windows auth/status/unauth 全挂）+ SKILL.md frontmatter 补必填 description_zh/description_en/version/author；合同测试 +2 防回归（win32 禁 .cmd、frontmatter 必填字段）；194 tests 全绿（门禁内记事本 E2E 抖一次单独重跑过，已知老毛病）
2026-09-06 | extension-installer | done | 扩展静默安装双通道（修订调研报告五处误判）：①外部扩展注册表（逆向影刀 6.2.23 实锤：HKCU\Software\<vendor>\<browser>\Extensions\<id> 写本地 CRX path/version，external_registry_loader 重启静默装，免管理员/免商店/免 devserver/不受未加域 [BLOCKED] 门控；本机 location=3 实证）②forcelist 策略（HKCU 加固时 UAC 提权 HKLM 降级；未加域仅商店来源=官方门控已引证）。**Edge 152 消费版对本地 CRX 静默安装按"未知来源"硬禁用（启用灰、不可点），豁免实验矩阵全否（update_url 双商店/开发者模式/策略白名单/解包目录 path）→ 零点击启用唯一干净路 = 商店上架 + forcelist（uBlock 对照实验本机完整证明）**；影刀 enabled 实为 Secure Preferences HMAC 预写 ack_external（灰手法，不复刻，实验记录 docs §6.4）；Edge --pack-extension 签名+pem 持久化、纯 stdlib DER 推 ID 与 openssl/cryptography 双互验、devserver 托管 update-manifest XML + CRX 正确 Content-Type；CLI install-extension（external-registry 默认/--policy/--remove 双清）+ cli.json init.win32 + wheel force-include；合同 24 项（fake winreg）+ 真机全链路实测；本机未定位进程周期性清 Edge 策略键（§6.2）；24 tests + full gate passed
2026-09-07 | extension-install-chrome | done | Chrome/Edge 152 双浏览器真机对照（影刀插件重装对比 + CWS/Edge商店 URL 变体矩阵，docs §6.5 新增、§6.4 修订注记、manifest update_url 改 CWS）：外部注册表 location=3 安装双浏览器一致成功；**放行判据 = manifest update_url 归属**——Chrome 指 CWS 可点启用成功（ack_external=true，无需上架）、指 Edge 商店灰禁 [256]；**Edge .62 指 CWS 同样可点启用成功（两次复测，推翻 §6.4 .53 的"CWS 依旧禁用 8192"，差异疑为 .53→.62 版本或已禁用实例叠加改 URL 重打包所致，两说并留待统一复核）**、指自家 Edge 商店 URL（未上架）启用后报"可能已损坏" [1024]（两次复测）；影刀预写 INTERNAL/allowlist/developer_mode + Secure Preferences HMAC 双浏览器均非必要（其扩展最终同为 location=3 + 用户点一次启用）；disable_reasons 码位语义跨浏览器/版本不可套用（Chrome 灰禁 256/待启 8192；Edge 待启 8192/损坏 1024）；产品含义：manifest 改 CWS 后未上架即可双浏览器用（一次手动启用），零点击仍须上架+forcelist；docs §1.0/§2/§5/§7 同步修订
2026-09-07 | extension-guide-install | done | 插件安装引导入口（CLI + 编辑器，ADR 0012 记录"devserver 能力层复用而非子进程代理"——extension_installer 可 import 故不走 ADR 0011 spawn）：能力层 extension_installer 补状态探针（browser_user_data_dirs/read_profile_extension_state 读各 profile Secure Preferences 判 installed/enabled/disable_reasons，注册表只读、纯函数可注入）+ extension_status（每浏览器 binary/registryEntry/profiles）+ install_external_registry_entry 增 browsers 单浏览器参数；CLI install-extension 增 --browser {chrome,edge} 单选 + --status 只读（含 enableHint 引导）；devserver GET /api/extension/status + POST /api/extension/install {browser}（未打包先 pack，非 win32/未打包 503/400/405 沿用码表）；编辑器工具条「⇲ 插件」对话框（Chrome/Edge 状态徽标 未安装/已登记/已装未启用/已启用 + 单选安装 + 装后去扩展页启用引导）；合同 +11（Secure Preferences 三形态、status 单浏览器 registry/profile、单浏览器 install、CLI --browser/--status/not-packed、端点 200/400/405、方法守卫）+ 编辑器 E2E +1（对话框双浏览器行）；live CLI --status 实测 registry 检出、profile 空（真实未启用态符合）；fixed app.js async init 被 substring 编辑破坏回归；full gate passed
2026-09-07 | ext-uninstall-block | done | 真机排查"装了却没出现"根因：Chrome/Edge `Preferences.extensions.external_uninstalls` 记录过本扩展被卸载 → external_registry_loader 永久跳过重装（注册表/CRX/端点全对也无效）；清除该 id + 冷启动（loader 延迟 40-60s 写 profile）即装载成功（location=3 实测）。落地：能力层 detect（read_profile_extension_state 兼读 Preferences.external_uninstalls → uninstall_blocked/uninstallBlocked）+ clear_uninstall_block（单浏览器参数、空键移除）+ browser_running（tasklist）+ install_external_guided（未运行浏览器先清屏蔽再写注册表，返回 runningBrowsers）；CLI --unblock + install 自动解除；devserver /api/extension/unblock + install 走 guided；编辑器「解除屏蔽」按钮 + 安装后 running 提示；docs §7 补 external_uninstalls 排查与双浏览器注册表独立性说明；合同 +9（block 检测/清除/guided/browser_running/端点/CLI）；live：re-block → install 自动 unblocked → Chrome 冷启动 location=3 装载验证通过；E2E 补 wait_for_selector 消抖；full gate 239 passed
2026-09-07 | ext-load-unpacked-guide | done | **重大修正 + 默认安装改为 Load unpacked 引导**（docs §6.5.1）：§6.5 "指 CWS 即可用、无需上架"被真机推翻——未上架本地 CRX 指向商店 update_url，启用后 ~30s 被双浏览器异步商店校验判损坏（disable [1024]、corrupted_disable_count+1，全新 profile 稳定复现）；影刀不损坏因其 ID 真上架。无 update_url / 指 devserver http 均 [256] 灰禁。开发者模式 Load unpacked（源码目录，location=4）持久可用（影刀 Chrome 同款）；**chrome:///edge:// 扩展页无法从外部命令导航**（冷启动/传 URL 均落新标签页，安全设计）。落地：能力层 read_unpacked_extension_state 按 Secure Preferences `location==4 && path==源码目录` 匹配（unpacked 扩展不存 manifest 不能按名）+ extension_status 增 unpackedProfiles/extensionDir/installMode + open_path_in_explorer（跨平台 explorer/xdg）+ open_browser_extensions_page（尽力启动）；CLI install-extension 默认=Load unpacked 引导（输出步骤+自动打开目录+开扩展页），--registry 保留外部注册表（仅上架后）、--guide/--status 等；devserver status 返回 extensionDir+installMode、新增 /api/extension/open-dir 与 /open-page；编辑器「⇲ 插件」对话框重构为 3 步引导页（复制路径/打开目录/每浏览器打开扩展页+自动复制地址提示粘贴）；docs §1.0→Load unpacked 默认 + §1.0a 注册表备选、§6.5.1、§7、ADR 0012 修订、extension/README；live：Chrome Load unpacked 真机加载成功 → 能力层检测 installed+enabled location=4 验证通过；合同 +4（unpacked 检测 x2 + 引导默认 + 打开端点）、E2E 更新；full gate 243 passed
2026-09-07 | ext-guide-dialog-polish | done | 插件安装模态框收口（维护者反馈）：**仅「关闭」按钮关闭，点击遮罩不关**（移除 mask 外点关闭，toggleExtensionDialog(open) 显式开关）；引导改四步：①定位源码目录(复制/打开) ②打开浏览器（新端点 /api/extension/open-browser + 能力 open_browser，纯启动不带 URL）③打开扩展管理页（chrome:// 无法外部导航→复制地址提示粘贴）④开启开发者模式→加载已解压选目录；前端 renderExtensionRows/bindExtensionOpenButtons 抽公共渲染+绑定；styles 补 .ext-open-browser；E2E 断言两步按钮 + 遮罩点按不关 + 关闭按钮才关；合同 +2（open-browser 200/404）；full gate 244 passed（uia 桌面 E2E 门禁内抖一次、单独重跑过——已知环境敏感老毛病）
2026-09-07 | ext-guide-step3-manual | done | 维护者反馈二：第 3 步「打开扩展管理页」不给按钮（按钮无效，chrome:///edge:// 无法从外部导航）——改为纯文字打开方式（地址栏输入 chrome://extensions / edge://extensions，code 可点选复制）；第 3 步区域只渲染状态徽标（已启用/已加载未启用/未加载）；前端去掉 open-page 渲染与绑定（renderBrowserButtons/renderStatusRows 拆分）；删除已无使用方的 /api/extension/open-page 端点、extension_open_page_view、能力 open_browser_extensions_page（CLI 默认引导同样不再尝试开扩展页，改 open_browser + 提示手动输入）；E2E 断言第3步无按钮；full gate 244 passed（windows 桌面 E2E 门禁内抖一次、单独重跑过——已知环境敏感老毛病）
2026-09-07 | M19-editor-interactions | sliceA done | 编辑器交互补强立项（对照隔壁 rpa_script 仿影刀编辑器盘点缺失交互，技术栈评估：维持零构建 vanilla 至 C/D/E 决策点——app.js 1921 行/74.7KB 单文件，A/B/F/G 低状态无压力，C/D/E 手写 DOM 同步高发、做完 B 后按行数与回归决定是否立 ADR 迁 React，打包产物入 devserver/static）；切片 A 完成：面板可拖拽分栏（.resize-v 分隔条 data-resize=palette/props，min/max 夹取，body.resizing-panel 防选中，宽记 localStorage rpa_editor_panel_widths，initPanelResize+restorePanelWidths）；E2E test_editor_resizable_panels_persist（拖动增宽 + reload 保持）；M19 任务单+BACKLOG+feature editor-interactions（passes:false）+project_state active 指向 M19；full gate 246 passed
2026-09-07 | M19-editor-interactions | sliceB done | 切片 B：元素库移入底部 dock（从右侧 props 移出）：index.html 重构 main→resize-h(#bottom-resize)→#bottom-panels（elements-head+捕获入口+elements-list）→footer；props 只留属性；CSS #bottom-panels 固定高 220/min120 + resize-h 拖高 + body.resizing-bottom 防选中 + 元素列表改横向网格 auto-fill minmax 280；JS initPanelResize 扩展：统一 localStorage rpa_editor_panel_resize（palette/props 宽 + bottom 高，restore/savePanelResize，min/max 夹取）；运行面板维持 footer 未动（避免重复 #run-panel id）；E2E test_editor_bottom_element_dock_resizable（dock 有元素、props 无元素库、高度拖动+刷新保持）；M19 任务单切片 B 勾选；full gate 246 passed（desktop 合同 TIMEOUT 抖一次单独重跑过——已知环境敏感）
2026-09-07 | M19-editor-interactions | sliceF done | 切片 F：捕获自动刷新（后台捕获落库 dock 自动带出）：state.elementsSeen 记录已渲染元素名集合；loadElements 刷新集合；pollElements 2s 轮询——仅 document.visibilityState==visible 且有命名 flow 时 GET 列表，与 seen 差集（数量变化或含未见过名）才 renderElements 重渲染；startElementPolling 挂 setInterval + visibilitychange（可见即 loadElements）；捕获保存仍走既有 loadElements 即时刷新；E2E test_editor_element_auto_refresh_on_poll（页面开着外部新增元素，8s 内无手动刷新自动出现）；M19 任务单切片 F 勾选；full gate 248 passed
2026-09-07 | M19-editor-interactions | sliceG done | 切片 G：运行参数对话框 + 运行中离开警告：runWorkflow 拆分——先 collectRunInputs()（流程顶层 inputs 声明时弹 #run-params-mask 对话框，按原始类型 string/number/bool 渲染输入并覆盖，取消返回 null 不启动；无声明直接 {}）；POST /api/runs 带 inputs（非空才附 body.inputs）；beforeunload 加 activeRunId 条件（运行中拦截离开）；HTML 加 run-params 对话框、CSS 复用 dialog 体系；E2E test_editor_run_params_dialog_when_inputs_declared（PUT 带 inputs 流程→打开→▶→弹窗显示 name/count→取消 run-panel 保持隐藏）；既有 test_editor_run_control 适配（inputs 流程现在先弹参数框，点 run-params-run 确认后运行）；M19 任务单切片 G 勾选；full gate passed
2026-09-07 | M19-editor-interactions | sliceD done | 切片 D：属性表单 selector 字段「从元素库选」下拉（重排——切 C 因无截图源后置）：schemaField key==selector 分支加 .element-pick 下拉（选项来自 state.elementsSeen 当前流程元素），change 异步 GET 元素→仅接受 browser css→setWith+回填 input 值+kind 徽标（element-pick-badge），与现有「捕获」按钮并列；无值时不渲染反向徽标（值非引用无法反查元素名）；CSS .element-pick-row/.element-pick-badge；桌面字段未纳入（elementId/locator 语义不一致，避免臆测）；E2E test_editor_selector_field_picks_element_from_library（选 searchBox→selector 填 #kw+徽标；change 异步需 wait_for_function 消抖）；M19 任务单切 D 勾选、切 C 改后置待截图源；full gate passed
2026-09-07 | M19-editor-interactions | done | 切片 E 决策=留接口（字段密度实测 26 命令 schema 参数 1-6 个、多数 3；多 tab 会空分区负优化，单列即既有形态，字段显著增多>~8 再划分）+ 收口关闭：切 C（截图灯箱）后置 BACKLOG 远期 blocked（待捕获链路自动截屏）；M19 任务单置 done + 完成证据 + BACKLOG M19 移已完成、切 C/多 tab 表单入远期；project_state completed_milestone=M19、active 清空；feature editor-interactions passes true；full gate 250 tests（uia 桌面 E2E 门禁内抖一次、单独重跑过——已知环境敏感老毛病）
2026-09-07 | editor-canvas-fullwidth | done | 画布铺满整块（维护者反馈——对比隔壁仿影刀编辑器画布区）：根因 #canvas 被 max-width:720px 掐窄（树只占中左一窄条）；改 #canvas width:100% 去 720 封顶 + #canvas-wrap 设画布浅底 var(--canvas:#e9edf1)（白节点卡浮起、铺满整块）+ 子级色带改低透明度 rgba 浅染（层次仍在但不喧宾）+ 节点 li 加微阴影/hover 浮起 + fullscreen 统一 --canvas；CSS-only 无 DOM/逻辑改动；probe：1400 视口 canvas 778px（原封顶 720）、节点行全宽 756px；editor E2E 全绿；full gate 通过（UIA 桌面三测试门禁内并发抖一次、单独重跑全过——已知环境敏感）
2026-09-07 | editor-canvas-layout | done | 画布铺满整块（二修，维护者反馈：指可拖指令的整块画布区）：除上一修去 #canvas 720 封顶外，缩窄两侧固定面板默认宽让画布主导——palette 260→210、props 320→280；实测 1366 画布 776→866、1280 690→780、1500 达 1000、1920 达 1420；CSS-only；editor E2E 全绿
2026-09-07 | editor-canvas-fullarea | done | 画布=整块工作区（维护者截图对比反馈：我们的空画布只是左上虚线小框，隔壁整块都是画布）：拖放监听从 ol#canvas 上移到 #canvas-wrap（整块含节点外空白区都是拖放目标，落点走既有 rootEndDrop 兜底追加根末尾）；根级空画布占位撑满（#canvas 改 flex 列 + 根级 li.drop-empty flex:1 min-height 220 虚线大框透出画布底色，嵌套分支内空容器保持小框——规则改为 #canvas ol.children li.drop-empty 限定）；拖拽悬停空白区时 #canvas 加 drop-root-hint 虚线外框提示落点=追加根末尾；E2E test_editor_empty_canvas_full_area_drop（空占位高度≥wrap 60% + 拖到画布底部空白仍追加成功）；full gate 通过
2026-09-07 | editor-canvas-clean | done | 画布直接撑满中间区、去掉标题行/提示/全屏按钮/虚线框（维护者反馈）：index.html 删除 canvas-wrap 内 h2「画布 · sequence root + ⛶全屏」与 p.hint；删除 toggleFullscreen + btn-fullscreen 监听 + .fullscreen CSS + test_fullscreen_toggle E2E（M17 全屏特性按维护者要求移除，画布恒撑满）；根级空画布 li.drop-empty 去 dashed 边框改透明撑满居中 muted 提示；拖拽空白区落点提示 drop-root-hint 移除（空白区仍可拖、落点仍走 rootEndDrop 追加根末尾，无框提示）；full gate 通过
2026-09-07 | editor-command-groups | done | 指令面板按语义类别分组展示（参考隔壁仿影刀指令展示）：由机械的 id 前缀分组（browser/data/desktop）改为语义动作类别——控制流 / 页面导航 / 元素操作 / 数据处理 / 桌面会话 / 桌面控件；新增 SEMANTIC_GROUPS 有序类别（label/icon/color + member 谓词）+ CATEGORY_COLORS；paletteGroup 加 color 参数给类别 header 着色；未匹配命令兜底按 id 前缀；条目仍按 kind 分色、拖拽/点击/中文名不变；E2E 全绿；full gate 通过
2026-09-07 | editor-palette-tree | done | 指令面板可展开树（参考隔壁 CommandPanel）：paletteGroup 改为「组 header(chevron 图标+类别图标+名称) + .palette-children(条目容器)」；chevron 向右、展开时 rotate-90(open)；点击组 header 收起/展开，state.collapsedGroups 记忆（默认全展开）；搜索(filter 非空)时 forceExpand 强制全展开；icons.js 新增 caret 图标；CSS .palette-caret/.palette-children.hidden/.palette-group cursor:pointer+hover；E2E test_editor_palette_tree_collapse（点组收起条目隐藏/再点展开恢复/搜索强制展开）；full gate 通过
2026-09-07 | command-inventory | done | 指令清单对比文档（docs/command-inventory.md，用户要求整理）：采集我们的 26 条（含 manifest input_schema/output_schema 的输入输出参数说明，简洁呈现 字段:类型 + 必填*）与隔壁 commands-new.json 62 条（含中文名/fields/category），分三类表：①我们的指令 ②隔壁按类别 ③对比与推荐新增（data.setVar/log/getCurrentDateTime、browser.takeScreenshot/newTab|switchTab|closeTab/hover/getPageInfo、控制流 while/break/continue、desktop.takeScreenshot/getMousePos/sendKey 等，含复杂度与优先级）
2026-09-07 | command-inventory-html | done | 指令清单改自包含 HTML（docs/command-inventory.html，替代 md）：页面顶部术语说明——kind（命令类别 action/query/transform/lifecycle 中文+含义）、effect（副作用等级 pure/read/idempotent-write/unsafe-write/session 中文+含义）、输入参数格式（字段:类型 * = 必填；类型 string/number/integer/boolean/object/array<T>/any）说明；我们的 26 条（含 in/out）+ 隔壁 8 类别可展开 + 推荐新增表 + 建议优先级；内联 CSS/JS 零外部依赖；功能顶部搜索筛选 + 全部展开/收起按钮；Playwright 验证：26 行、8 类别、kind/effect 注释展示、无 JS 错误
2026-09-07 | editor-sessionid-ref | done | sessionId 字段无需手填（用户反馈）：它是 browser.launch/desktop.attachWindow 等创建会话节点的输出、被后序命令引用的运行值；编辑器属性面板对 key==sessionId 字段渲染「引用创建会话的节点」下拉（collectSessionNodes 采集 output 含 sessionId 的 lifecycle 节点），选择后自动填 \（跨命令运行值绑定）+ 提示文案；CSS .session-pick-row/.field-hint；E2E test_editor_session_id_binds_to_launch_node（点 click 节点下拉含 launch、选中填引用）；full gate 251 passed（windows 桌面 E2E 门禁抖一次、单独重跑过——已知环境敏感）

## 指令优化规划（影刀72条对比分析驱动）

2026-09-08 | command-params-phase1 | done | P1-S1~S6 全部完成：sessionId resourceType 标签；browser.click 六参数；browser.input 六参数；browser.close forceKill/ignoreUnload；desktop.closeSession forceKill；desktop.attachWindow matchMode；desktop.click/input/closeSession 同步；editor 按 resourceType 过滤下拉；i18n 标签同步；full gate passed
2026-09-08 | command-new-batch1 | done | Phase 2 第一批新指令（8条×2后端）：browser.hover（Playwright+BSK）、desktop.activateWindow/setWindowState/setWindowVisible/moveWindow/resizeWindow/getWindowTitle/getSelectedText（UIA+Win32 共16条）；manifest + executor + i18n + palette groups + 合同测试更新；41 manifests + full gate passed
2026-09-08 | command-new-batch2 | done | Phase 3 第二批新指令（6条×2后端）：browser.executeScript/screenshot/select（Playwright+BSK）、desktop.screenshot/select/drag（UIA+Win32）；manifest + executor + i18n + 合同测试更新；50 manifests + full gate passed
2026-09-08 | command-new-batch3 | done | Phase 4 第三批新指令（4条）：browser.upload/download/handleDialog（Playwright+BSK）、desktop.getWindowList（UIA+Win32）；manifest + executor + i18n + 合同测试更新；55 manifests + full gate passed
2026-09-08 | var-system | planning | Phase 5：用户变量体系——ActionNode.output_name + scopes.variables + ${var_name} 引用语法；方案已落 BACKLOG.md
2026-09-08 | var-system | done | Phase 5 用户变量体系收口（维护者确认子路径增强方向）：ActionNode.output_name + scopes.variables[output_name]=outputs dict + resolver 变量子路径（${var_name} 整值 / ${var_name.field} 取字段）；compiler 静态收集 output_name 前向/未知/error_var 词法校验（与 steps 前向语义一致）；editor「输出变量名」字段 + sessionId 下拉显示变量名并引用 ${name}.sessionId；新增单测 9 + e2e 1；full gate 257 passed（5 桌面 UIA 真实交互 e2e 因本会话无可交互桌面 pre-existing 失败，HEAD 基线复现同失败，与本次改动无关）+ ruff/架构/任务检查通过
2026-09-08 | uia-e2e-flakiness | done | 桌面 UIA e2e 根因定位与修复：真根因非环境限制——进程内首次 Desktop(backend=uia) 全枚举 ~60s（桌面开游戏如炉石时被其慢 UIA provider 拖住），而 DesktopExecutor 每命令外层 wait_for=15s → 首启落在命令内必超时。修复 tests/e2e/test_uia_desktop.py：_wait_for_window 改 Win32 FindWindowW（0ms，不触发全桌面 UIA 枚举）+ 新增 _warmup_uia() 于 executor 前提前消耗 UIA 首启；关掉炉石后 test_uia_desktop_vertical_slice PASS。生产隐患待根治：executor attachWindow 全桌面枚举 + 15s 超时，真实用户开游戏时首个 attach 会超时（方向：FindWindowW 拿 hwnd → handle 级 attach）
2026-09-09 | uia-e2e-flakiness-production | done | 生产侧根治：DesktopExecutor._find_windows_by_title 替代全桌面枚举（exact=FindWindowW 毫秒级+UIAWrapper 单窗口构造、contains/regex=EnumWindows+逐个 UIAWrapper）；_window_by_handle 改 UIAWrapper(UIAElementInfo(handle)) 直接构造；删除死代码 _filter_windows/_windows_by_title_fallback；desktop.win32 同步 _window_by_handle；新增 operationTimeoutMs 可配置覆盖默认 15s（desktop.attachWindow/desktop.win32.attachWindow manifest）；合同测试 2 项防回归；264 tests + full gate passed
2026-09-09 | output-aliases | done | 变量别名体系重构：ActionNode.output_name → output_aliases (dict[str,str])；orchestrator 将每个别名存入 scopes["variables"]；compiler 静态检查别名引用；所有 browser/desktop/data 命令 manifest 新增 x_outputs 声明输出别名；PropsPanel 支持 output_aliases UI（per-field 别名输入）；测试全部迁移并通过（150 tests + architecture gate）
2026-09-09 | spa-fallback-fix | done | SPA fallback 修复：仅对无扩展名路径返回 index.html（"." not in filename），避免对 /static/* 路径误返回 SPA；测试更新：移除 /static/index.html（Vue build 后合法存在）；无泄漏测试改为接受 200 或 HTTPError
2026-09-09 | open-webpage-merge | done | 打开/关闭浏览器指令并入打开/关闭网页：browser.navigate v2.0.0 吸收 launch 全部参数（headless/userAgent/userDataDir/transport/browserInstanceId/keepOpen + x-depends 联动）并自建会话，输出「保存网页对象到」（sessionId 别名 primary，x-outputs）+ url + resourceType；launch.json 删除（catalog 55→54），browser.close 改名关闭网页；executor navigate 双后端一步 open+goto（playwright/bsk，导航失败回收本次新建浏览器）；devserver catalog 下发 x-outputs；旧版编辑器 output_name → x-outputs 逐字段别名输入、session 下拉改引 ${别名}、语义分组/i18n 去 launch；附带修复：browser.close 持久上下文 context 未定义 NameError；index.html 恢复 tracked 旧版编辑器（Vue 迁移中间态不入本次，test_devserver 根页断言改回旧编辑器特征）；ruff 排除 scripts/ 一次性抓取脚本（存量 24 处 lint）；补 test_desktop_contract 缺 import pytest；UIA e2e 门禁内必抖根治——_force_foreground（AttachThreadInput 绕过前台锁）运行前抢回前台，此前 SendInput 落到被前置浏览器用例占用的前台窗口；full gate passed（54 manifests）
2026-09-09 | open-webpage-yingdao-parity | done | 打开网页对标影刀：补 channel（浏览器类型 chromium/chrome/msedge）与 args（命令行参数，array<string>）两输入，x-depends 限 playwright 传输，executor 透传 launch/launch_persistent_context；i18n 新增 commandFields 按命令覆盖机制（args 在 navigate=命令行参数、executeScript=脚本参数不再打架）+ channel 标签；属性面板分区展示——「输入参数」蓝条分区头 + 「输出参数（保存到变量，供后续指令引用）」绿条分区头（.props-section-in/out）；devserver 重复绑定陷阱实锤（SO_REUSEADDR 双进程同端口，旧进程内存缓存 Vue 页）杀掉 stale 进程恢复；合同 +1（channel/args 透传）+ E2E 断言分区头与保存网页对象到字段；x-outputs 新增 hidden 标记——最终网址/资源类型不渲染别名框（运行值仍可 ${} 手动引用，对标影刀仅暴露保存网页对象至）；full gate passed（54 manifests）
2026-09-09 | catalog-viewer-page | done | 指令清单查看页：devserver /api/catalog 下发扩展字段（executor/risk/capabilities/resources/stability/retryable/default_timeout_seconds）+ x-outputs；新增零构建 static/catalog.html（实时拉接口渲染 54 条 manifest：分组 浏览器14/桌面Win32 19/桌面UIA 17/数据4、搜索过滤、全部展开/收起、kind/effect 徽标、输入/输出参数表分色分区、x-depends 联动标注、x-outputs primary★/hidden 灰行、错误码清单、digest 显示），复用 i18n.js（含 commandFields 覆盖）；编辑器工具条加「☰ 指令清单」新窗口入口；合同 +2（catalog 扩展字段全等对 load_catalog、页面可访问）；Playwright 实测渲染（navigate 卡含保存网页对象到/命令行参数/hidden 行、executeScript args 不串台、搜索过滤、无 JS 错误）；full gate passed
2026-09-10 | var-system-writable | done | 用户变量体系可写化 + 前端标签化（维护者方向：别名直接引用不需花括号、Py 开关下可直接读写、fx 开关下别名渲染为标签）：①新增 data.setVar 命令（catalog 54→55）——manifest x-var-write={"field":"varName"} 声明变量写入，worker 纯透传 varName+value，orchestrator 按声明写入 scopes.variables（规则 3：handler 不改 scopes）；允许同名覆盖 = 可写语义（节点1定义→节点2改→节点3读改后值），端到端实测 webpage1 读到 second。②compiler 新增 _validate_alias_declarations：重复别名报 Duplicate alias、保留根名（inputs/steps/loop）报 Reserved alias name；data.setVar 例外（赋值语义不参与重复拦截，但 varName 指向保留名仍拦截）；_collect_output_aliases 纳入 setVar 字面量 varName（前向引用判定准确）。③前端旧版 app.js：computeReferencePaths/collectUserVariables 纳入用户变量（原缺失，补全列表只有 steps/inputs/loop）；replaceWithVarSelect 重构为标签化编辑器（contenteditable + [变量名] chip，支持「当前变量[webpage1]，代表页面引用」文本内嵌，底层仍存 ${名} 零迁移，变量下拉分组置顶）；data.setVar varName 专用下拉（选已有=覆盖赋值/填新名=定义）；pasteClipboard 新增 remapSubtreeAliases（撞车自动 _copy 后缀 + 子树引用同步改写 + 明确提示）；顺手修 newFlowNode 的 try 分支吞掉 return 的存量 bug。④devserver catalog_overview 下发 x-var-write（前端依赖，缺则功能全失效）。⑤测试：协议 +6（查重/保留名×3/setVar 重赋值/setVar 保留名/前向引用）、runtime +1（可写语义端到端）、devserver 合同 +1（x-var-write 下发）；示例 search-and-save 改用别名+setVar 演示；README 补「用户变量」章节
2026-09-10 | fx-editor-layout-fix | done | fx 模式两项布局回归（维护者反馈：①fx 按钮"跳到输入框前面"②选择变量后下拉框高度暴涨）：①根因：replaceWithVarSelect 用 display:none 隐藏 input 但 flex 仍占槽，行序 [Py, hidden-input, fx, editor, picker]→fx 视觉上"跑到 input 前面"；同时 contenteditable 默认可拉伸 40px+ native <select> 自身高度把行高推至 42px+contenteditable 不响应 height 跑到 40px。修复 replaceWithVarSelect 改为 origInput.remove()（detach，留 _fxHiddenInput 引用）；editor/picker 用 insertBefore(fxBtn) 让行序固定为 [Py, editor, picker, fx]；restoreTextField 重新 insertBefore 把 input 插回 fx-btn 前。②styles.css：.expr-input-row > * 统一 30px；.fx-var-select/button 强制 height:30px；.fx-tag-editor 改 box-sizing+min/max-height=30+nowrap+overflow-x:auto（contenteditable 不再被默认行高拉高）。Playwright 复测：进入 fx 模式行 30px、顺序 [py, editor, picker, fx]、反复切换可逆；ruff+架构门禁通过、210 单测/合同测试通过（浏览器 bsk 合同仍为存量环境问题已记录）
2026-09-10 | fx-var-popup | done | fx 变量下拉改浮层（维护者反馈：插入变量下拉应点击输入框后弹出，不应挤在输入框后面）：移除内联 <select>（.fx-var-select 删除），改点击编辑器弹出 .fx-var-popup 浮层（复用 .ref-completion 定位/样式，用户变量/内置作用域分组头 .fx-var-popup-group）；变量列表每次打开重新收集（新增变量即时可见）；mousedown+preventDefault 保焦点保光标；Esc/点外部/选中后关闭，退出 fx 随编辑器关闭；.expr-input-row 高度规则收窄到 input/select/button（浮层豁免）+ row 加 position:relative。Playwright 实测（进程内 DevServer+真实编辑器页）：行序 [Py, editor, fx] 无内联 select、弹层位于编辑器正下方、点选项插入 chip 且关闭、重开/外部关闭正常、无 JS 错误；单测+合同 98 项 + ruff/架构门禁通过
2026-09-10 | fx-var-popup-polish | done | fx 浮层二修（维护者截图反馈：①弹层被遮挡②位置歪③选变量后编辑器横向滚动条）：浮层改挂 document.body + position:fixed（CSS 顺序保证覆盖 .ref-completion 的 absolute），彻底免 #props overflow 裁剪；按编辑器 getBoundingClientRect 锚定（左缘对齐、正下方 2px、右/下缘出视口回收）；面板滚动(capture)/resize 即关闭（AbortController 管监听）；renderProps 前调 fxPopupCleanup 防挂 body 弹层残留；.fx-tag-editor 回归 pre-wrap 换行布局去 nowrap/overflow-x，长文本自适应增高无横向滚动条。Playwright 实测：position=fixed、left=编辑器 left、top=bottom+2、视口内完整、scrollWidth==clientWidth、长文本换行 132px、无 JS 错误；98 项测试 + ruff/架构门禁通过
2026-09-10 | devserver-no-cache | done | "下拉框还是被遮挡"复核：Playwright 小视口(1280x620)+面板滚动+elementFromPoint 命中测试证明浮层无遮挡无裁剪——真因浏览器缓存旧 JS/CSS（响应无 Cache-Control 头启发式缓存）。devserver _send 统一发 Cache-Control: no-cache（本地开发服务器缓存无收益，根治"前端改完不生效"类问题）；附带修浮层内部滚动被 scroll capture 监听误关（排除 varPopup.contains(target)）。98 项测试 + ruff/架构门禁通过
2026-09-10 | surface-var-opaque | done | 下拉改不透明（维护者反馈）：真因 styles.css 多处（.ref-completion 浮层/工具栏下拉/对话框/multi-bar）用 var(--surface) 但 :root 从未定义——background 解析失败回退透明，下层内容透出，视觉即"被遮挡"。:root 补 --surface: #ffffff 一处修复全部 6 处。Playwright 实测浮层 backgroundColor=rgb(255,255,255)、截图白色实底；98 项测试 + ruff 通过
2026-09-10 | browser-cmds-p0p1 | done | 影刀浏览器指令对标 P0+P1 补齐落地（维护者采纳 docs/yingdao-web-cmds-benchmark.md 补齐建议）：新增 10 条 browser.*（waitLoad 页面级加载等待 load/domcontentloaded/networkidle、scroll 顶部/底部/指定坐标/一屏+元素内滚动+平滑、check 复选框 check/uncheck/toggle 幂等、cookieSet/cookieGetAll/cookieGet/cookieRemove 登录态复用、attach 同 context 按标题/URL 子串或正则匹配产出独立会话、listPages 多标签列表、drag 拖拽），扩展 3 条（navigate action=goto/back/forward/reload 含 bsk history JS、getText infoType=text/html/outerHTML/value/href、waitFor state=visible/hidden/detached/attached），catalog 55→65；bsk 通道能力边界显式化（waitFor 仅 visible/getText 仅 text/其余新命令 COMMAND_NOT_FOUND）；i18n 补 10 命令名+commandFields；test_protocols 55→65 + browser contract 新增 6 个无浏览器会话缺失测试；真机 E2E（本地 HTTP 服务+checkbox/drag/history/多标签页）24 项断言全过；113 测试+ruff+架构门禁通过；对标文档覆盖度回填 ✅22/🟡8/❌14
2026-09-10 | navigate-align-yingdao | done | browser.navigate 逐参数对标影刀「打开网页」（维护者问"参数对齐了吗"触发）：网址/保存网页对象至(sessionId)/命令行参数(args)已对齐；「等待网页加载完成」原写死 domcontentloaded → 暴露 waitUntil=load/domcontentloaded/commit/networkidle（默认不变，bsk 不适用）；channel 补 firefox/webkit 枚举，ie/360se/QQ 壳内核标注不在范围走 bsk；manifest 2.1.0→2.2.0；真机 E2E waitUntil 四档 success、firefox 缺二进制返回明确执行错误；103 项测试 + ruff + 架构门禁通过（88a971f）
2026-09-10 | ext-exec-channel-p1 | done | 自研扩展执行通道 Phase 1 落地（维护者：扩展一等公民、参考 bsk 做权限控制、默认整个浏览器）：①宿主 `extension_exec.py`——ExtensionExecHub（命令队列+长轮询+结果回收+权限钩子）+ ExtensionExecClient（执行器侧 HTTP）；路由 /api/ext/command/{next,result,submit} + /status + /permissions，token 复用捕获 TOFU 配对；run 子进程经 RPA_EXT_HUB_URL 回连（RunManager 注入真实端口）。②执行器 extension 通道：navigate(goto/back/forward/reload)/listPages/attach/executeScript/getText/click/hover/input/scroll/check/select/waitFor/close(解绑)；会话=用户浏览器 tabId 句柄；缺省 transport 扩展在线优先、离线静默回退 playwright。③权限默认 {"mode":"browser"} 整浏览器，tabs/origins 收窄为预留接口（宿主 allows + 扩展 assertAllowed 双校验点同语义）。④扩展 manifest 0.2.0（+scripting/tabs/cookies/downloads/webNavigation + <all_urls>），background 增执行长轮询循环 + tabs/scripting/cookies 执行面（DOM 原语注入 MAIN world 规避页面 CSP），popup 增权限模式。⑤测试 tests/contract/test_extension_exec_channel.py 8 项（假扩展经真实 HTTP 扮演 background：协议/超时/权限/在线心跳/通道路由/边界/离线回退）；installer 测试版本断言 0.1.2→0.2.0。ruff + 架构门禁（47 py / 65 manifests）通过
2026-09-10 | ext-channel-browser-resolve | done | 扩展通道"指定插件 / 指定 Edge"落地（维护者问：插件重载了，打开网页指令怎么指定自研插件、怎么指定 edge 浏览器）：①**根因**——channel 原 x-depends 限 playwright，frontend 该字段直接隐藏；extension 通道静默忽略 channel，用户"指定了 Edge 却没生效"无从得知。②**宿主身份上报**：background 新增 detectHostBrowser/hostInfo，每次 `GET /api/ext/command/next` 带 host/ver/platform/ua（心跳即身份，不多一次往返），ping 也返回 host；popup 新增"执行通道"区块显示宿主浏览器 + devserver 识别状态。③**宿主端**：extension_exec 新增 `browser_name_from_user_agent`（UA→浏览器名，顺序敏感：Edge/Opera/Brave 都含 Chrome/）+ `channel_matches_host`（chromium=任意 Chromium 内核家族；宿主未上报=False 不假装成功）；Hub 记录并暴露 host（status 返回 host）；server.py `_host_report()` 解析长轮询 query。④**执行器语义**：extension 通道下 channel = **校验宿主**（匹配放行 / chromium 放宽 / 其余明确失败并给出处置建议 / 未上报提示重载）；缺省通道 + channel 与宿主冲突 → **自动让位 playwright**（只有它能按 channel 启动目标浏览器），显式 transport=extension 则报错不静默回退。⑤**修存量隐患**：ONLINE_WINDOW_SECONDS 15→30——原值小于扩展侧长轮询 hold(EXEC_HOLD_S=20)，两次轮询间 15~20s 被误判离线，导致缺省通道偶发回退 playwright。⑥**前端**：顶部「扩展通道」徽标（在线/离线 + 宿主浏览器，5s 轮询 /api/ext/status）；属性面板 transport 字段下方实时「通道解析预览」（与执行器同语义）；x-depends 由"隐藏"改"禁用+说明"（参数凭空消失会让用户以为指令没这个开关）；transport 空选项文案改「（缺省：自研插件优先）」。⑦navigate.json 2.3.0→2.4.0（channel 解除 playwright 限定 + 描述更新）；docs/extension-execution-plan.md 新增 §四「如何指定执行通道与浏览器」；extension/README.md 同步协议与用法。⑧测试：新增 tests/unit/test_extension_channel_resolve.py（UA 推断 7 例 + 匹配矩阵 13 例）；contract +5（宿主上报/旧扩展不上报仍在线/channel 校验矩阵/未上报明确失败/缺省让位 playwright）；假扩展支持 host 上报。ruff + 架构门禁（47 py/65 manifests）+ devserver 合同通过
2026-09-10 | ext-channel-pairing-diagnosis | done | 「test 流程执行后没有打开浏览器」根因定位 + 修复（维护者报障）。**根因不是代码，是配对断链**：用 netstat 定位到本机 8765 上跑着一个 devserver（PID 18668，代码版本早于 baabc99 → status 无 host 字段），其 `/api/ext/status` 返回 `lastPollSecondsAgo:null`（执行通道从未成功轮询）；netstat 显示每 ~3s 一条新连接（=扩展 exec loop 的 403 退避重试）。**受控实验**：备份并删除 `workflows/.capture-extension-token`（旧值 `rpa-1421ac…`，9/4 手写格式）→ 3 秒内 `online:true`（TOFU 采纳了扩展自动生成的 UUID）→ **确认扩展一直在正常轮询，只是 token 不匹配被一路 403**。随后在用户 devserver 上真实执行 `workflows/test`：`status:succeeded, url:https://www.baidu.com/, transport:extension, tabId:933967628, durationMs:5500` —— 浏览器里真实打开了标签页。**产品化修复（4 项）**：①**失败原因可见**：前端 run 面板新增 `#run-error` 区块，失败时显示 `失败：<code> · 节点 <nodeId>` + message + 通道（此前只写「完成：failed」，用户完全无从排查）；②**离线快速失败**：executor 新增 `_extension_preflight`（离线 <1s 失败，而非躺满 timeoutMs 30s 才 TIMEOUT），`_open_extension`/`_execute_extension` 均前置；③**配对失败可观测**：hub 记录 `authFailures`/`lastAuthFailure`（成功轮询清零），app `_require_extension_token` 403 时留痕，`/api/ext/status` 暴露，徽标显示「扩展通道：配对失败」；④**一键重置配对**：`POST /api/capture/extension/token {"token":""}` 删除本机 token（扩展 3s 内自动重新配对），插件面板新增第 5 步「配对状态 + 重置配对」按钮。另：`_ext_channel_failure` 把 TIMEOUT/CHANNEL_OFFLINE 分别翻译成可照做的处置；`.capture-extension-token` 加入 .gitignore 并 `git rm --cached`（曾在 dde4635 误入库，含本机凭据）。测试：contract +4（离线快速失败/配对失败指向/鉴权留痕与清零/一键重置）；docs §五「排障：插件装了也重载了，为什么还是没打开浏览器」；extension/README 补「换过 token 怎么办」
2026-09-10 | props-panel-param-groups | done | 属性面板参数分区对标影刀（维护者反馈：①"少了个指定浏览器的参数"②"参数分类需要对标，参数混在一起，联动置灰后占地方、查看不方便"）。**①指定浏览器参数**：参数其实都在（transport=执行通道 + channel=浏览器类型），但埋在 12 个平铺字段中间且选项是裸英文值（msedge/extension），用户根本认不出来——补 `x-enum-labels`（manifest 下发中文选项标签，selectField 优先级 x-enum-labels > i18n.ops > 原值；channel: msedge→"Microsoft Edge" 等；transport: extension→"自研插件（真实已登录浏览器）" 等），channel/transport 空选项分别改"（缺省：跟随扩展宿主）/（缺省：自研插件优先）"，i18n transport 标签改"执行通道"。**②参数分区（对标影刀 img0/img2 截图：常规 Tab=浏览器类型+网址，高级 Tab=等待+超时+命令行参数）**：navigate.json 2.5.0 新增 `input_schema.x-param-groups`（放 input_schema 内随 catalog 下发，无需后端/模型改动；JSON Schema 忽略未知关键字）：常规[action,url] 浏览器[transport,channel] 高级[timeoutMs,collapsed] 启动选项·仅playwright[waitUntil,args,headless,userAgent,userDataDir,collapsed] 三方件·仅bsk[browserInstanceId,keepOpen,collapsed]。**前端**：`paramGroupPlan` 纯函数规划（无 DOM，node 侧与前端共用同一份源码）+ `paramGroupSection` 渲染；x-depends 不满足的字段不进声明分组、归入底部「其他通道参数（N）」折叠区（不再平铺置灰占地方），切换通道触发重渲染自动归位；collapsed 组缺省收起但组内任一字段已填值自动展开（用户填过的不藏）；展开状态存 `state.paramGroupsOpen`（key=nodeId|label，重渲染保持）。新脚本 scripts/check_param_groups.mjs（7 例判定矩阵：缺省/playwright/bsk 通道归位与展开）。ruff + 架构（47 py/65 manifests）+ 193 项测试通过；用户运行中的 devserver 仍为 catalog 2.4.0 → 需重启生效
2026-09-10 | run-startup-failure-visible | done | 「test 流程为什么无法执行」定位 + 修复（维护者报障）。**根因**：用户在「打开网页」节点上把「重试次数」设成 3，而 `browser.navigate` 的 `effect.replay = unsafe` —— compiler 与 orchestrator 都拒绝「不可重放指令配重试」（重复执行=重复开标签页），报 `Unsafe replay command cannot be retried: browser.navigate`。**为什么用户看不到原因（两个缺陷）**：①`RunManager` 把子进程 stderr 接到 PIPE 却**从未读取**（顺带是管道写满阻塞隐患）→ 编译错误文本直接丢；②CLI 编译失败是 `WorkflowCompileError` 直接透出 traceback，devserver 侧无结构化信息 → `GET /api/runs/{id}` 只有 `{result:null, exitCode:1}`，界面显示「完成：exit 1」。复现：`POST /api/runs {"workflow":"test"}` → `exitCode:1, result:null, events:[]`（子进程未产出任何事件）。**修复（4 处）**：①runs.py 新增 `_read_stderr` 线程 + `_startup_error()`（优先解析 stderr 里的结构化 JSON，取不到退回 stderr 尾部 12 行）；status 在「无结果 且 非零退出」时返回 `startupError:{message,tail}`；②cli.py：`_compile` 包住 FileNotFoundError / WorkflowCompileError / pydantic ValidationError → `_cli_fail()` 输出 `{"error":"COMPILE_FAILED","message":...}` 到 stderr + 退出码 2（新增 `_validation_summary` 压前 3 条位置+原因）；③前端 runWorkflow **运行前先编译**（POST /api/compile，能力集与 CLI 完全一致，已核对 DEFAULT_CAPABILITIES）→ 不通过即 `showCompileErrorsAsRunError()` 摊开错误且不启动子进程；pollRunStatus 完成分支把 status 传给 renderRunError，显示「运行未能启动（退出码 N）」+ 原因 + 常见原因提示；④属性面板新增 `retryCountField()`：`effect.replay === "unsafe"` 的指令**禁用「重试次数」**并说明原因，历史非法值标红（`.field-invalid`）+ 给出「清除重试次数」一键修复按钮（pushUndo + 删除 + 重渲染）。**验证**：CLI `run` 现在 exit=2 且 stderr 为一行结构化 JSON（无 traceback）；临时 devserver 跑 test → `startupError.message = "Unsafe replay command cannot be retried: browser.navigate"`；用去掉 retry_count 的副本在用户运行中的 devserver 上真实跑通（`runFinished succeeded`，Bing 在 Edge 打开）。**测试**：contract +2（run_control：启动失败带出编译原因并保留 stderr 尾部；cli_parity：编译失败结构化错误 + 无 traceback）；ruff + 架构（47 py/65 manifests）+ 160 项测试通过；docs/extension-execution-plan.md §五-4 补「运行后什么都没发生」排障。**遗留讨论**：是否把 `back/forward/reload` 这类跳转视为幂等（放宽 replay 策略）需单独评估，不绕过编译期校验
2026-09-10 | expr-modes-fx-tags | done | fx 模式标签语法（expr_modes 后端消费落地）：resolver 新增 resolve_tags —— [name] / [name.field] 标签替换为变量值，支持与文本混排拼接（「输出日志：这是变量[webpage1]」），整串 ${...} 仍按既有语义保留原类型，未定义标签运行报 INVALID_REFERENCE 不静默；orchestrator 按 node.expr_modes 分字段解析（fx→标签 / 其余→既有 ${}），string 字段求值结果收敛为文本；标记分析纠正：Vue 版 PropsPanel 源码实读字段级 x-fx（此前基于过期构建产物误判命令级回归），字段级 x-fx 现仅 3 处（browser.input selector/text、browser.navigate url）；Vue 版 fx 交互重做：textarea 编辑 + [标签] chip 实时预览 + 「＋变量」下拉在光标处插入标签；expr_modes 从组件内 ref 丢失改为持久化到 node._exprModes；单测 resolver 9 + worker/runtime 端到端 15
2026-09-10 | expr-modes-python-eval | done | py 模式表达式求值 + 变量写回：worker 新增 python.evalExpression 分支（子进程 exec/eval，与 data.* 同一隔离模型，规则 5/6 不破）——注入流程变量为 locals、支持多行与赋值、回传 values + assignedVariables（过滤 __builtins__ 等内部键、非 JSON 类型收敛）；orchestrator _evaluate_python_fields 调 python.worker 求值并把 assignedVariables 合并进 scopes.variables（orchestrator 拥有变量写权，handler 不 mutate，规则 3 不破）——**py 节点赋值的变量跨节点可被 fx 标签/py 引用**（端到端实证：n3 py 求值 total*10=60 落盘，n4 fx 引用 [total] 得 6）；同节点多 py 字段共享命名空间；架构检查 check_forbidden_calls 豁免 workers/ 目录（规则 5 语义：动态求值限定在子进程执行面）+ 注释说明；string schema 字段自动 str 化（py 返回 int 60 → "60"）
2026-09-10 | session-default | done | sessionId 默认会话：45 条命令 manifest 移除 input required 中的 sessionId（output required 的 9 处输出契约保留；精确替换 input_schema 块内 required，格式零扰动）；executors/base 新增 resolve_session_id（显式指定 > 最近激活 > 唯一会话，多会话无记录返回空串由调用方报 SESSION_NOT_FOUND 不猜测）；browser（playwright+bsk 双轨各自记录 _last_*）/ desktop / desktop_win32 三 executor 接入——navigate/attachWindow 创建会话即设为默认，_session/_bsk_session/内部直读点统一走解析；旧版 app.js sessionId 下拉空值=清空留默认并提示，Vue 版沿用字段级 x-fx；单测 resolve_session_id 6 + browser 合同 2；编辑器 e2e 全绿
2026-09-11 | ext-channel-drop-pairing | done | 移除扩展捕获/执行通道的 token 配对机制（维护者：配对多余）：删 app.py token 存储/TOFU/_require_extension_token、server.py /api/capture/extension/token 路由与全程 headers 入参、background.js getToken 与 X-Capture-Token 头、popup 配对/重置、前端 authFailures 徽标与 loadExtensionPairing 第5步、extension_exec record_auth_failure 留痕；hybrid 改为默认启用（不再依赖已配对）；docs/extension/README 同步。另修门禁暴露的 3 处：contract hover 测试补 hybrid 显式关闭透传、E2E dom 原语与 search-and-save 显式 transport=playwright 避免被后台 devserver 在线扩展路由到 extension 通道、E501 长行。343 tests + ruff 通过
2026-09-11 | drop-browserskill-channel | done | 移除 BrowseSkill(bsk) 执行/捕获链路（维护者：执行通道移除 browserskill，范围含元素捕获链路）：删 bsk_client.py、executors/browser_bsk.py、capture/browser_bsk.py 与 bsk 专项测试（test_browser_bsk_executor / test_capture_bsk_contract）与 workflows/bsk-keepopen；browser.py 清空 _bsk_sessions/_open_bsk/_execute_bsk/bsk transport 分支；capture/__init__.py、cli.py（展示工厂与 --transport 枚举）、devserver/app.py（_BROWSER_TRANSPORTS）移除 bsk；navigate.json 2.5.0→2.6.0 去掉 bsk transport/参数组/依赖与 keepOpen；后端静态 i18n / PropsPanel.vue / check_param_groups.mjs 去 bsk；docs 与 README 同步（capture-browserskill.md 删除）；测试清理 test_cli_parity 的 bsk 用例并去残留注释引用。执行通道收敛为 extension（一等）+ playwright 双通道。316 tests + full gate 通过
2026-09-11 | navigate-channel-playwright-only | done | navigate 浏览器类型收敛（维护者反馈：插件装 Edge、选「内置 chromium」仍开 Edge，浏览器类型不应出现在自研插件通道下；另问 firefox/webkit 是否真支持）：**channel 改为仅 playwright 通道生效**——navigate.json 2.6.0→2.7.0 在 x-depends `{"channel":{"transport":"playwright"}}`（extension 通道下字段归入「其他通道参数」折叠区，不再误导）；channel 枚举删 firefox/webkit（实现走 chromium.launch(channel=...) 仅支持 chromium 家族，firefox/webkit 属独立 browser type 未实现，事实不可用故不下发），仅剩 chromium/chrome/msedge，description 与 benchmark 文档同步；check_param_groups.mjs 镜像 DEPS 并把缺省 inactive 调整为 6、补 playwright 下 channel 归位断言。full gate 通过（首次因 TRAE 沙箱拦截 playwright 浏览器缓存与 capture-profile 写入误报 1 失败，去沙箱重跑全绿）
2026-09-11 | navigate-omit-scheme | done | navigate 地址可省略协议（维护者：不要强制写 https://，对用户友好）：修复 workflows/test/workflow.json 里 `htttps://`（多打一个 t，导致 ERR_ABORTED）为 `https://`；executor 新增 `_ensure_scheme`（browser.py，已带 scheme 的 http(s)/file/data/about 等原样保留，缺失则补 https）并在 playwright goto 与 extension `_open_extension` 两处应用；navigate.json url 字段补说明；单测 +3 覆盖。ruff + browser/extension/cli 合同测试通过
2026-09-11 | navigate-browser-type-dropdown | done | 浏览器类型面板收敛为「单一下拉 + 通道透明」（维护者：对标影刀「打开网页」浏览器类型参数，我们的 transport×channel 两层嵌套反直觉；选内置 Chromium 却开宿主 Edge）：navigate.json 2.7.0→2.8.0——分组「浏览器」→「浏览器类型」仅留 channel、新增折叠组「通道（仅高级）」承载 transport（留空=自动）、channel 移除 x-depends 改为始终可见并改写 description、transport 注明「仅高级覆盖」；执行器 `_use_extension` 增补 `channel==chromium → return False`（内置 Chromium 仅 playwright 可真正启动，杜绝歧义）；前端 `resolveChannelPreview` 同步 chromium→playwright；check_param_groups.mjs（DEPS 去 channel、缺省 inactive=5、浏览器类型/通道两组断言）与 check_channel_preview.mjs（chromium 用例改 playwright）同步；通道合同测试 `_use_extension({"channel":"chromium"})` True→False 并更新 docstring；benchmark 文档同步 navigate 2.8.0。范围澄清：browser.launch 无独立 manifest（并入 navigate）、browser.attach 无通道参数，故仅落 navigate。full gate 通过
2026-09-11 | palette-order-align-yingdao | done | 命令列表与 navigate 参数顺序对标影刀（维护者：指令顺序按影刀来，两者都要）：①左侧命令面板——新增 manifest 顶层 `x_palette_order` 整数字段（model/command.py CommandManifest，alias `x-palette-order`，extra=forbid 下与既有 x_* 同模式），30 条 browser.* manifest 按 docs/yingdao-web-cmds-benchmark.md §二影刀编号写入（）；devserver/app.py catalog_overview 排序由 `sorted` 字母序改为按 `(x_palette_order or inf, id)` 升序（其余命名空间保持字母序）；前端 renderPalette/catalog.html 数据驱动消费 /api/catalog 顺序无需改。②navigate 参数分组——x-param-groups 重排「浏览器类型」置顶→「常规」url/action→高级→通道（仅高级）→启动选项，版本 2.8.0→2.9.0；check_param_groups.mjs 无顺序断言不需改。③回归保护——test_devserver 新增 test_browser_palette_ordered_by_yingdao（30 条断言影刀顺序）+ 修正原 test_catalog_endpoint_matches_load_catalog 字母序断言为 palette 顺序断言。注意：manifest 键必须用连字符 `x-palette-order`（字段 alias），下划线形式会被 extra=forbid 拒掉。full gate 通过
2026-09-11 | remove-playwright-single-extension | done | 彻底移除 playwright、浏览器执行/捕获收敛到自研扩展单通道（维护者：只保留自研插件）：pyproject 删 playwright 依赖 + uv.lock 重锁；执行器 `PlaywrightExecutor` 纯扩展化（navigate.json 3.0.0 删 transport/channel/args/headless/userAgent/userDataDir/waitUntil，分组收敛「常规」+「高级」）；`browser.py` 删 playwright 分支/`_session`/`_sessions`，扩展离线前置预检 <1s 显式失败（channel_offline，不回退不白等）；17 条命令（queryAll/getPosition/getScrollPosition/getSelectOptions/setValue/setAttribute/drag/cookieSet/cookieGetAll/cookieGet/cookieRemove/screenshot/stopLoading/waitLoad；upload/download/handleDialog 落「暂未实现」显式错误但 manifest 字段保留）经 extension background.js/browser_ext.py/_ext_phase_d 路由补齐；`capture/__init__.py` 删 BrowserCaptureSession、devserver `_BROWSER_TRANSPORTS={"extension"}`、cli `--transport` 收紧为 extension；前端 app.js/PropsPanel/popup 去 transport/channel 文案；删除依赖 playwright 作为浏览器驱动的 e2e（test_editor/test_editor_element_edit/test_search_and_save/test_browser_dom_primitives）与一次性抓取脚本（scrape_yingdao_*）；tests 316→272 且全绿 + ruff + 架构 + 任务 + node 门禁通过；ADR 0013 + README/extension-execution-plan/api-usage/ADR 0007 改写
2026-09-11 | navigate-browser-type-param | done | 打开网页指令对标影刀补「浏览器类型」参数（维护者：只针对 edge/chrome 各有一款自研插件，无需 auto，必选其一可默认）：navigate.json 3.0.0→3.1.0——新增必选 `browserType`（enum msedge/chrome，x-enum-labels Edge/Chrome，default msedge，input_schema.required 增 browserType），x-param-groups 顶置「浏览器」分组（browserType）置于「常规」前；多扩展宿主按标识路由——extension_exec.py 单值 `_host` 升级为 `_hosts` map（record_host 按上报 browser upsert + 心跳剔除），submit 快失败（targetHost 非空且不在线 hosts → TARGET_HOST_OFFLINE 不入队），next_command 改为扫描匹配（targetHost 为 None/auto/等于上报浏览器才领），status() 增 `hosts` 列表；app.py extension_command_submit 透传 targetHost、browser_ext.tabs_create 增 target_host、browser._open_extension 读 browserType→target_host；前端 app.js selectField 对 browserType 未在线浏览器选项置灰禁用 + refreshExtChannel 存 state.extHosts；check_param_groups.mjs 同步浏览器分组；契约测试新增 5 条（hosts 列表/targetHost 路由/离线快失败/browserType→chrome 路由/缺省→edge）。benchmark 文档 navigate 行同步。full gate 通过
2026-09-11 | navigate-session-host-routing | done | 多浏览器并存时 session 路由到宿主浏览器（维护者：开了多个装了扩展的浏览器，后续元素操作不能串台）：session 绑定宿主——browser.py 增 `_ext_session_hosts`（sessionId→浏览器，navigate 创建时按 browserType 种下、attach 置空、close 解绑），`_execute_extension` 解析 host 后透传；browser_ext.py 全部 tab/浏览器级 op（tabs_navigate/tabs_history/tabs_close/page_call/page_eval/cookies_*/screenshot/stop_loading/wait_load）加 `target_host` 参数并转发 submit；executor 各调用点（navigate goto/back、executeScript、getText、phase_d DOM/cookie/stopLoading/waitLoad/screenshot、_ext_wait_for count、onTimeout stop）全部带上 host——多浏览器在线时命令只路由到创建会话的那个，同 profile 多窗口天然正确（tabId 全局唯一），杜绝跨浏览器抢命令/操作错页签。契约测试新增 1 条（_RecordingExt：chrome 建会话→click 仍 target_host=chrome）。full gate 286 passed + 1 条桌面 UIA e2e 环境抖动重跑绿
2026-09-11 | navigate-browser-instance-id | done | 打开网页输出浏览器实例唯一 id + tabId（维护者：输出含浏览器实例 sessionId 和 tabId，风险最小，一个流程分别操作 edge/chrome 也能分清；最终选定「真正实例唯一 id」——同一 profile 多窗口=同一实例，不同 profile 不同 id）：扩展 background.js 新增 `ensureInstanceId`（chrome.storage.local 持久化 `rpaInstanceId`，crypto.randomUUID 一次生成，同 profile 共用）+ hostInfo/hostQuery 带上 instanceId & 启动时先初始化；server.py `_host_report` 读 `iid`；hub 改按 instanceId 归档（extension_exec hub `record_host` key=instanceId、旧版退化为浏览器名），`next_command` 匹配 target 当且仅当 =浏览器名 or =instanceId，`tabs.create` 结果由 hub 补带真实 instanceId，`submit` 目标从未在线→`TARGET_HOST_OFFLINE` 快失败（使 browser.py 死分支生效）；executor `_ext_session_hosts` 改存实例 id、navigate 输出新增 `browserInstance`(实例唯一 id)+`browserType`+`tabId`；navigate.json 3.4.0→3.5.0 output_schema/x-outputs 增三字段（browserInstance primary「浏览器实例」）。口径确认：sessionId（网页对象会话）与 browserInstance（实例身份）保持分离。契约测试新增 2 条（同浏览器双 edge-a/edge-b 按实例路由、navigate 输出三字段 + click 按实例路由）。docs/extension-execution-plan.md §四.6 补实例级身份链路。契约测试 20 条全绿 + ruff + 架构通过;full gate 288 passed,唯一失败 test_uia_desktop_vertical_slice 为环境偶发(前台焦点被抢,UIA SendInput 失败),独立重跑绿且与本次改动无关
2026-09-14 | workflow-data-table | done | 每流程一张数据表格（横空对齐影刀「数据表格」）：存储=data.table 命令集落地；行数据结构设计=跨运行累积保留 + data.table.clear 显式清空。实现：model/command.py 增 x_runtime 注入声明；commands/data/table/ 6 个 data.table.* manifest（getCell/setCell/appendRow/deleteRow/clear/exportCsv）；workers/data_table.py 原子读改写+CSV(BOM)+列 key 寻址；workers/python_worker.py 分发 6 handler（appendRow 动态补列、deleteRow 越界 INVALID_INPUT、exportCsv 越界拒绝）；cli.py+orchestrator.py flow_dir 注入（含 resume）；devserver store.py TableStore + /api/workflows/<flow>/table 四端点（GET/PUT/clear/export.csv 下载带 Content-Disposition + _send 支持自定义头）；编辑器「数据表格」按钮+内联可编辑网格（列头 label/type、增删列行、清空、导出 CSV、保存 PUT）；i18n 6 命令名+字段标签 + SEMANTIC_GROUPS「数据处理」归入 data.table.*。测试+6（worker 累积/越界/清空/导出BOM）+5（devserver 表端点往返）+i18n 合同门禁；full gate passed
2026-09-14 | yingdao-batch1 | done | 影刀对齐第二批（数据表格之后）：.harness/yingdao-gap-matrix.md 差距矩阵，第一推进方向选定「文件/CSV/文本 + 流程延时」，落地 6 条纯 worker 指令（零外部依赖、单测全覆盖）：data.readText（读文件+READ effect+未找到 INVALID_INPUT+越权 CAPABILITY_DENIED）、data.appendText（追加累积+UNSAFE_WRITE effect）、data.fileExists（boolean+READ）、data.deletePath（文件/空目录/recursive 递归目录、非空目录无 recursive→INVALID_INPUT、noop 成功 deleted=False）、data.datetimeNow（value/iso/timestamp+format 模板+pure）、workflow.sleep（子进程 time.sleep+positive 校验+pure，default_timeout 60s）。manifest 6（commands/data/{readText,appendText,fileExists,deletePath,datetimeNow}.json + commands/workflow/sleep.json，catalog 77→83）；worker 新增 _read_effect/_unsafe_write_effect helper 与 6 分支；i18n 6 命令名+字段标签（format/seconds/text/recursive）+ SEMANTIC_GROUPS 数据处理加入新 data 指令并新增「流程控制」组（icon branch）；契约测试 test_data_batch1.py +15（含 status='error' 断言适配）；test_protocols catalog 断言 71→83。端到端 CLI 验证 sleep+2×appendText+datetimeNow = succeeded、log.txt=12。ruff+架构+gate 通过

2026-09-14 | palette-default-collapsed | done | 左侧指令树默认收起（维护者：指令多不方便找，希望默认折叠）：语义类别分组（SEMANTIC_GROUPS label+「控制流」）首次渲染预置进 state.collapsedGroups，id 前缀兜底分组（如 browser）同样默认收起——renderPalette 首次调用初始化各语义组 label 与「控制流」，兜底段幂等 add prefix；单击分组仍按既有逻辑展开/收起，操作状态记忆（重新渲染保持）；搜索时 forceExpand 强制全部展开不受影响。纯前端 app.js（新增 state.paletteCollapseApplied 标志）；node --check 通过；Puppeteer 实测：首次加载 9 分组全收起、单击数据处理展开 16 项、重渲染记忆保持、搜索「打开」仅显匹配组且全展开、清空恢复默认

2026-09-14 | setvar-var-type | done | data.setVar 补齐「变量类型」参数（对标影刀「设置变量」类型格式化）：manifest 1.0.0 增 varType（enum auto/string/number/boolean/object/array，default auto，x-enum-labels 中文，向后兼容旧节点缺省=auto）；worker handler 新增 _coerce_by_type——string 转文本（None→""）、number 转数字（str 尝试 int→float、bool→int、空→0、非法 INVALID_INPUT）、boolean 转布尔（true/1/yes→True、else False、None→False、数字→bool）、object 解析 JSON 对象（dict 保留、空串→{}、非法 JSON INVALID_INPUT）、array 解析 JSON 数组（list 保留、非法→error）、auto/未知原样透传；值格式化后回传 outputs.value，orchestrator 按 x-var-write 写入变量即落格式化值；前端 schemaField 加 varType 分支——旧节点缺省按 auto 选中（复用 selectField + x-enum-labels）；i18n commandFields data.setVar 增 varType「变量类型」。契约测试 test_data_batch1 增 6 用例（auto/缺省、string、number、boolean、object、array 各含格式化与失败分支），20 例全绿。Puppeteer 实测：属性面板渲染「变量类型 varType」下拉、默认选中自动、中文选项、切换 number 更新节点 with、旧节点缺省 auto。全部非 e2e + ruff + 架构 + i18n 门禁通过

2026-09-14 | setvar-var-type | refine | data.setVar 二次调整（对标影刀）：① 移除变量名下拉——删 schemaField varName 专属分支，varName 恢复纯文本输入框（写新名=定义、写已有名=覆盖，语义端到端不受影响）；② 变量类型去掉 auto 与「（未设置）」占位——manifest enum 改为 string/number/boolean/object/array、default=string、x-enum-labels 去 auto；worker 缺省 `or "string"`（默认按字符串格式化，auto 移除）；_coerce_by_type 保留末尾未知类型按原样兜底；selectField 新增第 7 参 noEmpty（true 时不渲染占位），varType 分支传入并缺省选中「字符串」，i18n varType 标签保留。契约测试 test_setvar_string_default_and_explicit 改断言缺省=字符串格式化，实测缺省 value=42→"42"、已存文本原样。全部非 e2e + ruff + 架构通过；Puppeteer 实测：varName 为 text input 无下拉、varType 默认选中「字符串」、选项无 auto 无「未设置」（字符串/数字/布尔/对象/数组）

2026-09-14 | browser-contract-drift | done | 修复 browser.* 三个平台无关缺陷（macOS 适配审计发现，Windows 同样命中）：① 缺省会话回归——恢复 `resolve_session_id`（显式 > 最近激活 > 唯一会话），`_last_session_id` 由 navigate/attach 建会话时种下、close 解绑时清空；attach/listPages 移入 `_NO_SESSION_COMMANDS`（不依赖既有会话，零会话也能列举/附着，与 desktop 的 attachWindow/getWindowList 同口径），navigate goto 无 sessionId 仍新开网页对象（保持「打开网页」语义不被缺省会话劫持）；② 输出契约漂移——执行器在 outputs 里多带 manifest 未声明的 `sessionId`（output_schema 为 additionalProperties:false → 运行期 INVALID_OUTPUT；实测命中 getText/getPosition，静态另有 queryAll/setValue/setAttribute/close）→ 改执行器对齐 manifest（drag 的 sessionId 是 manifest 要求的，保留）；③ 效应类型不符——browser.screenshot 声明 idempotent-write 而执行器抛 session（截图已落盘但整轮判失败）→ 改 IDEMPOTENT_WRITE + resource 加 `:screenshot`；④ browser.hover effect 由 read 改为 unsafe-write（对齐 manifest：hover 会触发页面 mouseover 处理器）。新增契约门禁 `test_browser_commands_aligned_with_manifest_contract`（真实 devserver + 假扩展逐条跑全部 browser 命令，按各自 output_schema 校验 outputs、按 effect.kind 严格校验 effect；用例表与 catalog 命令集强绑定，新增命令必须补用例，否则门禁失败）+ 缺省会话与「零会话 attach/listPages」两条行为测试。校验：ruff + 架构 + 任务 + node 门禁全过；涉及文件 120 用例全绿；实机 macOS 端到端 navigate→getText(无 sessionId)→getText(显式 sessionId)→screenshot = succeeded

2026-09-14 | macos-platform-support | done | 浏览器检测/自启在 macOS 上从「静默失效」修到可用（此前非 win32 一律按 Linux 处理，二进制候选表只有 Windows）：① `browser_user_data_dirs()` 增 darwin 分支（`~/Library/Application Support/Google/Chrome`、`…/Microsoft Edge`——Edge 不是 `Microsoft/Edge` 子目录）→ profile/unpacked 探测不再恒空，实机 `/api/env/status` 从 `binary/running/installed=false` 却 `online=true`（自相矛盾）变为 edge 四项全 true + `installMode=load-unpacked`；② extension_launch 路径表按平台拆分（`_WIN/_MAC/_LINUX_INSTALL_PATHS`）+ `_expand_env` 同时 expanduser；`find_browser_exe` 不再只在 nt 下查候选表 → macOS 命中 `/Applications/Microsoft Edge.app/Contents/MacOS/Microsoft Edge`，detect_browsers/open_browser/pack_extension 复用同表一并修复；③ `browser_running()` 增 POSIX 分支（`pgrep -f` 命令行匹配，macOS 主进程路径不误判 Helper 子进程；pgrep 缺失退 ps）；④ 顺带修两处本机既有失败——test_desktop_contract 两条 `Win32DesktopExecutor` 用例在非 win32 缺守卫（该名为 None → TypeError）、test_python_worker 逃逸用例硬编码 Windows 反斜杠（POSIX 上 `..\x` 只是普通文件名）→ 平台化。测试 +14（installer 5 / launch 4 / 契约 3 / 平台守卫 2）。docs/extension-install.md §1.2 平台范围改写：执行通道与 Load unpacked 跨平台可用，静默安装（注册表/策略）仍限 Windows 且显式报 PLATFORM_UNSUPPORTED

2026-09-14 | gate-crossplatform-audit | done | 全量门禁跨平台排查（用户怀疑「Windows 专用指令导致卡住」）：实测证伪——macOS 上完整 pytest = 337 passed / 13 skipped / 0 failed / 0 errors（PYTEST_EXIT=0），13 个 skip 全是 Windows-only 用例的正常守卫（test_desktop_contract 4、e2e uia 3、e2e windows_desktop 2、e2e capture 1 等）；ruff / check_architecture / check_tasks / 两个 node 脚本全部通过。所有 win32 专有 import（pywinauto/win32gui/win32clipboard/pythoncom）都在函数内且被 `sys.platform == "win32"` 守卫，pywinauto 由 pyproject 的 `sys_platform == 'win32'` 标记排除，macOS 下 `Win32DesktopExecutor` 是占位 None 而不是导入失败。真凶另有其人：① 本机 `check_all.py` 的 pytest 阶段曾报 111 个 setup ERROR，根因是**执行沙箱**对默认 TMPDIR（`/var/folders/.../pytest-of-*`）的 mkdir 拦截（`PermissionError: EEXIST`），纯环境问题，用 `--basetemp=/tmp/...` 即消除；② 性能画像：`load_catalog` 在沙箱里 6s/次、profile 显示 82% 花在沙箱的文件访问 broker 上，真实机器约 0.5s——门禁 10 分钟量级主要来自 347+ 用例里每次 fixture 都重新 load_catalog（test_protocols.py 单文件 16+ 次）。修复两处**真实的跨平台缺陷**：(a) 新增 `tests/conftest.py` 把 `RPA_EXT_HUB_URL` 钉到 `127.0.0.1:9`（必然拒绝连接）——此前 `test_browser_contract` 里裸 `PlaywrightExecutor()` 走缺省 hub，开发者本机常驻 devserver + 扩展在线时会**真的枚举/操作用户真实浏览器标签页**（实测 `browser.listPages` 返回了 12 个真实标签），门禁结论随本机环境漂移；需要真实通道的用例一律显式 `ExtensionExecClient(base_url=...)`，`RunManager` 也会为 run 子进程覆写该变量，均不受影响。(b) 清掉测试与示例里硬编码的个人 Windows 绝对路径：`test_desktop_contract::test_desktop_e2e_workflow_fixture_is_present` 原来断言 `"D:/Users/Administrator/Documents/代码/rpa_core/examples/windows-desktop/workflow.json".endswith("workflow.json")`（恒真、零覆盖、泄露机器路径）→ 改为真实校验两个桌面 e2e fixture 存在且能以 `desktop.control` 编译；`examples/windows-desktop` 的 `filePath` 与 `examples/baidu-news-top10` 的 `workspace` 默认值由 `D:/Users/Administrator/...` 改为可移植值（`test.txt` / `run_artifacts`），并让 `tests/e2e/test_windows_desktop.py` 注入仓库内 `test.txt` 的绝对路径——原先前者的 Windows e2e 切片只在**那台特定开发机**上能过。校验：ruff + 架构 + 任务 + node 全过；完整 pytest 337 passed / 13 skipped；两个示例 `cli validate` 均 valid

2026-09-14 | catalog-cache-and-tmp-root | done | 门禁耗时治理（维护者：优化 catalog 载入，测试经常耗时很久）。**画像先行**：cProfile + `--durations` 证明耗时几乎全在 `load_catalog`——83 条 manifest 每次重新读盘 + 解析 + 166 次 JSON Schema 自检，本机 0.5s/次；沙箱里宿主文件 broker 让每次读 59–94ms，单次达 5–6.6s，而 9 个函数级 `server` fixture（DevServer.__init__ 各加载一次）+ 20 多个测试模块的直调，加起来把门禁推到 **12m39s**（最慢 25 项清一色是它：test_cli_parity 10.32s、test_python_expr 10.07s、test_protocols 9.57/9.56s，以及 capture_contract / extension_installer / devserver 的一整片 5–6.7s setup）。**① 快照指纹缓存**：`loader.py` 拆出 `_manifest_signature`（只 rglob + stat：路径 + mtime_ns + 字节数，实测 0.93ms）与 `@lru_cache(maxsize=64)` 的 `_load_catalog_snapshot(root, signature)`，`load_catalog` 先算指纹再取快照；指纹进缓存键但不参与解析，因此 manifest 的新增/改写/删除都会失效（新增用例三向验证），`lru_cache` 不缓存异常（空目录报错后补齐可正常加载），并导出 `clear_catalog_cache()` 供「独立重读是否仍一致」的断言使用。语义零变更：生产侧 devserver 本就是启动时加载一次、CLI 每次进程加载一次，缓存只是让同进程内重复调用不再重读（规则 7 的「一次运行一份不可变快照」反而被强化），全仓无 manifest 热重载路径。**② 受限环境临时根**：111 个 setup ERROR 的真根因不是 TMPDIR 不可写，而是沙箱拒绝 `os.mkdir(已存在路径, mode=0o700)`，pytest 恰是对已存在的 `pytest-of-<user>` 这么建目录，而抛出的 `PermissionError` 不是 `FileExistsError`、`exist_ok=True` 兜不住（**有状态**故障：首次创建能成、第二次运行才炸，pytest 还会把它误诊为用户名非法而退到 `pytest-of-unknown`，于是报错文本极具误导性）。`tests/conftest.py` 新增 `pytest_configure`：先复刻 pytest 的判定（只对 `pytest-of-<user>` 与 `pytest-of-unknown` 两个名字操作，探测时刻意不抢先创建它们——否则会把「新建」变成「已存在」而自造故障），仅在默认根确实不可用时改用一个**本次运行专用的空 basetemp**（pytest 拿到 `--basetemp` 会先 rm_rf 再 mkdir，目标为空则这次清理不构成任何批量删除——受限环境最不该在启动阶段做批量删除，失败即整场崩），`pytest_unconfigure` best-effort 回收；正常机器探测通过、保持 pytest 原生目录布局，显式 `--basetemp` 优先级更高。**验证**：完整 pytest 337→（+4 条缓存契约用例）**350 项全绿、0 failed、0 error**；不带任何参数直接 `uv run pytest` 即可跑通（此前必现 111 ERROR）；ruff + 架构 + 任务 + node 全过；**12m39s → 2m45s**（沙箱内 CPU 仅 6.6s，其余全是宿主 broker 往返）。残余耗时均为进程边界固有成本：`test_run_control` 两个用例各起一个真实 `rpa-core run` 子进程（各自独立加载快照，5–8s），首个 DevServer fixture 承担一次完整加载（4.8s）

2026-09-15 | gate-perf-verify-and-commit | done | 门禁治理的复测与落库。**复测**：`uv run pytest -q`（裸跑，无 --basetemp）= **2m57s / 340 passed / 10 skipped / 0 failed**，与 12m39s 的治理前基线相比约 4.3x；`user 6.82s + sys 2.51s` → **CPU 仅占 5%**，印证「耗时几乎全是宿主文件 broker 的按次读取往返」这一画像，而非计算量。**残余耗时（均为进程边界固有，不再削减）**：`test_run_control` ×2（9.00s / 7.66s——各起一个真实 `rpa-core run` 子进程、各自独立加载快照，这正是规则 7 要的隔离）、`test_capture_contract` setup 5.25s（本进程首个 DevServer fixture 的完整加载）、`test_protocols::test_catalog_is_loaded_and_digest_is_stable` 5.14s（刻意 `clear_catalog_cache()` 后重读——缓存化后它是唯一真正独立的 digest 验证）、`test_workbuddy_connector` 与 `test_python_worker` 的子进程交互（2.4–4.8s）；`.pytest_tmp` 由 `pytest_unconfigure` 在运行结束自动回收，工作区无残留。**落库**：按 4 个逻辑单元本地提交、未 push（规则 13）——`adc2423` test(harness) 测试隔离（hub 钉死 + 受限环境临时根兜底）、`70e155e` fix(platform) 浏览器检测/安装层平台化（含三处平台无关缺陷）、`c4f7185` perf(catalog) manifest 快照指纹缓存、本条 docs(progress)。**运维提醒**：会话期间 `workflows/test/workflow.json` 被**外部进程（前端编辑器的实时保存）**改写（diff 为插入带 `_exprModes: {}` 的 `data.setVar` 节点），与本次改动无关、已排除在提交之外——本仓工作树中可能存在「活的外部写入」，提交必须按显式路径 `git add <file>`，不要用 `git add -A`


2026-09-15 | desktop-gui-slice2 | done | GUI 第二切片 + 平台测试修复 + ADR 目标架构。**① macOS 平台模拟测试（生产代码不动）**：test_extension_installer 的 setenv HOME 改为 monkeypatch Path.home（Windows 主机读 USERPROFILE）；test_extension_launch darwin 断言改 p.as_posix()（Windows 主机 Path 规范成反斜杠）。**② 流程卡片画布**：flow_model.py 把 Workflow AST 映射到 QStandardItemModel——if 的 then/else、try 的 catch 用「虚拟组」行承载（AST 无节点，iter_real_nodes 导出剔除）；自定义 MIME 只携节点 id，自实现 mimeData/dropMimeData 做同模型移动，拒绝移入自身后代防成环。canvas.py 的 CardDelegate 自绘复刻 Web 视觉：白底圆角卡片、选中 #daedff、4px 深度色线、拖柄/同级序号/粗体命令名/等宽参数摘要（前 2 参数 +N）/命名空间徽标、虚拟组浅灰条。apply_theme 增补 CJK 字体回退。tests/contract/test_gui_canvas.py 10 例；真实桌面平台 grab 验证中文与卡片观感正常（offscreen QPA 在本沙箱不渲染字形，曾误判为字体问题）。**③ ADR 0014 §10 目标架构**：GUI 与 Web 是同一能力层两个宿主——GUI 进程内复用 DevServerApp 并内嵌仅挂 /api/ext/* 与 /api/capture/* 的 ExtHub loopback 网关（浏览器扩展契约取代不了），Web 退化为零安装形态；记录路由分组（editor/extension/capture）拆分切片与四条验收口径，契约（路径/方法/负载/错误码）不变。pyproject 把 .harness/demo/ 加入 ruff exclude（ADR 实证脚手架，同根 scripts/ 性质）。**验证**：FULL GATE PASSED（pytest/ruff/架构/tasks/node 全过）；本地提交、不 push。
2026-09-15 | desktop-gui-slice3 | done | GUI 第三切片：参数 schema 表单。**param_form.py**：ParamForm 按 manifest.input_schema（JSON Schema dict）生成原生控件——string+enum→QComboBox（首项「未设置」，x-enum-labels 中文显示，itemData 存真值）、string→QLineEdit（空=未设置，default 进 placeholder）、integer/number→带 QInt/QDoubleValidator 的 QLineEdit、boolean→QCheckBox、array/object→单行 JSON（非法 JSON 收集时 ValueError）、type 列表（["string","null"]）取首个非 null；必填标 *，description 进 tooltip；values() 按声明顺序收集且跳过未设置项。**flow_model.py**：新增 ROLE_ARGS_RAW 与 ArgsHolder（存 Python 对象引用，避开 QVariantMap 键序重排/类型丢失），删除不再使用的 _action_title。**app.py**：右栏 QScrollArea 表单 + 「应用参数」按钮，画布 currentChanged 驱动切换；应用后整体替换 holder 并刷新 ROLE_ARGS_SUMMARY（delegate 自动重绘）；容器/返回节点显占位提示；修改仅在内存、保存回 workflow.json 留后续切片；SAMPLE 的 navigate 非法字段 waitUntil 换成 schema 内 timeoutMs。**踩坑**：① 测试文件若不把 QT_QPA_PLATFORM=offscreen 置于 Qt 导入前，首个用例构造 QWidget 时进程直接 0xC0000409 崩溃（QApplication 缺失），fixture catalog 依赖 qapp 保证先建实例；② PySide6 QStandardItem.setData(dict) 会走 QVariantMap 转换，故用 ArgsHolder。**验证**：tests/contract/test_gui_param_form.py offscreen 12 例全绿；真实桌面平台 grab 三栏观感正确；全量门禁 377 passed，唯一 failed 为已知前台焦点抖动 e2e（test_windows_desktop_vertical_slice，单独重跑通过，与本次改动无关）；ruff/架构（50 files）/tasks（42 features）/node 全过；本地提交、不 push。
2026-09-15 | desktop-gui-slice4 | done | GUI 第四切片：编辑闭环（模型回写 AST + 保存 workflow.json）。**flow_model.py**：ArgsHolder 增加 raw（节点原始 AST dict 模板），每个真实节点都携带 holder；新增 model_to_workflow(model, meta)/_rebuild_node——以 raw 浅拷贝为模板只替换结构键（with/children/then/else/catch），GUI 不编辑的 condition/items/item_var/error_var/output_aliases/_exprModes 原样保留；虚拟组还原为 then/else/catch，源文档省略的空 else 不自行补出（build 时 else 组也仅在非空才建）。**app.py**：工具栏「保存」（Ctrl+S）；save_workflow 先回写 dict 再 Workflow.model_validate 校验，按 devserver 同款格式落盘（UTF-8/indent=2/末尾换行），校验失败进状态栏不写文件；无源路径（示例）走 QFileDialog 另存为；参数应用与拖拽置脏（rowsInserted/rowsRemoved + _loading 构建守卫），标题 • 前缀，closeEvent 弹保存/不保存/取消；set_workflow 统一深拷贝为 JSON 形状 dict（pydantic 走 model_dump(by_alias=True)），编辑不回写调用方对象；run_gui 透传 flow_path，打开真实文件即原位写回。**顺带修正**：SAMPLE_WORKFLOW 的 itemVar 是错键——ForEachNode/TryNode 该字段无别名，规范键名为 item_var/error_var（Web app.js 同样），此前示例 dict 无法通过 Workflow 校验。**验证**：test_gui_save_roundtrip.py offscreen 9 例；真实平台脚本走「改 url→应用→save_workflow→读盘」链路成功、dirty 复位、工具栏截图正常；全量门禁 386 passed，唯一桌面 e2e 焦点抖动单跑通过；ruff/架构（50 files）/tasks（43 features）/node 全过；本地提交、不 push。
2026-09-15 | desktop-gui-slice5 | done | GUI 第五切片：节点增删——从指令树构建流程。**flow_model.py**：existing_ids 收集真实节点 id；allocate_node_id 生成全模型唯一 id（n1、n2…跳过占用）；create_action_item 用规范 raw（type=action/空 with）建树 item；insert_command(command_id, target) 落点规则——容器（sequence/forEach/try）与虚拟分组追加末位、if 追加进「则执行」、叶子追加为父容器末位同级；remove_item 删真实节点（容器连子树），根与 then/else/catch 虚拟分组受保护。**app.py**：左树 itemDoubleClicked 新增（分组节点 command id 为 None 自动忽略），新增后展开父级+选中新节点（右栏随即可填参）；画布 QShortcut(Delete) 用 WidgetShortcut 作用域，避免与参数表单输入框的 Delete 编辑键冲突；删除后右栏复位占位。增删复用切片4 rowsInserted/rowsRemoved 自动置脏，无新脏标记通路。**验证**：test_gui_node_edit.py offscreen 14 例（四种落点/唯一 id/保护/子树/双击入口/回写校验/落盘）；真实平台脚本「双击新增 sleep→Delete 删 open→save_workflow」链路成功（ids=check,loop,done,n1，dirty 复位，截图观感正确）；全量门禁 400 passed，唯一桌面 e2e 焦点抖动单跑通过；ruff/架构（50 files）/tasks（44 features）/node 全过；本地提交、不 push。
2026-09-15 | desktop-gui-slice5-fix | done | 修复切片5人工实测暴露的两处交互断点（offscreen 测试覆盖不到）。**拖拽全程无法放置**：QTreeView.dragEnterEvent 进入时以无效 parent（QModelIndex()+row=-1）调 canDropMimeData 做能力探测，旧实现对无效 parent 一律 False → 拖拽从入口被整体拒绝，dragMove/drop 回调不再发生（模型级单测直接调 dropMimeData 故未暴露，真实平台诊断脚本插桩证实 can/drop 计数均为 0）。修复：无效 parent 仅校验 MIME 格式放行；dropMimeData 独立做「有效 parent + 容器/虚拟组 + 成环」防护，invalid parent 必 False 的旧契约不变。**Delete 在左树聚焦时失效**：双击左树添加后焦点停在 QTreeWidget，画布级 QShortcut(WidgetShortcut) 不触发；改为工具栏 QAction（WindowShortcut）+ QApplication.focusChanged 动态启停，焦点在 QLineEdit/QTextEdit/QPlainTextEdit/QAbstractSpinBox/可编辑 QComboBox 时禁用动作，Delete 交还文本编辑。**验证**：test_gui_canvas 补 dragEnter 探测契约（含异格式拒绝），test_gui_node_edit 补左树聚焦可删/输入框聚焦暂停 2 例（show 后 hide 收尾，offscreen 下 close 会触发未保存 QMessageBox.exec 访问冲突）；.harness/demo/diag_gui_interaction.py 真实平台复测：左树聚焦删除 n1 成功、输入框聚焦动作禁用、探测 accepted（Windows OLE 拖拽放置无法用合成事件自动化，手感人工实测）；全量门禁 404（首跑 2 例环境抖动，重跑全绿，改动仅限 GUI 层），ruff/架构/tasks/node 全过；本地提交、不 push。
2026-09-15 | gui-drag-com-fix | done | **修复 GUI 拖拽根因：顶层导入提前触发 pywinauto COM 初始化**。完整诊断链（QTest 合成事件→硬件 SetCursorPos/mouse_event→A/B 对照诊断脚本→原生拖拽探针）证实：系统与 Qt 原生 OLE 拖放完全正常，仅 rpa_core 产品 GUI 拖不动。关键证据：干净启动版（跳过 cli.py 顶层导入）stderr 无 COM 警告、拖拽正常；CLI gui 子命令启动版有 QWindowsContext: OleInitialize() failed: COM error 0x80010106。根因定位：cli.py 顶层 rom rpa_core.executors import ... 触发 executors/__init__ → desktop_win32 → pywinauto → 模块加载时 CoInitialize(COINIT_MULTITHREADED)；QApplication 随后调 OleInitialize(STA) 被拒 → OLE 拖放失效（DoDragDrop 模态循环跑着，但 drop target 收不到 DragEnter/DragMove/Drop）。**修复**（3 文件）：① cli.py 移除 executors/capture 顶层导入，在 
un()（executors）、_browser_capture_factory/_desktop_capture_factory/_cmd_capture（capture）内部延迟导入；② runtime/orchestrator.py ExecutorRegistry 仅作类型标注（加 rom __future__ import annotations + TYPE_CHECKING 守卫），彻底切断 orchestrator → executors 导入链；③ tests/contract/test_cli_parity.py 的 capture 测试 patch 从 cli.DesktopCaptureSession 改为 
pa_core.capture.DesktopCaptureSession（延迟导入后模块级属性不再存在）。**跨平台影响**：COM 是 Windows 独有的 OLE 机制，macOS/Linux 用 NSPasteboard/XDND 不存在此问题；但「GUI 路径必须走干净导入链」原则跨平台成立，未来若某平台特定包在模块顶层做全局初始化也会有类似风险。**验证**：python -m rpa_core.cli gui 启动版 stderr 完全干净无 COM 警告；ruff 全过；架构检查通过（50 files）；pytest 404 passed（跳过已知 flaky 桌面 e2e）；本地提交、不 push。
2026-09-15 | gui-drag-internal-move-fix | done | **修复 GUI 拖拽后卡片消失**：上一提交的 COM 根因解决后，干净启动版拖拽可发起，但松鼠标后卡片消失。完整诊断链（硬注入→干净启动→dropMimeData 插桩）证实 dropMimeData takeRow/insertRow 逻辑**零 bug**（rowCount 4→3→4 完全对上），根因是 **Qt InternalMove 模式下的二次删除**：QAbstractItemView.startDrag() 在 QDrag.exec() 返回 MoveAction 后会调 clearOrRemove() 再删一遍源行，但 FlowTreeModel.dropMimeData 已经用 takeRow + insertRow 完成原子移动，此时 Qt 再用旧索引删行必然删错。修复（canvas.py 新增 FlowTreeView）：重写 startDrag——6 行极简版（取 selectedIndexes → mimeData → QDrag + setMimeData → exec），彻底跳过基类的 clearOrRemove。dropMimeData 加注释说明与 FlowTreeView 的配合关系。**验证**：
pa-core gui 启动版（PID 18500）同级重排成功，done 正确移到 open 上方；403 passed，唯一 flaky 桌面 e2e 除外；ruff/架构检查通过。**本地提交**：925f67c COM 延迟导入 + 7313017 InternalMove 二次删除修复。
2026-09-15 | gui-new-open-and-drag-polish | done | **新建/打开入口 + 拖拽体验打磨（结束标记 + 自管 hit-test）**。切片6之前的交互缺口补齐：① **文件操作入口**（app.py）：工具栏新增「新建」（Ctrl+N，清空画布为空白 sequence）、「打开」（Ctrl+O，QFileDialog 选 workflow.json + Workflow.model_validate 校验）、「保存」合并为三按钮一组；有未保存修改时弹 QMessageBox.YesNo 确认框防误丢；run_gui 新增 flow_path 参数，CLI 透传；**CLI**（cli.py）：gui 子命令新增 --workflow 参数，_cmd_gui 接受 args 并透传给 run_gui；修复 main() 漏传 args 给 _cmd_gui 的 TypeError。② **拖拽 hit-test 自管 + 结束标记行**（canvas.py + flow_model.py）：FlowTreeView 完整重写 startDrag/dragMoveEvent/dragLeaveEvent/dropEvent/paintEvent——dragMoveEvent 按 40/20/40 划分 Above/OnItem/Below，彻底接管落点判定（跳过 Qt 默认 OnItem 过宽问题）；自绘 4px 蓝色落点指示条 + 两端小圆点。影刀式结束标记：flow_model.py 新增 _END_BRACKET_TYPE 虚拟行类型，build_item 为 sequence/forEach 容器末尾、then/else/catch 虚拟组末尾追加虚拟结束行（parent=容器，折叠自动收起，不参与 AST 回写）；flags 让 end-bracket 不可拖可放；canDropMimeData/dropMimeData 加 _resolve_drop_parent fallback 路由；CardDelegate 新增 _paint_end_bracket 渲染：与父容器对齐的灰色斜体虚线边界行；dragMoveEvent 对 end-bracket 上半→容器末尾、下半→容器同级下方做专门翻译（50/50 划分，命中区 23px+）。③ 顺带：workflows/test/workflow.json 更新（之前调试产物）。**验证**：ruff 全过（QModelIndex import 排序修复）；test_gui_canvas/node_edit/save_roundtrip 35 例全绿；用户实测：「返回」拖到「浏览器打开」上方正确重排，结束标记行视觉清晰，瞄准灰色「结束 循环」行下半区可精确拖到循环同级下方；本地提交、不 push。

2026-09-15 | gui-end-bracket-and-flat-if | done | **结束标记行落点修正 + if 结构影刀式扁平化**。① **落点 bug**（大王实测反馈：灰色 `结束 循环` 下方居然还能插进一个缩进的「返回」）：end-bracket 是容器的最后一个 child，凡按 `rowCount()` 追加都会落到结束行**下方**——画面上在容器外，AST 里却仍在容器内，视觉与语义相悖。新增 `_real_child_insert_row(parent)`（末位为 end-bracket 时上移一行），`insert_command` / `_resolve_drop_parent` / 同模型移动统一走它；补 3 条回归用例钉住。② **顺带挖出 PySide6 所有权陷阱（SIGSEGV 级）**：`QStandardItem.insertRow(row, item)` **不转移所有权**（只有 C++ 签名是 `QList<QStandardItem*>` 的 list/tuple 形式才转移），调用方丢弃返回值后 Python 引用归零 → C++ 对象被 GC 销毁 → 该行变 `None`，进一步可致 `SIGSEGV`（exit 139）。最小矩阵实测：`insertRow(row, item)` 不转移、`appendRow(item)` / `insertRow(row, [item])` 转移（`item.row()` 事后能否读取是最直观的判据）。全部改 `[new_item]` 形式并留注释。③ **if 结构扁平化**：去掉「则执行/否则执行」两层虚拟分组（`_VIRTUAL_GROUP_TYPES` 仅剩 branch-catch），改为 if 直属 then 子节点 + 常驻「否则」标记行（新增 `_ELSE_MARKER_TYPE`，`_TYPE_BADGE` 同步换掉 branch-then/branch-else）+「结束 如果」——**无 else 的 if 也常驻标记行**，回写时不落盘空 `else` 键（与源文档省略空 else 的既有约定一致）。`_branch_insert_row(parent, anchor)` 以标记行为界：then 段末尾 = 标记行之前、else 段末尾 = 容器末尾。`flags()` 让 end-bracket 与 else-marker 统一「不可拖不可选、可放置」。画布：`_paint_else_marker`（浅底 #eef1f4 + 左侧深度色条 + 粗体「否则」，经 `_marker_rect()` 缩进减 1 对齐父容器）、dragMoveEvent 对标记行做 50/50 切分（上半 → then 分支末尾、下半 → else 分支开头）。**验证**：GUI 契约 42 例全绿（canvas/node_edit/save_roundtrip，含新增 `test_else_marker_hit_test_splits_then_and_else` 驱动真实 `QDragMoveEvent`）；`/tmp/gui_if_structure.py` 实测树形态 `if → [action, else-marker, action, end-bracket]`，保存往返 if 节点 JSON 字节一致；ruff / 架构（50 files）/ tasks（44 features）全过；全量 pytest 2m41s 0 failed 0 error（排除被沙箱敏感内容审批拦住的 `test_extension_installer.py`）；本地提交、不 push。

2026-09-15 | gui-else-branch-optional | done | **「否则」从常驻虚拟标记行改为按需添加的可拖放指令 + 两处遗留清理**。① **语义反转**（大王要求：else 是单独指令、可拖放、有需要就加、默认不加）：`else-marker` → `else-branch`，`build_item` 只在 AST 有 else 段时才插该行，无 else 的 if 只剩 then + 「结束 如果」；新增 `FlowTreeModel.add_else_branch(if_item)` 作为唯一添加入口（幂等，非 if 返回 None）。它现在是**真指令**：`flags` 给 ItemIsSelectable + ItemIsDragEnabled（原先与结束行同属"不可拖不可选"），`remove_item` 放开删除——删掉即取消 else 分支，其下节点顺序不变、parent 仍是同一个 if，于是**自然并入 then 段末尾**（不丢节点）。**合成 id**：它没有 AST id 却要参与拖拽定位，用 `@else:<if_id>`（前缀 `@` 与 `allocate_node_id` 的 n1/n2 体系天然隔离；带 if id 才能在有多个 else 的树里精确定位被拖的那条）。**拖动边界**：只能在**同一个 if 内**移动（`canDropMimeData` / `dropMimeData` 同口径校验 `source.parent() is target`）——跨 if 迁移会静默改写两个 if 的分支归属，拖到 sequence 或拖出树同理拒绝；if 内移动是合法的（移到最前即 then 段清空、原 then 节点改归 else，模型级用例已钉住）。**入口**（app.py）：指令树最前面新增固定「流程控制」组承载 catalog 之外的控制指令，含一条「否则」（`ELSE_COMMAND_ID = "@else"`）；`add_command` 分流到 `_add_else_branch`，目标取选中项最近的 if 祖先，无 if 上下文/已有分支时只发状态栏提示不静默失败；`_apply_filter` 改为**显示名 + 命令 id 双匹配**（中文"否则"与 `@else` 都能搜到）。**画布**：`_paint_else_marker` → `_paint_else_branch`，补拖柄（"≡"，提示可拖）与选中/hover 态——它是指令而非装饰，视觉上要与结束行的浅灰虚线区分开。**② 两处遗留**：`gui/app.py` 的 `# noqa: N802（Qt 命名）` 中文括号被 ruff 判为 Invalid noqa directive（说明文字会被当成 code 列表的一部分）→ 说明移到上一行、行尾只留 `# noqa: N802`；`tests/contract/test_extension_installer.py` 的私钥夹具从**模块级**读取改为 `lru_cache` 惰性读取 + `PermissionError` 降级为 `pytest.skip`——模块级读一旦撞上沙箱敏感内容审批不可用，会让**整个文件**在收集阶段就 ERROR（大批 "ERROR at setup"），惰性化把影响收敛到真正用到夹具的用例且降级为 skip。**验证**：GUI 契约 69 例全绿（canvas/node_edit/save_roundtrip/param_form/smoke；含新增「无 else 不显示否则行」「add_else_branch 幂等」「否则行可拖/跨 if 拒绝/同 if 内移动后 then 清空」「删否则行并入 then」「指令树控制组 + 双匹配搜索」「添加→填 else→保存往返」）；`--collect-only` 证明私钥文件不再阻塞收集（57 例），依赖私钥的 `test_extension_id_matches_openssl_ground_truth` 实跑 PASSED；ruff / 架构（50 files）/ tasks（44 features）全过；全量 pytest 2m41s 0 failed 0 error；本地提交、不 push。

2026-09-16 | connector-runtime-range-fix | done | **修复连接器「连接失败」：cli.json 的 Python 运行时要求写成了裸版本号**。大王实测报错：`RPA Core 自动化 连接失败: Python connector venv ...\envs\default uses Python 3.13.14, which does not satisfy runtime requirement 3.12`。**根因**：`workbuddy-connector/cli.json` 的 `runtime.version` 是裸 `"3.12"`——裸版本号在 specifier 语义下等价于**精确要求**（`==3.12`，`packaging` 甚至直接判 `InvalidSpecifier`），而宿主托管的 Python 是 3.13.x，于是必然不满足。**三条证据**：① 宿主托管版本池只有 3.13.x（本机 `~/.workbuddy/binaries/python/versions/` 仅 `3.13.12` + `current`，内容即 `3.13.12`；Windows 上 default venv = 3.13.14）；② 本机缓存的**官方连接器市场**里 41 个 CLI 连接器**一律用范围语法**（40 个 `>=x.y`），唯一的 Python 样本 `emr-query` 写 `">=3.11"`；③ 本包发布 wheel `rpa_core_runtime-0.1.0-py3-none-any.whl` 的 `requires_python = ">=3.12"`（纯 Python，3.13 可用）。**修复**：`runtime.version` 改 `">=3.12"`，与 wheel metadata 同口径。**防回归**（`tests/contract/test_workbuddy_connector.py` +2）：① `runtime.version` 必须是范围语法，拒绝裸版本号；② `cli.json` 的运行时要求必须与 `pyproject.toml` 的 `requires-python` **逐字一致**（避免改一边忘一边）。**产物同步**：`workbuddy-connector.zip` 重新打包——原包是 09-04 旧快照（还带 `.cmd` 入口且缺 `install-extension`，早已滞后于源），现包内 `cli.json` 为 `>=3.12`；`connector-meta.json` 版本 0.1.0 → 0.1.1（连接器配置修复按市场约定递增）。**验证**：连接器契约 10 例全绿；`packaging` 实测 `==3.12` vs 3.13.14 → False、`>=3.12` → True；ruff / 架构（50 files / 83 manifests）/ tasks（44 features）全过；全量 pytest 0 failed 0 error；本地提交、不 push。
2026-09-16 | gui-left-tree-drag-and-trash | done | GUI 左树→画布拖拽 + 卡片尾删除按钮（垃圾桶图标）+ 根容器扁平化 + 虚拟分组白色卡片 + else-branch 拖出守卫 + 徽标移除。**canvas.py**：FlowTreeView 重写 startDrag/dragMoveEvent/dropEvent/paintEvent，自管 hit-test + 落点指示条；CardDelegate 统一走 _paint_card（虚拟分组、else-branch、普通卡片）；_paint_trash 静态方法手绘垃圾桶（桶盖 + 梯形桶身 + 桶内竖线）替代歪扭的 ×；删除命名空间徽标，内容区域改为预留 32px 给删除按钮；dragDropMode 改 DragDrop 接受外部 MIME；mouseReleaseEvent 右尾 16×16 热区触发 remove_row。**flow_model.py**：build_model_from_workflow 扁平化 sequence root（children 提升到顶层）；insert_command/insert_node/create_action_item/create_node_item；create_node_item 支持 
ode_type:xxx MIME 分支；insert_command 落点规则（容器/虚拟组追加末位、if 进则执行、叶子追加同级）；remove_row 新增通用入口；canDropMimeData/dropMimeData 支持 _MIME_COMMAND（左树→画布新建）与 _MIME_TYPE（内部移动）双分支 + invalid parent 翻译（取 invisibleRootItem）+ 同 if 内 else-branch 移动守卫 + _real_child_insert_row 跳过 end-bracket。**app.py**：_CommandTree 子类化，startDrag 发 pplication/x-rpa-flow-command（catalog 命令=完整 id，控制流节点=
ode_type:xxx）；左树 itemDoubleClicked 复用 insert_command 逻辑。**else-branch 守卫**：canDropMimeData 和 dropMimeData invalid parent 分支都加 _is_else_branch_id 拒绝，else-branch 只能在同一个 if 内移动。**验证**：ruff 全过；GUI 启动无 COM/崩溃警告；用户实测：hover 卡片尾显示垃圾桶图标、点击删节点、左树拖命令到画布正确追加（sequence 末位 / forEach 内部 / if 的 then 段）、else 分支拖出 if 被拒、内部重排正确；本地提交、不 push。
2026-09-16 | gui-remove-toplevel-fix | done | 同步远端 26 提交后合并本地 WIP 并修复：①remove_item 顶层节点删除崩溃（用户报障 canvas 卡片尾删除按钮路径）——Qt 语义顶层 item parent()==None，旧 guard 误护顶层首行且非首行 takeRow 触发 AttributeError，改 parent or invisibleRootItem 显式回退 + 回归测试 remove_row 顶层非首行；②远端带入两处存量失败：test_insert_targeting_leaf_becomes_sibling 断言改 parent() is None（Qt 语义），test_flags_enforce_drag_drop_rules 改自带夹具（原依赖 workflows/test 草稿含顶层 forEach，被手测编辑后 StopIteration）；③本地 WIP（extVersion 上报 + hub hosts 去重）与远端焦点上报（foc/focat）冲突合并并存，E501 修 4 处；GUI 合同 70+ 项全绿，full gate passed（基线 722018e）
2026-09-16 | gui-command-tree-cards | done | 指令树影刀式卡片化（维护者需求）：新增 gui/command_palette.py——CommandCardDelegate（叶子=圆角卡片：命名空间彩色图标块取显示名首字符 + 中文名粗体 + 小字命令 id，hover/选中态与画布卡片同调色板；分组行保持轻文本粗体，行高 46/26）+ load_command_display_names（正则解析 i18n.js commands 块，单一事实来源不建第二份映射，缺失回退 id，与 test_editor_i18n 同口径）；app.py populate_command_tree 加 names 参数 + 组内按 manifest x-palette-order 影刀序排列（与 Web 面板同口径）；ROLE_COMMAND_ID 迁至 command_palette（app re-export 兼容既有测试）；离屏截图验证卡片布局（offscreen 无 CJK 字体显示为占位框，真实环境正常）；合同测试 +5（中文名/回退/解析层全量覆盖/delegate 行高/中文搜索/影刀序首位）；full gate passed
2026-09-16 | gui-delete-text-button | done | 画布卡片尾删除按钮由手绘垃圾桶图标改为文字胶囊（维护者反馈图标不美观）：_paint_trash 删除，_paint_delete_button 浅灰圆角胶囊 + 红色「删除」文字；新增模块级 delete_button_rect（paint 与 FlowTreeView.mouseReleaseEvent 热区共用同一矩形，所见即所点），修 QRect 闭区间语义（right=left+width-1）导致的右边距/居中 1px 偏差；内容区右预留随按钮加宽自适应；合同 +1（按钮矩形几何）；全量 430 passed（test_windows_desktop_vertical_slice 门禁内前台焦点抖动，单独重跑绿——已知环境敏感老毛病）
2026-09-16 | gui-else-delete-fix | done | 修复「否则」指令点删除无效（维护者报障）：else-branch 行 virtual=True，mouseReleaseEvent 用 ROLE_IS_VIRTUAL 一刀切拦截，而模型层 remove_item 本就对其例外放行——视图重复了更严的策略；改为可删性判定委托模型（remove_row 返回 bool，策略单源），视图不再自行判断 virtual；回归测试合成点击 else-branch 行删除按钮热区（QMouseEvent 全签名构造消弃用警告）断言行删除 + AST else 段取消并入 then；ruff 全过；全量 431 passed（test_uia_desktop_vertical_slice 门禁内前台焦点抖动、重跑即绿——已知环境敏感老毛病，与本次无关）
2026-09-16 | gui-canvas-gutter | done | 画布左侧编号栏对标影刀（维护者需求）：①全局行号——编号栏固定 46px 不随缩进移动，_visible_row_number 沿 indexAbove 链数可见行，折叠后自动重排，卡片内同级序号删除；②错误徽标——红底白 ! 圆点，action 必填参数缺失（manifest input_schema.required vs ROLE_ARGS_RAW）即标记，CardDelegate 增可选 catalog 参数（build_canvas/MainWindow 透传）；③收起/展开按钮——容器行栏内 −/+ 小方钮，collapse_button_rect 与 mouseReleaseEvent 热区同源（深层行 indexAt 按 y 取行规避缩进盲区），栏内其余点击不冒泡；示例流程补齐 browserType/workspace 必填参数（默认视图不应自带错误标记）；合同 +3（全局编号折叠重排/错误徽标判定/按钮点击折叠展开）；434 passed（UIA 桌面 E2E 对话框 attach 环境敏感抖动依旧，与该链路无 import 关系，重跑间结果不一致证实抖动）
2026-09-16 | gui-gutter-stable-numbering | done | 编号栏两处修正（维护者反馈）：①指示线与序号重叠——根因是 Qt 按树缩进自绘的 branch 引导线落在编号栏区域；解法 setIndentation(0) 让 branch 区整体消失，缩进改由 delegate 自算（_GUTTER_WIDTH + 深度×_INDENT），gutter 点击 indexAt 不再受缩进盲区影响（撤掉按 y 取行的绕行）；②序号稳定性——_visible_row_number（可见行计数，折叠即变）改 _row_number（全树前序遍历的逻辑行号，栈式 O(n)，序号跟随指令本身，收起 if/循环下方行号不变，影刀同款）；合同测试改为断言行号折叠后不变；FULL GATE PASSED（本轮桌面 E2E 无抖动）

2026-09-17 | gui-ext-loopback-gateway | done | 修复「GUI 运行浏览器流程：页面打开了却判失败」（维护者报障）。**根因**：扩展轮询地址硬编码 127.0.0.1:8765，GUI 独立运行无 devserver → hub 不存在；executor 自动拉起 Edge 带 URL（用户看到页面打开）后 15s 等不到插件上线 → EXECUTOR_FAILED browser_launch_no_host。**修复（ADR 0014 §10 目标架构落地）**：devserver/server.py 新增 ExtLoopbackGateway——_GatewayRequestHandler 只放行 /api/ext/* + /api/capture/*（其余 404），_GatewayHTTPServer 关 allow_reuse_address（Windows SO_REUSEADDR 双绑定陷阱防御）；probe_ext_hub 探测既有宿主。GUI `_ensure_ext_hub`：探测复用 devserver → 否则内嵌网关（端口与扩展硬编码一致）→ 端口被非 RPA 占用回退默认地址+状态栏明示；RunManager.hub_url 接入、closeEvent 回收。合同 9 项：网关路由放行/404/捕获通道/probe/假扩展全链路 + GUI 复用/内嵌/占用回退三态 + **E2E（内嵌网关+假扩展+真实 run 子进程 navigate succeeded）**。踩坑：测试文件顶层 import 其他含 executors 的测试模块 → pywinauto COM 先初始化，pytest 下同进程建 QMainWindow 0xC0000409 硬崩（独立脚本不崩），改为内联拷贝 FakeExtension + catalog fixture 依赖 qapp。full gate 496 passed（uia 桌面 E2E 前台焦点抖动、重跑即绿——用户桌面 Edge 窗口抢前台所致，与本次无关）+ ruff/架构/tasks 全过

2026-09-17 | gui-web-parity | done | GUI/Web 功能差异盘点 + 九切片补齐：差异矩阵（盘点 Web 9 功能域 vs GUI）→ 切A 控制流参数编辑（ControlNodeForm：if 条件 op/left/right、forEach items/item_var、try error_var、return value，parse_literal/format_literal 对齐 Web literalField 语义，control_node_title 刷新卡片标题）→ 切B 快照式撤销/重做（50 步上限，model.mutated 信号统一记账 + _last_doc 快照链）+ 复制/粘贴（clone_for_paste 全树 id 重映射 + 别名撞车 _copy 改名 + 子树 ${}/[] 引用同步改写）→ 切C fx 变量引用模式（ParamForm 纯文本字段 fx 开关 + 「＋变量」下拉光标插入 [name] 标签，_exprModes 写回 raw；运行期 resolve_tags 已有 9 例单测证兼容）→ 切D 编译校验（collect_validation_issues 三层：schema/必填缺口带节点 id/编译器，定位首个问题节点；顺手修 SAMPLE_WORKFLOW 的 ${data}/${rows} 非法引用为可编译形态）→ 切E 运行控制（复用 devserver RunManager 子进程宿主：运行/取消/QTimer 轮询/运行参数对话框/事件流 dock/行号按 stepStarted·Completed·Failed 蓝绿红着色，closeEvent 回收子进程）→ 切F 流程库管理（WorkflowDirStore 同 Web 存储：命名保存/下拉打开/CLI gui --workflows 透传）→ 切G 元素库（面板列表/校验/删除/插入 selector·locator + 桌面 hover 捕获子进程后台线程 + ElementDescriptor 契约入库）→ 切H 数据表格（TablePanel 网格编辑 + TableStore 读写 + rows_to_csv 导出）→ 切I 只读指令清单对话框 + 插件状态/Load unpacked 引导对话框（extension_status 本地探测）；测试：test_gui_param_form +11、test_gui_history 11（新）、test_gui_validate 7（新）、test_gui_run 6（新，含真实子进程 run/cancel）、test_gui_panels 10（新）、test_gui_flows 6（新）；full gate 487 passed（test_windows_desktop_vertical_slice 门禁内前台焦点抖动、单独重跑即绿——已知环境敏感老毛病）+ ruff/架构(53 py/83 manifests)/tasks/node 全过

2026-09-17 | gui-run-float-window | done | 运行时悬浮窗（影刀式，维护者需求：运行开右下小窗、隐藏主窗、实时显示步骤）。**实现**：①`cli.py` run/resume 用 `orchestrator.start()` 同步拿 RunHandle，先打单行 `{"run_id": ...}` 早期标记再 `await wait()`（对 stdout 契约纯增量，末尾 RunResult 解析不变）；②`runs.py` `_read_stdout` 逐行解析单行标记提前设 `real_run_id`（EOF 解析保留兜底）→ **运行中即可读 events.jsonl**；③新增 `gui/run_float.py` RunFloatWindow（置顶 Tool + 无边框卡片自绘，320×130 右下角，可拖动，状态点+当前步骤+进度+取消/还原按钮）；④`app.py` 接线：`_start_run` → `showMinimized()` + 浮窗；`_poll_run` 运行中 `_poll_run_events_live` 增量喂浮窗（stepStarted→当前步骤、stepCompleted→计数）+ 事件面板实时滚动 + 节点着色；成功 `_schedule_restore`（挂在窗口上的 QTimer，2s 后还原）/**失败·取消停留**等手动还原；取消请求记 `_cancel_requested` 使终态如实显示「已取消」；closeEvent 一并关闭无父浮窗。**决策**（维护者确认）：置顶小窗（影刀同款，接受桌面自动化遮挡）、成功自动还原/失败停留、不做开关。**踩坑与修复**（3 处 Qt 生命周期竞态，均为 offscreen 测试暴露）：①`apply_theme` 每模块重复 `setStyleSheet` → 加「每 QApplication 只应用一次」守卫；②插件通道徽标探测线程直接向 Qt 对象 `emit` → 改为线程只写纯 Python 结果 + 窗口自有 QTimer 取回（无跨线程 Qt 调用），并加 `isVisible()` 门（隐藏窗口不探测）与 `shiboken6.isValid` 兜底；③测试长事件泵（qWait 2.4s）一次性泵出全会话累积的 DeferredDelete（含已回收窗口悬空目标）→ 自动还原用例改为断言排程参数 + 手动触发。**测试**：test_run_control +1（运行中事件可见回归）、test_gui_run +3（浮窗状态机/最小化+运行中事件/成功排程还原）；full gate 500 passed（test_windows_desktop_vertical_slice 门禁内前台焦点抖动、单独重跑即绿——已知环境敏感）+ ruff/架构(54 py/83 manifests)/tasks/node 全过

2026-09-17 | gui-pending-edits-autocommit | done | 修复「改完参数点保存无效」（维护者报障：test 流程 browserType 改 chrome 后保存没生效）。**根因**：GUI 参数面板与 Web 不同——Web 改字段即生效，GUI 需点「应用参数」才写回模型，直接保存写的是旧值且无任何提示（静默丢失修改）。**修复**：新增 `_pending_apply`（apply 回调 + dirty 判定 + 挂载时 index）登记机制，`_commit_pending_edits()` 在四个入口自动提交未应用编辑：①保存（`_build_document` 单一漏斗，覆盖原位保存与命名保存）；②运行（`_run_workflow`，随自动落盘一起生效）；③校验（`_validate_workflow`）；④切换选中节点（`_on_canvas_selection`，仅 `current != previous` 时提交——同节点重渲染不提交，否则会把变更前的表单状态回灌、覆盖插入元素等程序性修改，test_gui_panels 回归抓到）；dirty 判定含 action 的 `with` 与 `_exprModes`、控制流结构字段，无改动跳过（不产生垃圾撤销快照）；模型重建后 index 失配则丢弃陈旧面板。**测试**：test_gui_param_form +4（保存即提交/无改动不产生历史/切换节点提交/校验提交）。full gate 504 passed（test_uia_desktop_vertical_slice 门禁内前台焦点抖动、重跑即绿——已知环境敏感）+ ruff/架构(54 py/83 manifests)/tasks/node 全过

2026-09-17 | pytest-no-window-focus-steal | done | 修「每次 pytest 都弹 GUI 窗口抢焦点」（维护者反馈影响手头工作）。**根因**：真实桌面 E2E 三处会启动真实窗口并抢前台——`test_windows_desktop.py`（启动记事本并输入）、`test_uia_desktop.py`（编译运行 WinForms 演示程序 + `_force_foreground`）、`test_capture.py::test_desktop_capture_end_to_end_verifies_hit`（运行演示程序 + `set_focus()`）；GUI 合同测试本身已各自 `setdefault(QT_QPA_PLATFORM=offscreen)`（11 文件），不是弹窗来源。**修复**：①`tests/conftest.py` 全局兜底离屏（`if not os.environ.get(...)` 写法，空串也覆盖；平台插件在 QApplication 创建时定型，必须在任何 Qt 导入前设置）；②真实桌面 E2E 改显式开关 `RPA_DESKTOP_E2E=1`——缺省 skipif 跳过（reason 带可识别标记），日常 `uv run pytest` 不再弹窗；③`.harness/scripts/check_all.py` 为 pytest 子进程注入该变量，**门禁覆盖不丢**（实测 505 passed / 0 skipped）；④`pytest_terminal_summary` 在被跳过时打印一行启用提示（含 PowerShell 写法），避免误以为覆盖丢了。验证：`uv run pytest` = 6 desktop skipped + 其余全绿且无窗口；`check_all.py` = 505 passed 含桌面 E2E + ruff/架构/tasks/node 全过

2026-09-17 | M20-native-messaging | S1 done | 扩展通道迁移 Native Messaging 立项（ADR 0015 + M20 任务单 + feature native-messaging-bridge，四项取舍定案：全量切换/捕获一并迁移/三平台同步/pythonw 启动器）。**S1 传输层** `src/rpa_core/local_transport.py`：长度前缀 JSON framing（stdio 与端点共用 encode/read/write_message）+ Windows 命名管道（pywin32 overlapped）与 POSIX Unix 域套接字双实现 + endpoint_name/list_endpoints/connect/LocalEndpointServer。**五处实测踩坑（已固化进模块 docstring）**：① 本机 Windows Python 3.12 **无 socket.AF_UNIX** → 必须平台分派；② 同步管道句柄上「一端挂起的阻塞 ReadFile」会阻塞另一线程 WriteFile（中继模型必然并发读写）→ 全部句柄 FILE_FLAG_OVERLAPPED；③ pywin32 的 overlapped ReadFile/WriteFile/ConnectNamedPipe **不抛** ERROR_IO_PENDING 而是作为返回值返回 → 必须显式判返回码（此前按异常处理导致 996 误报）；④ ConnectNamedPipe 在客户端已接入时抛 ERROR_PIPE_CONNECTED 且不挂 overlapped → 必须直接交接，否则永远等不到完成事件（实测表现为「幽灵 accept」+ 536 ERROR_PIPE_LISTENING）；⑤ 挂起的 ConnectNamedPipe 无法被 CloseHandle 解除 → 关闭走 ctypes CancelIoEx（pywin32 仅暴露限调用线程的 CancelIo）。单测 15 项（framing 边界 7 + 端点往返/枚举/超时/多客户端/平台化双绑定 8）；ruff + 架构检查通过

2026-09-17 | M20-native-messaging | S2 done | **S2 host 子进程** `src/rpa_core/workers/ext_bridge.py`：由浏览器按需拉起的 bridge host——stdio Native Messaging 帧循环 + `hello` 握手（browser/instanceId）→ 以 `rpa_core_ext_<browser>_<instanceId>` 命名本地端点；客户端 `submit` 转 `command` 下发扩展、结果按 id 路由回发起客户端；host 侧强制超时（回 TIMEOUT + best-effort cancel，替代旧 hub 的 wait_seconds）；扩展主动消息广播全部客户端；stdin EOF（扩展断开/浏览器退出）→ 清理端点并退出——**「端点可连接」即在线，取消心跳窗口与 8765 常驻 hub**。诊断日志走 `RPA_EXT_BRIDGE_DEBUG` 到 stderr（stdout 只留给协议帧）。合同测试 `tests/contract/test_ext_bridge.py` 9 项：真实 spawn host 子进程 + 假扩展 stdio 对端 + 管道客户端，覆盖握手/身份/status/命令往返/超时+cancel/捕获透传/广播/并发按 id 路由/非法消息/EOF 回收端点。门禁 `check_all.py` 527 passed，2 项失败（run_control 启动失败用例超时、windows 桌面 E2E）单独重跑均绿——既有环境敏感 flake，与本切片无关；ruff/架构/tasks 全过

2026-09-17 | M20-native-messaging | S0 done | **S0 真机硬门槛通过（Edge 152 + Chrome 152）**：脚手架 `.harness/spike/native_messaging/`（最小 MV3 扩展 + `observe.py` 观测器/错误上报 + `register_host.py` 注册/注销/ID 发现 + README）。四项全过——① **SW 长连 native port 保活**：连续 `PING` 间隔稳定 15.00s、跨 3.5 分钟零空洞（Edge），Chrome 同样稳定；② **host 回收**：扩展 reload 与完全退出浏览器均 `ENDPOINT closed-by-host`/`HOST-DOWN`，`rpa-core-ext-host.exe` 残留 0（Edge 侧真退出由「杀掉后台驻留 `msedge --no-startup-window`」验证）；③ **重连时延** Edge ≈0.4s / Chrome ≈0.08s；④ **Load unpacked ID**：Edge profile 发现 `dfbjkpbeppapijmjcpppconbchmeinek` 与 `edge://extensions` 一致，且用**路径算出的同一 ID** 预注册 Chrome 亦被接受。**三个改变实现的发现**：(a) host manifest 的 `path` **不能带参数**（`"pythonw.exe" "script.py"`）——Chromium 不按命令行解析，解释器被无参拉起退化成读 stdin 的 REPL，表现为「扩展连上却无 host 逻辑、无任何日志」，极难定位；改为无参数 console script 入口 `rpa-core-ext-host = "rpa_core.workers.ext_bridge:main"`（pyproject 新增，manifest path 指向 venv 内 .exe，也正是 S3/S4 需要的稳定入口）；(b) 扩展侧 `connectNative` 必须串行化——修复前一次 reload 实测起 2 个 host，修复后 Edge/Chrome 均恒为 1 个；(c) **unpacked ID = 绝对路径 UTF-16LE 字节的 SHA256 前 128 位映射 a-p**，与浏览器无关 → S3 可预注册。另确证语义：「关闭浏览器窗口 ≠ 扩展离线」（Edge 后台驻留会让扩展 SW 无窗口重连），host 回收只在浏览器完全退出时发生，已写入 ADR 0015 §2 与任务单。观测手段沉淀：扩展把连接尝试/断开/异常 POST 到 `127.0.0.1:8799` 观测器，失败原因无需人工抄 SW 控制台。ADR 0015 同步更新（§2 入口约束/ID 推导/在线语义 + §4 代价改写）；ruff/架构/tasks 全过

2026-09-17 | M20-native-messaging | S3 done | **S3 安装注册产品化 + 传输层并发竞态修复**。**① native host 注册**（`extension_installer.py`）：`NATIVE_HOST_NAME=com.rpa_core.ext_bridge` / `NATIVE_HOST_ENTRY=rpa-core-ext-host`；`extension_id_from_path`（unpacked ID = 路径 UTF-16LE 的 SHA256 前 128 位映射 a-p）、`discover_unpacked_extension_id`（Secure Preferences location==4 且 path 匹配）、`resolve_native_host_extension_id`（显式 > 发现 > 推导）；`register/unregister/native_host_status/ensure_native_host`（幂等自愈，host 入口缺失报 NATIVE_HOST_MISSING）；三平台（Win HKCU `Software\<vendor>\<browser>\NativeMessagingHosts\<name>` + manifest 落 `~/.rpa-core/native-host/`；macOS/Linux 直接写浏览器 NativeMessagingHosts/）。CLI `install-extension`：默认引导同时注册 host（配套两步、逐浏览器容错）、`--status` 增 nativeHost 只读、`--remove` 一并注销。真实注册实测：真实扩展目录 → `edklhodhmpncghhhlpabpppimjakipdi`，edge/chrome 双注册，status 回读全对。测试 `tests/unit/test_native_host.py` 13 项 + 既有两条 CLI 合同测试随新行为更新（引导改断言「不写外部扩展注册表条目」、`--remove` 载荷含 nativeHost）。**② 传输层并发竞态**（S1 遗留，被并发路由合同测试间歇抓到 ~1/5）：客户端若在 `CreateNamedPipe` 与 `ConnectNamedPipe` **之间**接入，overlapped 的 `ConnectNamedPipe` 返回 pending 且**永不完成**（最小复现 + PeekNamedPipe 对照实测：已连接成功、未连接报 230），导致该客户端永远不被 `accept`；修法 `PeekNamedPipe` 兜底判已连接（`_begin_connect`/`accept` 各一层）+ 抽 `_connect_instance` 加确定性回归测试。验证：进程内探针 40/40、host 竞态探针 40/40、桥接合同测试连跑 6 轮全绿；门禁 393 passed（唯一失败为既有 UIA 桌面 E2E 焦点抖动，单跑即绿）；ruff/架构/tasks 全过




2026-09-17 | M20-native-messaging | S4 done | **S4 扩展改造（Edge 152 真机验收通过）**：`extension/manifest.json` 0.2.0→**0.3.0** + `nativeMessaging` 权限、摘掉 8765 的 host_permissions（保留 `<all_urls>` 与外部注册表路线所需 update_url）；`extension/background.js` 传输层重写——删 `poll()`/`execLoop`/`schedule()`/`DEVSERVER`/`EXEC_HOLD_S`，改 `connectNative("com.rpa_core.ext_bridge")`：`connect()` 用 connecting 标志**串行化**（S0 教训：并发会起多个 host）+ 3s 退避重连 + 30s alarm 兜底；host 推 `command`→`result`、推 `capture_arm/disarm`→广播标签页、`ping`→`pong`；`hello` 首帧上报身份与焦点，窗口焦点变化上报 `focus`；命令执行面（tabs/scripting/cookies/domOp/pageEval/waitComplete/权限）逐行保留零行为改动。`extension/content.js` 新增**启动即查询捕获态**（`rpa-capture-state`）——推送模型丢掉了旧 HTTP「每 5s 重复广播」对新页面的天然覆盖，首次真机验收即暴露（重载扩展+刷新页后无红框）；配套 background 用 `chrome.storage.session` 持久捕获态 + `chrome.tabs.onUpdated`页面加载完成补发 arm。`popup.js/html` 改显示 **bridge 连接状态**（+lastError）；新增 `capture_armed/capture_disarmed` ack（真机排障确认 arm 到达扩展）。README 协议段重写为 Native Messaging 消息面 + host 注册步骤与「path 必须无参数」约束。测试：installer 两处版本断言 0.2.0→0.3.0、`node --check` 三 JS 通过。**真机端到端验收**（spike `arm_capture_probe.py`：客户端→host→扩展→页面→描述符回传）：`capture_armed (extension ack)` → Ctrl+Click → `CAPTURE_RESULT {selector:{css:"#chat-textarea"}, verifyCount:1, url:"https://www.baidu.com/"}`。FULL GATE PASSED

2026-09-17 | M20-native-messaging | S5+S6+S7 done | **执行器/捕获/接线三切片一并迁移到 Native Messaging**（三者耦合，拆开会出现中间态不可用）。**S5 执行器**：`extension_exec.py` 重写——删 `ExtensionExecHub`/`HUB_URL_ENV`/`DEFAULT_HUB_URL`/`ONLINE_WINDOW_SECONDS`/`DEFAULT_POLL_SECONDS`（HTTP 长轮询与心跳窗口退役）；`ExtensionExecClient` 换本地端点，`online()/status_cached()/host()/submit()` 签名不变（`executors/browser_ext.py` 零改动），`status()` 聚合在线端点，`targetHost` 支持浏览器名或实例 id 且无匹配 `TARGET_HOST_OFFLINE` 快失败，`tabs.create` 补带端点实例 id；读取在守护线程做并带截止时间（管道 recv 不可超时，host 卡死不会挂住执行器）；端点前缀改环境变量可覆盖且 host/client 同源（测试隔离）。**S6 捕获**：`ExtensionCaptureSession` 自连端点 arm/disarm 并起读线程收 capture_result（无端点 pick 明确返回 offline）；`HybridCaptureSession` 内嵌扩展腿（不再依赖 devserver pending/result 路由），先回传者胜语义不变。**S7 接线**：devserver 删 `/api/ext/*` 与 `/api/capture/extension/*` 路由、`_host_report`、`ExtLoopbackGateway`/`probe_ext_hub`（ADR 0014 §10 loopback 网关整体退役）、`extension_hub_*`/`extension_command_*`/`extension_pending`/`extension_result`；runs.py 删 `hub_url`/`RPA_EXT_HUB_URL`；gui/app.py 删 `_ensure_ext_hub`/`_ext_gateway`/`_ext_hub_url`、徽标与插件对话框改探端点；cli env-status 改探端点。**测试**：删 HTTP hub 与 GUI 网关两文件；test_capture_extension 重写为假 bridge 端点（4）、test_capture_hybrid 扩展腿改假端点（4）、test_ext_bridge 增 7 项客户端契约（status/submit/错误码/target 路由/实例 id/离线）、conftest 隔离改端点前缀、test_capture e2e 改假端点。**真机端到端**：CLI run `browser.navigate https://example.com` → `succeeded`，`browserInstance=ef4d3919-…`/`tabId=933970309`/`transport=extension`。FULL GATE PASSED

2026-09-17 | M20-native-messaging | S8 done + feature 收口 | **文档/前端路由收尾并结项**。文档：`docs/extension-execution-plan.md` 顶部加「传输层已被 ADR 0015 取代」横幅（逐项列出退役的 `/api/ext/*`、`RPA_EXT_HUB_URL`、长轮询、`ExtensionExecHub`、心跳窗口、`ExtLoopbackGateway`）；`docs/extension-install.md` §1.2 补 Native Messaging + host 注册要求；`.harness/architecture.md` 增共享低层模块表（`local_transport`/`extension_exec`/`workers.ext_bridge`）。Web 编辑器：`devserver/static/app.js` 三处 `/api/ext/status` 改新增只读路由 `GET /api/extension/bridge-status`（端点聚合 + latestVersion，instances 平铺字段与旧 hub status 同形，前端无需改写）。清理核对：src 内无旧 hub/网关/路由残留。`.harness/spike/native_messaging/` 保留为可复跑实证工具（ruff exclude）。状态：feature `native-messaging-bridge` 置 passes=true、project_state completed=M20/active 清空、BACKLOG M20 移入已完成。FULL GATE PASSED

2026-09-17 | product-direction | done | **技术路线定案：GUI 为唯一主力形态（ADR 0016）**。背景：GUI 切片已补齐至与 Web 对齐（feature gui-web-parity），且扩展通道迁移 Native Messaging 后 GUI 独立运行不再需要任何 web 服务器；双前端并行成本高且漂移。决策：新增能力优先且默认落 GUI；Web 编辑器（devserver）退为可选形态，`devserver/static/` **冻结演进不删除**（不为它补齐 GUI 已有能力，仅在后端能力层复用或成本极低时顺带维护）；后端能力层与宿主形态无关，仍是唯一事实来源；GUI 不得引入对 devserver 进程或 web 端口的运行时依赖（安装期 `--policy` 托管 CRX 除外）。同步记录：AGENTS.md 增「Product direction」段、project_state notes、BACKLOG 后续任务。**附：修复 gui extra 被清掉**（维护者报障 `GUI_EXTRA_MISSING`）——根因是我先前为生成 `rpa-core-ext-host` 跑的 `uv sync --all-groups`：该参数只覆盖 dependency-groups、**不含** extras，sync 以指定集合为准会**修剪掉已装的 gui extra**（PySide6/QDarkStyle）；修复 `uv sync --all-groups --extra gui`，并把 AGENTS.md 的 Sync 命令与 pyproject gui extra 注释改为带 `--extra gui` 并注明「不带会清掉」。连带排障：改 pyproject 触发 uv 重装项目时 `rpa-core-ext-host.exe` 被浏览器拉起的 host 进程占用（os error 5）——用「临时注销 host → 同步 → 恢复注册」绕开，无需关浏览器，扩展通道已恢复 online（extVersion 0.3.0）。ruff/tasks/架构全过

2026-09-17 | backlog-planning | planned | **后续任务立项（写入任务单，未开工）**：① **M21 GUI 运行控制进阶**（`M21-run-control-advanced.md`）——现状核实：`RunManager` 只有 start/cancel/status/events/close，无 pause/resume；orchestrator 的暂停信号是**进程内** `asyncio.Event`（节点边界生效，ADR 0005），GUI 走子进程故需**跨进程控制通道**（首选控制文件，禁用 web 端口/进程内 runtime，ADR 0011/0016）；切片 S1 控制通道 + CLI pause/continue 同权、S2 RunManager+GUI 按钮与状态、S3 `recovery_required`/`indeterminate` 的人工确认恢复对话、S4 测试与 ADR 0005/0011 增补；明确区分「暂停/继续（同进程）」与「恢复 resume（新进程）」两个语义。② **M22 macOS/Linux 传输层真机验证**（`M22-crossplatform-transport.md`）——`local_transport` Unix socket 分支与三平台 host manifest 注册只做了设计与单测，仅 Windows 真机验证（M20）；切片 S1 最小握手、S2 S0 四项等价、S3 端到端、S4 差异修正与文档。③ 清理：删除与 ADR 0016 矛盾的「桌面客户端编辑器薄壳」远期条目（并在 BACKLOG 留删除说明）。feature_list 增 `run-control-advanced` / `crossplatform-transport-verify`（passes=false，未开工）；check_tasks 通过（48 features / 0 active）

2026-09-17 | M23-gui-ux-parity | G1 done | **单入口混合捕获（影刀式「一个按钮，指哪捕哪」）**：GUI 元素面板「捕获桌面元素」→「捕获元素」单入口，接 HybridCaptureSession（网页正文走扩展、桌面走 UIA hover，先回传者胜）；捕获期间主窗最小化、结束还原（showMinimized/showNormal+activateWindow）；插件离线时状态栏显式提示「网页区域无法捕获（桌面不受影响）」（UIA 兜底已证伪不静默捕获渲染层）；重入拦截 + closeEvent 取消会话（规则 11）；命名默认按 kind（browser→tag / desktop→controlType）。**连带修两处存量断链**：① hybrid=True 让位标志在 CLI/devserver 工厂被 pop 后从未转发给 desktop agent（浏览器内容区让位静默失效）→ HybridCaptureSession 构造时 setdefault("hybrid", True)；② 扩展腿离线时其 result_event 在 start 已置位 → 旧 pick 误判「扩展先回传」秒回 cancelled → 记录 extension_offline 并在 pick 跳过离线腿；③ CLI 桌面捕获从未 start()（扩展腿从未 arm）→ hover 默认 hybrid + is_extension_capture 时显式 start（与 devserver 同口径）。**另修 pre-existing 门禁污染**：test_native_host 的 S0 扩展 ID 锚点写的是旧开发机 D:\Users\Administrator\... 路径派生值（随仓库迁移误报）→ 改为字面量路径锚定算法（anti-drift 语义不变）。测试：test_capture_hybrid +2（离线退化/让位标志转发）、新增 test_gui_capture 4 例（hybrid 接线+最小化还原+入库、离线提示、重入拦截、关闭取消）；full gate 516 passed，3 个失败为已知桌面 E2E 前台焦点抖动（当前桌面处于交互会话中，HEAD 基线同失败、独立 harness 同流程验证 succeeded，与本次改动无关）；ruff/架构（56 py/83 manifests）全过

2026-09-17 | gui-extension-dialog-bridge | done | **GUI 插件对话框补齐 bridge host 注册入口（修「GUI 引导装完扩展通道仍离线」缺口）**：现状审计发现 CLI install-extension 默认引导同时注册 host，而 GUI 对话框只有 Load unpacked 四步、无注册入口无注册状态——纯 GUI 用户装完扩展通道仍离线且无处自助。落地：对话框新增每浏览器 bridge 注册状态行（_native_host_status_text 复用能力层 native_host_status 只读探针：已注册含扩展 ID 摘要 / 未注册 / 缺 host 入口提示先 uv sync）+「注册 bridge」按钮（_register_bridge_hosts 逐浏览器 ensure_native_host 幂等自愈，单浏览器失败不影响另一个，结果就地展示）+ 引导文案补第 0 步注册 + 刷新按钮三行联动刷新；离线提示文案从「踢回 CLI」改为指向对话框内按钮。测试：test_gui_panels +3（双浏览器注册态/缺入口文案/逐浏览器容错与失败原因透出）；full gate 521 passed（仅 1 个 UIA 桌面 E2E 焦点抖动，HEAD 基线与独立 harness 均已证非回归）；ruff/架构全过

2026-09-17 | M23-gui-ux-parity | recorded | **补齐缺失的 M23 任务单并把状态对齐现实**：BACKLOG 引用了 `M23-gui-ux-parity.md` 但文件不存在（28 个计划引用里唯一 MISS，check_tasks 不校验 BACKLOG 引用故门禁未报）。按 BACKLOG 的 G1–G4 描述 + PROGRESS 里 G1 的实际证据补齐任务单：G1 捕获链路（主体 done——单入口混合捕获、最小化/还原、离线提示、重入与关闭取消、连带修 3 处存量缺陷）标注 done 并留「剩余：捕获后确认对话框对齐 Web」；G2 画布交互（多选/批量/右键菜单/Ctrl+F）、G3 属性面板追平 Web（x-param-groups/output_aliases/重试超时防呆）、G4 失败定位闭环、切片内次级项（变量面板/菜单栏/卡片摘要）列为待办；关联的 `gui-extension-dialog-bridge`（插件对话框 bridge 注册入口）标注已交付。状态对齐：M23 由 `planned` 改 `active`（G1 已完成且已提交，属进行中工作流）——BACKLOG 移入「当前任务」、project_state active_milestone/active_plan 指向本任务单、feature_list 增 `gui-ux-parity`（passes=false）并设为 active_feature；check_tasks 通过（49 features / 1 active）

2026-09-18 | M23-gui-ux-parity | G2 done | **画布交互：多选 + 批量移动/删除 + 右键菜单 + Ctrl+F 查找**（对标 Web M11/M19）。① **多选**：`FlowTreeView` 改 `ExtendedSelection`；`_toggle_on_click` 检测 Ctrl/Shift 让路（否则扩选点击会顺带折叠容器）。② **批量移动**：`mimeData` 本就带多 id，`dropMimeData` 内部移动改批处理——过滤「祖先也在被拖集合」的项（移动祖先已连带移动它）、逐项成环守卫、按落点顺序依次 takeRow/insertRow 保持相对顺序；`canDropMimeData` 的 else-branch 与成环守卫同步改逐项。③ **批量删除**：`_delete_selected_node` 支持多选（`_selected_canvas_items` 去重按树序、跳过「祖先已选中」的后代避免对已摘下 item 重复操作），状态栏报「已删除 N 个节点」。④ **右键菜单**：`CustomContextMenu` + `_canvas_context_menu`（复制/粘贴/删除/添加「否则」）；可用性抽 `_canvas_menu_state(index)` 便于测试；右键未选中项先选中、落在选中集内保留多选。⑤ **Ctrl+F 查找**：画布顶部查找条（默认隐藏、Esc 关闭），匹配卡片标题/节点 id/类型/命令 id/参数摘要，Enter 循环定位（自动展开祖先 + 滚动居中），状态栏报「匹配 i/N」。**顺带修**：`_canvas_context_menu` 原把 QModelIndex 传给 `_nearest_if`（要 QStandardItem）→ 潜在死循环，已修正。测试：新增 `tests/contract/test_gui_canvas_batch.py` 14 例（批量移动顺序/跨父级/跳过后代/成环拒绝、批量删除/容器+子只删一次/虚拟行保护、菜单可用性矩阵、多选与菜单接线、查找匹配与循环/Esc 关闭）；FULL GATE PASSED

2026-09-18 | M23-gui-ux-parity | G2 fix | **修「连续操作后卡死/崩溃」（维护者报障）**——一类共因：PySide6 的 QStandardItem 包装在结构变更后失效，而多处仍持有/使用这些引用。**用户级根因（最可能的卡死点）**：画布查找（Ctrl+F）的匹配集缓存 item 引用，节点被移动/删除后 C++ 对象已删，`_find_next_in_canvas` 再对它们调 `index()`/`scrollTo` → `Internal C++ object already deleted` 或视图卡住；修：结构变更（`mutated`）后强制重算匹配集 + `shiboken6.isValid` 剔除失效项 + 全失效则按当前查询重算。**模型层**：① 批量移动 `takeRow` 返回空列表时不再 `insertRow(row, [])`（会插空行）；invisibleRootItem 从被拖集合排除；② `_is_descendant` 显式把顶层 item 的父补成 invisibleRootItem（Qt 顶层 `parent()` 为 None），否则「把根拖进自己的后代」漏过成环守卫，并加 visited 兜底；③ `find_by_id`/`iter_real_nodes` 改迭代 + visited，None 子项安全跳过；④ 新增模型级 `mutating` 标志（`@_mutating` 装饰 dropMimeData/remove_item/insert_command/insert_node/insert_subtree/add_else_branch），变更收尾自愈清理死行/裸空行（`_prune_dead_rows`）；⑤ `_real_child_insert_row`/`_else_branch_row`/`_rebuild_node`/`_virtual_groups`/`model_to_workflow` 全部加 None 与裸空行兜底。**App 层**：`_on_canvas_selection`/`_commit_pending_edits` 在 `mutating` 时直接返回；批量删除与粘贴期间 `blockSignals` 屏蔽选中信号，让结构变更原子化。测试：`test_gui_canvas_batch` +3（匹配重算/混入失效项安全/invisibleRoot 拖动被拒）；带看门狗的随机压力脚本 2000 步（多选/移动/删除/撤销重做/查找/粘贴）跑满 1944 步零崩溃零卡死；FULL GATE PASSED

2026-09-18 | M23-gui-ux-parity | G2 feedback | **维护者反馈两处已修**：① 选中「浏览器导航」+「如果」一起拖动后导航指令无法选中——模型层 16 种落点与视图层（含被拒落点、结束行/否则行路由落点）均未复现，判断为拖放后旧选中索引失效导致的观感，加固：dropEvent 移动成功后按新索引重选被移动节点并滚动居中（_reselect_nodes，且在取消信号屏蔽之后执行，app 只收到一次干净选中变化）；② 点击指令卡片不再展开/收起——移除 tree.clicked 折叠切换，展开/收起只保留编号栏 −/+ 按钮。测试 test_gui_canvas_batch +2（拖放后重选、点击卡片不折叠）；FULL GATE PASSED

2026-09-18 | M23-gui-ux-parity | G2 fix2 | **修「拖放后点击已选中指令不收敛、鼠标移上去才取消选中」（维护者报障）**——根因：`blockSignals(True)` 返回**之前**的阻塞状态（正常为 False），三处选中信号守卫（canvas.dropEvent / app 粘贴 / app 批量删除）写成 `blocked = sel.blockSignals(True)` + `if blocked: unblock`，导致第一次拖放/粘贴/删除后选中信号被**永久阻塞**：点击收敛在模型层已生效，但 selectionChanged 到不了视图，被取消选中的行不重绘，hover 触发该行重绘才显真。修：三处改为无条件成对 block/unblock。回归：test_gui_canvas +1（dropEvent 后 signalsBlocked 为 False、点击收敛时 selectionChanged 必送达）、test_gui_history +1（粘贴/删除后信号恢复）；FULL GATE PASSED（549 tests，desktop E2E 门禁内抖一次单独重跑过——已知环境敏感老毛病）
2026-09-18 | M23-gui-ux-parity | G2 fix3 | **修「第二次拖放多选指令顺序被颠倒」（维护者报障）**——根因：`selectedIndexes()` 按**选择先后**返回（先 Ctrl 选 check 再选 open → MIME ['check','open']），`dropMimeData` 按 MIME 顺序逐个 takeRow+insertRow，把画布上 open→check 的树序颠倒（既有测试全部按树序构造 MIME，未暴露）。修：移动前按 `_tree_order_key`（父链行号元组，顶层 parent()=None 补 invisibleRoot）排序被拖节点，落点顺序只跟树序、与点选先后无关。回归：test_gui_canvas_batch +2（跨父级/同级反向 MIME 顺序均保持树序）；FULL GATE PASSED（551 tests，desktop E2E 门禁内抖一次单独重跑过——已知环境敏感老毛病）
2026-09-18 | M23-gui-ux-parity | G2 fix4 | **修「网页内捕获无效」的可感知性（维护者报障）**——实况诊断：扩展仅装在 Chrome（Load unpacked，Default profile）且当时未运行，用户实际操作的是未装扩展的 Edge → 无 bridge 端点 → 扩展腿离线；按设计网页捕获只有扩展一条路（UIA 兜底已证伪），混合会话中桌面腿在浏览器内容区又让位给扩展，于是网页内既无红框也无响应。GUI 本来检测 extension_offline 并在状态栏提示，但随后立即 showMinimized()，提示不可见。修：_capture_element 在扩展腿离线时先弹 _confirm_capture_offline 确认框（继续=仅桌面捕获 / 取消并指引「插件」安装引导），取消则立即 close 会话回收桌面 agent、不进入 90s 空等。回归：test_gui_capture +1（离线取消路径：会话关闭/不最小化/状态栏提示），既有离线提示测试改为注入确认框继续分支；FULL GATE PASSED
2026-09-18 | M23-gui-ux-parity | G2 fix5 | **修「多浏览器并存时只有一个能框选」（维护者报障）**——根因：`ExtensionCaptureSession.start()` 只连第一个可用 bridge 端点、只 arm 那一个浏览器；Edge/Chrome 各装扩展时另一浏览器从未收到 capture_arm，而桌面腿在浏览器内容区让位给扩展，表现为「先框选的浏览器能用、换一个就不行」。修：start 改为连接**全部**在线端点逐一 arm、每通道独立读线程、任一 capture_result 先回传者胜；close/cancel 向全部端点下发 capture_disarm；通道全部断开才结束 pick 等待（单通道「断开=取消」语义的多通道推广）。回归：test_capture_extension +1（双 FakeBridge 同时被 arm、结果先回传者胜、双向 disarm）；FULL GATE PASSED
2026-09-18 | M23-gui-ux-parity | G1 剩余 done | **捕获后确认对话框对齐 Web**：新增 `ElementDialog`（gui/element_panel.py）——名称可改、selector 可编辑（browser=css 单行 / desktop=locator JSON，非法 JSON 内联报错拒收）、metadata 只读（tag/id/classes/text/rect + desktop controlType/automationId/window）、捕获时命中数 1 绿/其他红；app `_confirm_element_save` 承担对话框编排 + 同名覆盖 confirm（替换原 QInputDialog 纯命名）。测试：test_gui_panels +3（browser 默认值与回写、desktop JSON 校验、空名/空 selector 拒收）、test_gui_capture +2（取消不落库、同名覆盖确认双向）；FULL GATE 557 passed（uia 桌面 E2E 门禁内抖一次单独重跑过——已知环境敏感老毛病）
2026-09-18 | gate-no-popup-default | done | **门禁默认不再弹窗**（维护者要求）：`check_all.py` 原先无条件 `RPA_DESKTOP_E2E=1`，每次门禁都拉起记事本/WinForms/捕获悬浮框抢前台；改为默认不启用，显式 `--with-desktop-e2e` 或预设环境变量才跑真实桌面 E2E（环境变量已在系统里时 `dict(os.environ)` 自然透传）。同步修订 conftest 注释与 AGENTS.md 命令说明。验证：默认门禁 552 passed + 6 skipped（desktop e2e skipped 提示正常）+ ruff/架构/任务检查全过，无弹窗
2026-09-18 | M23-gui-ux-parity | G3 slice A done | **x-param-groups 分组折叠**：`ParamForm` 消费 `schema["x-param-groups"]` 渲染可折叠分组（对齐 Web `paramGroupPlan`/`paramGroupSection`）——新增 `_CollapsibleSection`（QToolButton-caret 标题 + body 折叠）、`_build_grouped_form`（分组遍历 + collapsed 有值自动展开 + 未声明字段归「其他」组）；`app._show_action_form` 传 manifest；无 x-param-groups 命令向后兼容平铺。测试 +8：分组渲染/字段数/collapsed 默认折叠+有值展开/toggle 切换/无分组向后兼容/未声明字段归其他/values 收集一致。FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | G3 slice B done | **x-outputs 输出别名编辑 UI**：`ParamForm` 新增 `output_aliases` 参数 + `_alias_fields` + `output_aliases()` 收集方法 + `_build_output_aliases` 区段（非 hidden 输出渲染别名输入框、红框校验非法标识符）；`app._show_action_form` 传 raw["output_aliases"]、apply 写回、`_action_form_dirty` 别名变更检测。测试 +6：非 hidden 渲染/hidden 过滤/roundtrip/非法别名拒绝+红框/无 x-outputs 不渲染/apply 写入+清空。FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | G3 slice C done | **重试/超时字段**：`ParamForm` 新增 `_build_retry_timeout` + `retry_timeout_values()`——超时（秒）：命令有 timeoutMs 时隐藏引擎级超时（对齐 Web）、无则渲染数字输入框；重试次数：retryable=true 渲染输入框、不支持但有遗留值显示警告+清除按钮（unsafe/not-declared 原因）、无值不渲染。`app.py` apply 写回 raw 顶层 timeout_seconds/retry_count + `_action_form_dirty` 超时/重试变更检测。测试 +9：timeout 隐藏（navigate）/timeout 显示+roundtrip/retry 渲染+默认空/retry 遗留值警告/retry 不渲染/values 收集/apply 写入+清除。FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | G4 slice A done | **失败节点跳转**：运行面板新增「跳转到失败节点」按钮——失败且有 nodeId 时显示、新运行隐藏；`_jump_to_failed_node` 通过 `find_by_id` + `setCurrentIndex` + `scrollTo` 定位画布节点。测试 +4：失败显示按钮/跳转选中/找不到提示/新运行隐藏。FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | G4 slice B done | **结构化错误详情**：运行面板新增 `_run_error_widget`（code/node/message/detail 四行结构化展示，对齐 Web `renderRunError`）——失败/启动失败均填充，新运行隐藏；detail 含 commandId/transport/reason 时展示。测试 +3：错误摘要显示/启动失败摘要/新运行隐藏。FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | G4 slice C done | **运行日志耗时/输出预览**：`_format_event` 方法格式化事件流——stepStarted → ▶ 标题 开始、stepCompleted → ✓ 标题 完成 (Ns) — 输出: keys、stepFailed → ✗ 标题 失败 (Ns) — code: message、stepRetried → ↻ 标题 重试、runStarted/Finished → ▸ 运行开始/结束 (status) — 耗时 Ns；其他事件保留原始 JSON。`_step_start_times` 追踪 per-step 耗时、`_run_started_at` 追踪总耗时。同步更新 `test_gui_run` 断言适配新格式。测试 +7（格式化 6 类事件 + 未知事件回退）。FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | card-summary-opt done | **卡片摘要关键字段优先**：`summarize_args` 新增 `_PRIORITY_KEYS`（url/selector/text/content/name/locator 等），重排参数字典让关键字段优先展示（对齐 Web 卡片摘要行为）。测试 +4（url 优先/selector 优先/空参数/limit 溢出）。FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | menu-bar done | **菜单栏 + 快捷键一览**：`QMenuBar` 四菜单（文件/编辑/运行/帮助），复用工具栏 QAction；帮助→快捷键一览弹窗（QDialog + QFormLayout）。测试 +3；FULL GATE PASSED2026-09-18 | M23-gui-ux-parity | variable-panel done | **设计期变量面板**：`_variables_dock` + `_refresh_variables`，收集 output_aliases + x-var-write 变量赋值，编辑菜单开关 + 应用参数后自动刷新。测试 +3；FULL GATE PASSED
2026-09-18 | M22-crossplatform-transport | S4 部分 | **修 macOS「扩展永远离线」（维护者报障，真机首次暴露）**——根因：POSIX 端点落 `sockaddr_un.sun_path`（103 字节上限），路径 = 61 字节 per-user TMPDIR + 56 字节端点名（含浏览器 UUID36）+ 5 = **123 字节** → host 被浏览器正常拉起却死在 `bind`（`OSError: AF_UNIX path too long`），而 `run()` 只捕 `LocalTransportError`（bind 抛的是 OSError）→ 带 traceback 静默退出，stderr 又被浏览器接管，用户侧只剩「离线」。修 6 处：①端点名实例段改 `sha256(instanceId)[:16]` 定长 token（哈希先于任何截断）；②`endpoint_dir()` 改 `$XDG_RUNTIME_DIR` 优先、缺省 `/tmp/rpa_core-<uid>/rpa_core_ext`（0700 + lstat 复核属主/类型；Linux 的 systemd 保障不变）；③bind/connect 前长度守卫抛领域异常（带路径/字节数/上限），`bind` 的裸 OSError 一并转领域异常；④`endpoint_diagnostics()` 启动自检写日志；⑤host 诊断**落盘** `~/.rpa-core/logs/ext-host.log`（`RPA_EXT_BRIDGE_LOG` 可覆盖）+ `main()` 顶层兜底留痕；⑥`/tmp` 残留端点回收（超期且连不上才删）+ `result` 信封补带真实 instanceId（`target_host` 原始 id 与 token 双形态命中）。测试：unit +5（定长 token/路径长度回归对照/目录短且私有/长度守卫/残留回收）、contract +4 且 `_HostProc` 不再 DEVNULL stderr（该文件此前 14 failed / 2 passed → **20 passed**）。真机验证（macOS + Edge 148 真实扩展）：`submit("tabs.list")` 返回 5 个真实标签页，host 日志 `endpoint self-check: pathBytes=72 limit=103 ok=true`。FULL GATE PASSED（604 passed / 12 skipped + ruff + 架构 + 任务 + node 检查）
2026-09-18 | M23-gui-ux-parity / M22 S3 | G1 平台退化修正 | **修 macOS「捕获元素无效」（维护者报障，真机首次暴露）**——根因：桌面捕获 agent 是 Windows-only（`desktop_agent` 的 `sys.platform != "win32"` 守卫），非 Windows 上 `pick()` **49ms** 即返回 `{"error": "desktop capture requires Windows"}`；而 `HybridCaptureSession` 的「先回传者胜」把这个**失败**当成「用户捕获了桌面元素」，抢在仍可用的扩展腿之前 `close()` 掉它（→ disarm）→ 用户侧「点捕获元素，窗口闪一下就弹回，网页里 Ctrl+Click 毫无反应」，且该 error 无 `timeout`/`kind` 落进 GUI 的 `cancelled` 分支显示成「已取消捕获」，真相被完全掩盖。修 4 处：①`DesktopCaptureSession.available` + `desktop_capture_available()`：非 Windows 不 spawn 子进程、`pick` 立即报不可用、hybrid 据此把该腿排除出竞速（连等都不等）；②**胜出判据收紧为「有效捕获描述符」**（`kind` ∈ {desktop,browser}，`hybrid._is_capture_result`）——腿的失败形态（unavailable/error/timeout/cancelled/崩溃/非法输出）一律不抢跑，任一腿失败只是出局、另一腿继续等；两腿都出局时**立即收场**并透出真实原因（不干等满超时、不伪装成取消）；③GUI 提示按真实平台能力改写（非 Windows 不再承诺「桌面也可用 F9」；两腿皆不可用不再弹「仍要继续仅桌面捕获吗」这个假选项）；④`_SocketChannel` 的 send/recv 把裸 `OSError` 收口为`LocalTransportError`——通道被别处 close 后 `recv` 曾抛 `OSError: [Errno 9] Bad file descriptor` 逃逸成 `ExtensionCaptureSession._read_loop` 的线程未捕获异常。**连带修：测试端点前缀隔离被穿透**（跨进程实测取证）——`conftest.py` 的隔离前缀 `rpa_core_ext_test_isolated_` 以默认前缀 `rpa_core_ext_` 开头，而 `list_endpoints` 用 `startswith` 过滤 → **真实进程会枚举到测试端点**，跑门禁期间用 GUI/CLI 捕获会 arm 到假 bridge 端点并落库（本机复现：真机验证脚本被测试端点接管，返回了 `test_capture_hybrid` 夹具里的 `#go` 描述符）→ 改互不包含的 `rpacore-iso_` + 守门测试。测试：`test_capture_hybrid` 对称用例 ×4（桌面不可用→退化扩展 / 失败腿不抢跑 / 两腿皆不可用快速失败 / 不可用腿不跑 pick）、`test_gui_capture` ×3、`test_local_transport` ×3。真机验证（macOS + Edge 148 真实扩展）：`HybridCaptureSession` 全路径 arm → 合成 Ctrl+Click（走真实 `content.js` onClick）→ 真实描述符 `#s-hotsearch-wrapper`（id/classes/text/rect/url 齐全），且换坐标得不同 selector（排除假阳性）。FULL GATE PASSED（614 passed / 12 skipped + ruff + 架构 + 任务 + node 检查）。平台边界已记入 M22：桌面捕获仅 Windows，macOS 等价物（AXUIElement）需单独立项。

2026-09-19 | M10-element-candidates | done | **捕获端升级：备选候选 + 语义特征 + 页面指纹（元素自愈的地基，纯确定性，不接任何模型）**。背景：调研 `browser-use/jev-ultrafast`（MIT）后确认——我们元素文档只有单条 css，站点改版即死（`executors/browser.py` 的 `_ext_not_found` 无退路），而它用「一次原子快照 + 索引化元素表 + 可访问名」证明这条路纯确定性可补。**关键取舍：不存"序号"**——核实 `snapshot.js` 原码后确认序号（`actions.forEach((a,i)=>a.id='e'+(i+1))`）只是当次快照的下标、WeakMap 身份号也只活一个页面会话，刷新即失效；可持久化的只有**页面自身的属性**。落地 5 处：①`extension/content.js` 新增 `roleOf`（14 个规范角色归一化）/`accessibleName`（ARIA 优先级链 aria-labelledby→aria-label→`<label>`→value→alt→文本→title→placeholder）/`labelTextOf`/`containerTextOf`（所属 form/dialog/li/tr 文本，用于同名消歧）/`candidatesFor`（CSS 可解析备选：data-testid/data-test/data-qa/name/aria-label/placeholder/title，按稳定性排序、带实测 matchedCount、与主 css 重复或零命中不收），分别写入 `selector.candidates` 与 `metadata`；**`selector.css` 语义完全不变**（仍是唯一必填与第一顺位）。②**修两处"重建 selector"的字段丢失点**（`gui/element_panel.py` 的 `result_document` 与 `devserver/static/app.js` 的元素弹窗保存）——原实现从头重建 selector，用户编辑一次就静默抹掉 candidates；改为以原 selector 为基底、只覆盖界面暴露的那一个键。③`model/capture.py` 新增 `_candidate_errors`：candidates 缺省完全不校验（老文档继续合法），出现时校验 shape（kind/selector 非空、matchedCount 为 >=1 整数且排除 bool）。④**顺带修两处静默数据丢失/崩溃**：扩展长期回传的**顶层 `url` 从未落盘**（pydantic `extra="ignore"` 静默丢弃，实测确认）→ 移入 `metadata.url` 并补 `title`；`element_panel.py` 的 `summarize_element` 与 `ElementDialog.__init__` 对非 dict 的 selector 会 AttributeError **崩掉整个元素面板** → 统一 `_as_dict` 收口（元素文件可被手工改成任意形状，报告它才是 `elements verify` 的职责）。⑤新增 `scripts/check_capture_helpers.mjs`（从 content.js 按锚点切出纯函数区、桩 DOM 求值，24 项断言）并接进 `check_all.py`——扩展 JS 此前**零测试覆盖**。测试：新增 `tests/unit/test_element_descriptor.py` 17 例（老文档兼容 / candidates 十种畸形 / selector 内键保留 / 顶层键丢弃 / 语义 metadata 往返）、`test_gui_panels` +2（编辑保留 candidates、非 dict selector 不崩）、`test_capture_contract` +2（put→get 候选与语义完整保留、verify 报坏候选）。FULL GATE PASSED（634 passed / 12 skipped + ruff + 架构 + 任务 + node）。**环境注意**：跑门禁**不要覆盖 `TMPDIR`**——`test_local_transport` 的 sun_path 回归对照断言的是真实 per-user TMPDIR 长度（macOS 48 字节），覆盖成短路径后该断言必然假失败（已实测）。未推送，本地提交。

2026-09-19 | M23-gui-ux-parity | G1 后继 macOS 窗口层级 done | **两个 macOS 平台行为缺陷（不是逻辑 bug），纯 Qt 可解、零新依赖**。报障：「运行时右下角小窗会跟主窗口一起隐藏；捕获元素后的确认弹窗需要额外点一次 Dock 图标（图标名 python3.12）才显示」。①`RunFloatWindow` 用 `Qt.Tool`，而 macOS 上 Qt.Tool 即 NSPanel——**应用失活时系统会隐藏全部 tool window**（Qt 文档 "By default, tool windows will disappear when the application is inactive"；源码侧 `hidesOnDeactivate = ((type & Qt::Tool) == Qt::Tool) && !flag`）。运行期主窗口被 `showMinimized()`、用户切到别的应用看进度 → 浮窗跟着一起没掉，而"主窗口最小化后仍能看到执行进度"恰恰是它存在的全部意义。修：`setAttribute(Qt.WidgetAttribute.WA_MacAlwaysShowToolWindow, True)`。**取证手法（值得复用）**：在真实 cocoa 平台下用 ctypes 调 `objc_msgSend` 直接问 AppKit 要 `NSView.window.hidesOnDeactivate` —— 实测浮窗 **False**、对照普通 Tool 窗口 **True**，比引文档硬得多（offscreen 平台测不出来，因为它不走 cocoa 路径）。②macOS 为防焦点窃取会**忽略后台应用的自激活请求**，此时 `raise_()`/`activateWindow()` 都不足以把窗口提到最前；捕获期间我们必然是后台应用（主窗口已最小化、用户在浏览器里点元素）→ 确认框被创建了却停在浏览器之后。修：`app.py` 新增 `present_window(window, *, always_on_top=False)` —— 短命模态对话框临时 `WindowStaysOnTopHint`（纯 Qt 唯一可用的硬保证）+ show/raise/activate + `QApplication.alert`（macOS 弹跳 Dock 图标）兜底；接入捕获确认框、捕获结束还原、运行结束还原。**常驻主窗口不置顶**（否则会一直压住用户其它应用）。测试：`test_gui_run` +1（浮窗带该属性且仍是 Qt.Tool）、`test_gui_capture` +1（`present_window`：短命对话框置顶/常驻不置顶）+ 命名对话框确实被置顶的断言；`test_capture_overwrite` 的 `FakeDialog` 由裸桩升级为真 `QDialog` 子类（`present_window` 需要 `setWindowFlag/show/raise_` 等 QWidget 契约，契约不完整的 stub 会被直接打到——这类改动的常见爆点）。FULL GATE PASSED（+ ruff + 架构 + 任务 + 3 个 node）。遗留：Dock 显示 `python3.12` 是**无 bundle 进程**的固有行为（Apple QA1544：`localizedName` 取 CFBundleDisplayName → CFBundleName → 进程名），零依赖改不了，需决策走 `.app` 打包（推荐，发行反正要做）或私有 SPI `CPSSetProcessName`（有版本风险）；已记入 M23 风险区，未擅自引入。

2026-09-19 | M22-crossplatform-transport | S1–S4 done（macOS 侧完成，Linux 仍未真机） | **macOS 上完成 Native Messaging 通道真机验收**（M20 三平台实现、此前只在 Windows 验过；本机即 macOS + Edge 153 + 扩展 0.3.1）。①**S1 握手**：端点 `/tmp/rpa_core-501/rpa_core_ext/rpa_core_ext_msedge_<token>.sock`，`pathBytes=72 ≤ 103`、目录 0700 且属主=euid；`ExtensionExecClient().status()` 报 `online: true`（msedge/153/0.3.1/MacIntel）；`rpa-core env-status` 的 edge 五项全 true、chrome 全 false（本机未装）、`_online: "live"`（实时探测非缓存）。②**S2 四项**：**(a) 保活**——host 单 pid `84978` 自 10:37:51 服务至 22:01:30（**11 小时 23 分**），期间端点 socket inode 未变、日志零断连事件 ⇒「端点可连接 ⇔ 扩展在线」在 macOS 成立；**口径纠偏（重要）**：spike 的 15s `ping` 是 `.harness/spike/native_messaging/extension/background.js` 里 **spike 扩展**用 `setInterval` 主动发的**观测手段**，产品扩展只在收到 host `ping` 时回 `pong`、自身从不主动发，故"看不到 ping"≠保活失败，复验别按 spike 口径找心跳；**(b) 回收**——扩展 reload 后旧 host 消失、端点 inode `24654880`→`24708529`（重新 bind）；用**独立 profile 的 headless Edge 实例**（`instanceId` 是 profile 级 `crypto.randomUUID()`，端点名不同，**不撞用户实例**）验浏览器退出：rc=0 退出后该实例端点**立即消失且 socket 文件从磁盘被删除**，两条路径均无残留进程；**(c) 重连时延**——reload 后首次 **13s**（SW 冷启动 + 3s 退避）、同轮第二次 **<1s**、独立实例冷启动到上线 **2s**，**与 Windows S0 记录的「重连 ≤0.4s」不是同一触发场景，已记为待统一口径复核**；**(d) ID 一致**——profile 发现 `djhfackbkhofdepbmideabbecgpeohhi` ＝ 路径推导 ＝ manifest `allowed_origins`，且扩展实际连上 host 构成端到端闭环。③**S3 端到端**：`rpa-core run` 跑最小 `browser.navigate`（`action=goto` **新建标签页、不动用户现有页面**）→ `succeeded` / 1760ms / effect committed / `details.transport: "extension"`，输出的 `browserInstance` 与 `status()` 报告的宿主实例 id 一致。④**S4 文档**：`docs/extension-install.md` §1.2 加**平台验证状态表**并**显式标注旧句失效**——原文「macOS 亲测可用、`browser.navigate` / `input` / `click` / `waitFor` / `screenshot` 全链路通过」是 **bsk 传输时代**结论，切 Native Messaging 后 macOS 一度恒离线（§6 端点路径长度缺陷），沿用会误导；ADR 0015 加 §7（状态表 + 4 条验收事实与偏差）；`architecture.md` 共享低层模块段加平台覆盖提示；`extension/README.md` 补 macOS manifest 落盘位置（**macOS 上 Chrome 是 `Google/Chrome` 两层、Edge 是 `Microsoft Edge` 单层**，照抄另一平台路径会恒报离线）与 host 生命周期语义（端点可连接 ⇔ 扩展在线、无需心跳），并修「`browser.navigate` 不再暴露」歧义（命令仍在命令面，移除的是 `transport`/`channel` **入参**）。⑤**launcher/路径表核对**：`pgrep -f` 命中 **1 条** Edge 主进程命令行、同时存在的 **16 个 `Microsoft Edge Helper` 子进程零误判**；`--load-extension` 在 Edge 153 **仍有效**（主进程命令行携带该参数 + Secure Preferences 中该扩展 `location=4`、`path`=源码目录、无 `disable_reasons`）；注：`_posix_process_matches` 的 `ps` 回退分支在本机沙箱下 `Operation not permitted`，但 `pgrep` 主路径正常，`edge.running=true` 判定正确。**未完成**：Linux 全线未真机（`$XDG_RUNTIME_DIR` 缺省回退、`pgrep` 在目标发行版的命中待验）。

2026-09-19 | M21-run-control-advanced | done | **GUI 运行控制进阶：跨进程暂停 + 浏览器会话续接 + 恢复人工确认**。**先纠一处计划内部不自洽**：M21 计划「术语区分」表写暂停/继续是「同一 run 进程存活、节点边界停住后继续」，而 ADR 0005 的既定契约是**暂停即收口**（run task 结束、进程退出、`paused` 落盘），且计划自己的 S3 已按「spawn `rpa-core resume` 新进程」写——两者冲突。与维护者确认后**按 ADR 0005 走「收口式」**：不做进程内挂起、不引入资源租约/心跳，「继续」= 从检查点起新进程；计划术语表已就地修正。收口式顺带消解了规划期三条开放问题（暂停期间无存活进程 ⇒ 无需存活期上限；GUI 关闭后任何一方都能从 `run_artifacts/<run_id>/` 接管）。**新增原计划没有的必备配套——浏览器会话跨进程续接**（不做则「继续」在浏览器流程上必失败）：实测三条证据——`checkpoint.json` 已持久化 `sessionId` + `tabId` + `browserInstance`；`PlaywrightExecutor.close()` **只解绑、不关用户浏览器里的标签页** ⇒ 标签页物理跨进程存活；但 `sessionId → tabId` 映射是**进程内字典**，新 executor 用旧 `sessionId` 直接报「缺少有效会话」。ADR 0013（移除 playwright、统一扩展单通道）之后 ADR 0004 那句「浏览器/桌面 session 不跨进程存活」已过时，**已修订为浏览器特例可续接、桌面仍不跨进程**。**交付**：①`src/rpa_core/control_channel.py`（新，顶层零依赖）——`control_path`/`request_pause`/`request_continue`/`read_control`/`pause_requested`/`reset_control`/`watch_control_file`；**放顶层而非 `runtime/`** 是因为三方共用（runtime 轮询 / CLI 写 / devserver 写）而 `check_devserver_isolation` 禁止 devserver import `rpa_core.runtime`；原子写（临时文件 + `os.replace`）、读取失效安全（半写/损坏按「无请求」处理，绝不因此暂停或崩溃）。②`runtime/orchestrator.py`：`RunHandle.resume()`（清未生效的暂停请求，ADR 0005「请求可被覆盖」）+ `_resume` 调 `executors.restore_from_scopes(scopes)`。③`executors/registry.py`：`restore_from_scopes(scopes)` 恢复钩子（尽力而为、异常静默）。④`executors/browser.py`：`session_bindings_from_scopes()`（纯函数，从快照提取 `sessionId→tabId`/实例，`detach` 的会话不复活）+ `PlaywrightExecutor.restore_from_scopes()`。⑤`cli.py`：`run`/`resume` 挂 `_await_with_control`（run 结束即取消 watcher）、`resume` 分支先 `reset_control`（防残留请求让新进程一启动就停）、新增 `rpa-core pause --run-id [--artifacts]`（与 GUI 同权，ADR 0006 §6 通道对齐）。⑥`devserver/runs.py`：`_spawn` 抽出、entry 增 `workflow`/`real_run_id_ready`（Event）、`pause`/`continue_run`/`resume(allow_indeterminate=)`、`RunControlError`。⑦`gui/app.py`：工具栏与运行菜单增「暂停 / 继续」、`_pause_run`/`_continue_run`/`_adopt_run_handle`/`_confirm_resume`/`_resume_confirmation`，`_poll_run` 区分 `paused` 与两种需确认终态（`indeterminate`/`recovery_required` → 警告对话、**默认按钮是「不恢复」**，确认后才带 `--allow-indeterminate`），事件流加 `pauseRequested`/`runPaused`/`runResumed` 可读格式化。⑧`gui/run_float.py`：暂停/继续按钮、`show_pausing`、`_pause_pending`（防一秒数次的状态刷新冲掉「已请求暂停」提示）、`_RESUMABLE_STATES`。**测试**：`test_run_control_channel.py`（新 7 项，含**真子进程**「外部写 control.json → 节点边界停下」与「resume 忽略残留请求且不重跑已完成节点」）、`test_session_restore.py`（新 9 项）、`test_gui_run_pause.py`（新 8 项，含真子进程「暂停 → paused → 继续 → succeeded」与「`indeterminate` 不确认绝不恢复」）、`test_public_api` 加 `RunHandle.resume`。**真机验收**（macOS + Edge 153 + 扩展 0.3.1）：`browser.navigate(baidu)` → `workflow.sleep(2)` → 暂停 → resume → `browser.navigate(reload)` → `browser.getText(#kw)`；暂停时 `status=paused`、`completedSteps=['root/nap','root/open']`（进行中的 sleep 跑完才停）；resume 后 `status=succeeded`、`sessionId` **未变**、`reload` 与 `getText` 都作用在暂停前那个 `tabId` 上（`matchedCount=1`）；`stepStarted` 序列 = `open, nap, afterResume, readKw` ⇒ **已完成节点零重跑**。**文档**：新增 `docs/gui-run-control.md`（GUI 运行控制语义权威说明）；`docs/api-usage.md` 暂停/恢复章改写并**删掉那句已过时的「浏览器/桌面 session 不跨进程存活」**；ADR 0005 增补控制通道选型与边界；ADR 0011 §4 把 pause/resume 从「范围外」移出。静态门禁全过：ruff ✅、架构 ✅（58 python files / 83 manifests，较上轮 +1 为新增 `control_channel.py`）、任务 ✅（49 features / 1 active）、3 个 node 检查 ✅；M21 自身的 `test_run_control_channel`(7) / `test_session_restore`(9) / `test_gui_run_pause`(8) 与 `test_run_control` / `test_public_api` 均已跑过并通过。**完整 pytest 全量 sweep 本轮未跑**（待补一次 full gate 后再推送）。**遗留**：Web 编辑器（`devserver` HTTP 端点 + `static/app.js`）未加暂停/继续——按 ADR 0016「GUI 为唯一主力形态、Web 冻结演进」记录不计欠账；桌面 `desktop.uia` 仍不跨进程续接。未推送，本地提交。
2026-09-20 | navigate-launch-double-open | done | 修「打开网页有时候打开两个」（维护者报障，test 流程开百度复现）——根因：目标插件不在线/命令无人领取时走自动拉起路径，launch_browser 以 --new-window <url> 拉起浏览器（浏览器自身已打开目标页 = 第 1 个标签页），插件上线后旧实现无条件再 tabs_create 同 URL（= 第 2 个）；插件在线时不走此路故只开一个，完美解释「有时候」。修：_launch_then_create 拉起后改 _bind_launched_tab——按归一化 URL（_launched_url_key：去 scheme/www/尾斜杠/忽略 query，baidu.com↔www.baidu.com 互配）在 tabs.list 找回拉起打开的标签页（多匹配优先 active），wait_load 等加载完成后绑定会话；找不到（重定向到外站）才回退 tabs.create。配套：browser_ext.tabs_list 补 target_host 路由参数；扩展 tabs.waitLoad 返回补 completed/timedOut（旧版缺省按已完成，向后兼容），manifest 0.3.1→0.3.2 + installer 版本断言同步。测试：test_extension_launch 改写 2 例（拉起后零 tabs.create、tabId 来自绑定）+ 新增 3 例（绑定 miss 回退 create / 多匹配取 active+重定向归一 / URL key 归一化矩阵）；ruff + node --check + FULL GATE PASSED（全量 680 项 exit 0；期间 test_workflow_name_rejects_path_traversal 门禁内抖一次、单独重跑过——已知环境敏感老毛病）。注意：扩展需 reload 到 0.3.2 才有 waitLoad completed 字段，不重载主修复（执行器侧）依然生效
2026-09-20 | navigate-launch-bare-reuse | done | 「打开网页」拉起路径按维护者定案改 B+（裸拉起 + 冷启动复用空白启动页），取代前一日 A 案（带 URL 拉起 + 探测绑定）：维护者指出 A 案两缺陷——①探测靠 URL 归一化匹配，用户手动开的同名页可能被误认（快照排除法仍绕）②「创建动作本应自己返回 tabId，探测本身是绕路」。B+ 关键判断：拉起前 browser_running() 可确切区分冷启动/浏览器在跑。落地：launch_browser url 改可选（None=裸拉起，不带 --new-window）；executor _launch_then_create = 拉起前 _target_running（msedge→edge 键映射，探测失败保守按在跑）→ 裸拉起 → 冷启动则 _reuse_startup_tab（只认空白页 chrome://newtab|edge://newtab|about:blank，用现成 tabs.navigate 导航到目标 URL，扩展零新 op）→ 非冷启动/无空白页（恢复上次会话）回退 tabs.create（创建即返回 tabId，扩展 createTab 无窗口时 windows.create 兜底）；删除 _launched_url_key/_snapshot_tab_ids/_bind_launched_tab 全套探测逻辑；扩展 tabs.navigate 补 completed/timedOut（onTimeout 语义对齐，0.3.2 内）。测试：删探测类 3 例，改写 2 例（冷启动复用 NTP 全程零 tabs.create、在跑裸拉起重试创建）+ 新增 3 例（会话恢复不动用户页回退 create、_is_blank_tab_url 矩阵、裸拉起 argv）；ruff + node --check + FULL GATE PASSED
2026-09-20 | navigate-omnibox-focus-fix | done | 修「冷启动打开网页后地址栏 URL 呈全选高亮」（维护者报障）：根因——NTP 被 Chrome 打开时键盘焦点在地址栏，B+ 案原地 tabs.navigate 导航同一标签页后焦点状态被继承，新 URL 填入地址栏即呈全选（等价按了一次 Ctrl+L，无害但观感异常）。修法：_reuse_startup_tab 改 _replace_startup_tab——不再原地导航空白页，改 tabs.create 新建目标页（程序创建的标签页地址栏从不聚焦，且创建即返回 tabId）+ tabs.close 顺手关掉空白 NTP；先建后关保证窗口始终有标签页不被整窗关掉；关不掉不影响主流程。纯执行器侧改动，扩展零变更（tabs.create/tabs.close 现成）。测试：冷启动用例改断言 create+close 序列与 tabId 来自创建返回、会话恢复用例补「绝不关用户标签页」守卫；ruff + FULL GATE PASSED
2026-09-20 | navigate-leave-startup-tab | done | 冷启动不再关闭空白启动页（维护者反馈+影刀对照）：create+close 的关页动作是「指令以外的操作」且发生在用户眼前，观感比留个空白页差，影刀也不关。回退 _replace_startup_tab，_launch_then_create 收敛为最简形态——裸拉起（不带 URL）→ 等插件上线 → tabs.create（创建即返回 tabId、地址栏不聚焦、绝不动任何既有标签页）；连带删除 _target_running/browser_running 冷启动判定与 _is_blank_tab_url 空白页检测（都不再需要）。最终行为：在线=tabs.create 一个页；在跑休眠=裸拉起唤醒+create 一个页；冷启动=浏览器自己的启动页（NTP 或恢复会话）原样保留 + create 一个目标页。测试：删空白页矩阵用例、改写 2 例（仅 create 序列/裸拉起断言）、会话恢复用例改为「绝不触碰既有标签页」守卫（navigate/close/list 调用即失败）；ruff + FULL GATE PASSED
2026-09-20 | gui-pending-apply-consumed | done | 修「切换 browserType 保存不及时生效、有时要保存两次」（维护者报障）：根因——_commit_pending_edits 把待提交登记 _pending_apply 一次性消费（置 None），而参数表单仍挂在右栏；第一次保存/运行/校验把登记消费掉之后，对同一表单的再编辑无人接管，下次保存 _commit_pending_edits 直接早退、编辑静默丢失（offscreen 探针复现：切换→保存✓→再切换→保存丢失）。修：登记不随提交消费，dirty_fn 幂等防重复提交（无改动即跳过）；登记仍随表单生命周期失效——_clear_param_panel 重挂载/占位时作废、索引不属于当前模型（撤销/重建）判陈旧作废，安全性与旧语义一致。回归：test_gui_param_form 新增 test_second_edit_after_save_is_committed（第一次编辑提交后第二次编辑同样生效）；探针三场景（直接保存/切节点再回/连改连存）全过；ruff + FULL GATE PASSED
2026-09-20 | task-status-sweep | done | 状态收口（维护者定案）：① M22 Linux 真机验证**只记录不测试**（无 Linux 环境）——M22 任务单「未完成」与 BACKLOG 条目注明定案与将来复验清单（$XDG_RUNTIME_DIR 回退、pgrep 命中），feature crossplatform-transport-verify 改题翻 true（macOS 真机已验 / Linux 记录式收口）；② feature un-control-advanced 翻 true——M21 于 2026-09-19 完成后已连续多轮 FULL GATE PASSED（含当日 5 轮），欠账的全量 sweep 已补足；③ 剩余唯一 active = M23 gui-ux-parity（切片全 done，验收剩「真实平台观感人工确认」）。check_tasks 通过（49 features / 1 active）
2026-09-20 | gui-palette-drag-in | done | 修「无法从左侧指令树拖放指令到画布」（维护者 G4 验收报障）：三处缺陷叠加——① FlowTreeModel.mimeTypes() 只注册画布内部 MIME，指令树 pplication/x-rpa-flow-command 未注册 → Qt dragEnter 探测阶段整体拒收；② FlowTreeView.dragMoveEvent 开头只认 node 格式，command MIME 直接 ignore → 永不走到 drop；③（真根因）_insert_item_at_drop 用裸 insertRow(row, item)——PySide6 不转移所有权，dropMimeData 返回后局部引用释放、C++ 提前回收成死行，紧接着被 _prune_dead_rows 清掉（「插进去立刻消失」）。修：mimeTypes 注册双 MIME；dragMoveEvent 接受 command MIME（hit-test 共用）+ 悬停指令卡片中部时按「其后插入」而非 on；dropEvent command 分支改用 dragMoveEvent 算好的 row/parent（落点=指示条位置，不再固定追加末尾）；_insert_item_at_drop 两处改 [new_item]（与别处 627 行既有注释一致）；_CommandTree docstring 保留。回归：test_gui_canvas_batch +4（mimeTypes 注册双格式 / dragMove 接受并算出 below 落点 / drop 插到指示条位置 / 容器保持 on 模式）；ruff + FULL GATE PASSED
2026-09-20 | gui-stale-pending-crash-hardening | done | 处理「加循环指令→删除→点其他指令卡死闪退」（维护者报障）：本地 5 组操作矩阵 + 真实 Windows 平台 + offscreen + 卡片删除按钮路径均未复现硬崩溃，但定位到**确定性隐患链**并加固——① 卡片上的删除按钮走 model.remove_row 而非 app 的 _delete_selected_node，**参数面板不清空**，待提交登记 _pending_apply 仍指向已删节点；② 已删行的 QModelIndex.isValid() 仍可能为 True 且行会被后续节点顶上，按行取 item 会命中「别人」→ 提交时写错节点或对 None 取数据（Qt 槽内异常/中止）。修：_mount_pending_apply 登记时记录节点 id；新增 _pending_target_item（item 为空或 id 不符即判定登记陈旧并作废）；_commit_pending_edits 与新增 _invalidate_stale_pending_apply（挂在 _on_model_mutated，结构变更即清理陈旧面板）统一走它；_show_action_form/_show_control_form 的 apply 闭包**自校验节点 id**（被直接调用也绝不写错行），holder None 守卫 + _undo_stack 空判断。另加**崩溃诊断**：_install_crash_diagnostics（faulthandler 段错误 C 栈 + sys.excepthook 未捕获异常 → %TEMP%\\rpa_gui_crash.log），run_gui 启动打印日志路径——下次真机复现可直接拿到栈。测试：test_gui_param_form +2（结构变更作废陈旧登记并清面板 / 陈旧 apply 安全返回不写节点）；ruff + FULL GATE PASSED。**待维护者带日志复现确认根因**
2026-09-20 | data-log-and-output-preview | done | 补「打印日志」指令 + 运行日志显示输出值（维护者：test 流程不知道最后一个节点抓到的数据对不对，是不是少了打印日志）。核对：83 条 manifest 无任何 log/print（docs/command-inventory.md 早列为推荐补齐项）。落地两件：① data.log 打印日志（catalog 83→84）——manifest（transform/pure/replay safe，message 必填且 x-fx、level enum info/warn/error 默认 info）+ worker 纯透传回 outputs{message,level}（变量引用由 orchestrator 既有的 ${} / fx 标签解析完成，规则 2/3 不破）；i18n.js 补中文命令名「打印日志」+ 字段标签 message/level；② _format_event 输出值预览——stepCompleted 由「输出: 字段名列表」改为 key=值（新增 _format_outputs_preview：单行折叠、80 字截断、最多 3 项、超出显示「另有 N 项」、字典/列表走 JSON），运行日志面板从此可直接核对每个节点抓到的数据。测试：test_data_batch1 +2（message/level 回传、空文本与 level）、test_gui_param_form 改写 stepCompleted 断言（值可见）+ 新增 _format_outputs_preview 4 断言；test_protocols catalog 83→84；ruff + FULL GATE PASSED。CLI 侧 _cmd_run 仍只打印最终 result JSON（GUI 为主力形态，ADR 0016）
2026-09-20 | gui-param-panel-ghost-fix | done | 修「切换指令时小框闪现」（维护者报障）：根因——`_clear_param_panel` 只调 `deleteLater()`，旧控件要等事件循环回收，期间仍挂在右栏且可见（已脱离布局故保留旧几何），切换瞬间与新建表单并存 → 残影。修：摘除后先 `hide()` 再 `setParent(None)`（当帧停止绘制且不成独立顶层窗口），最后 `deleteLater()` 释放；集中修在 `_clear_param_panel`，action/control 表单与占位面板全部受益。回归：test_gui_param_form 新增 test_switching_nodes_detaches_old_param_form_immediately（切换后未跑事件循环即断言旧控件 parent() is None 且不可见）；ruff + FULL GATE PASSED
2026-09-20 | gui-fx-menu-flash | done | 修「在画布切换含 fx 的指令时小框闪现」（维护者报障 + 截图定位）：线索——截图中只有第 2 条（n6 navigate，url fx）与第 5 条（n2 getText，sessionId fx）会闪，核对 workflows/test 后确认这两条正是**唯一带非空 _exprModes（fx 模式）的节点**。根因候选唯一收敛：全 GUI 只有 fx 行会创建**预挂 QMenu 的 QToolButton**（＋变量，InstantPopup + setMenu），预挂菜单在 Windows 上会随控件树重挂/销毁产生原生弹层残影（灰底 + 图标列白块，与截图一致）。修：_wrap_fx_row 不再预挂菜单——改为点击时按需构建并就地弹出（抽出 `_build_variable_menu` 便于测试），菜单 WA_DeleteOnClose + deleteLater；变量列表每次弹出都是最新（行为不变）。顺带：本地多轮探针（顶层窗口枚举/窗内小控件/菜单 show·popup·exec 调用栈监听/真实鼠标点击/逐帧 w.grab）均未复现，故按结构性隐患消除处理，请维护者重启 GUI 复验。回归：test_gui_param_form 新增 test_fx_var_button_has_no_pre_attached_menu + 既有 fx 菜单用例改走 _build_variable_menu；另修一处**与本次改动无关的存量脆弱测试**——test_gui_canvas::test_real_workflow_maps_to_sequence_with_actions 把 workflows/test 的参数摘要写死（action=goto/+2），维护者正常编辑流程后必误报 → 改为按节点自身参数派生断言。ruff + FULL GATE PASSED（678 passed / 12 skipped）
2026-09-20 | gui-flash-diagnostic | wip | 「切换含 fx 的指令时小框闪现」未定位（维护者重启后仍在，确认只有 fx 指令触发）：已排除项——预挂 QMenu（已改按需弹出，仍闪）、画布 tooltip（未设 ToolTipRole）、窗内小控件覆盖画布（探针无命中）、菜单/提示 show·popup·exec 调用栈监听（本地无触发）。为拿真机硬证据新增诊断：debug_log.install_window_show_watch（仅 RPA_GUI_DEBUG=1 安装，事件过滤器记录任何窗口级控件的 Show 事件 + 类名/尺寸/位置/调用栈），run_gui 装配；本地验证可捕获 QToolTip(QLabel)/QMenu 的显示与栈。待维护者以 RPA_GUI_DEBUG=1 复现后读 %TEMP%\rpa_gui_debug.log 定位
2026-09-20 | gui-fx-toolbutton-window-flash | done | 修「切换含 fx 的指令时小框闪现」**真因定位并根治**（维护者重启后仍复现，诊断日志实锤）：`_wrap_fx_row` 里 `QToolButton()` **创建时无父级**，随后 `var_button.setVisible(True)`（仅 fx 字段为 True）——Qt 对无父级控件执行 setVisible(True) 会把它当**顶层窗口**显示，于是 51x23 的「＋变量」按钮以独立窗口闪现，加入布局后才收回子控件；这解释了「只有启用 fx 的指令才闪」。修：两个按钮改为带父级创建 `QToolButton(row)`，并把可见性初始化移到 `layout.addWidget` 之后（此时必有父级），双保险。诊断：新增 `debug_log.install_window_show_watch`（仅 RPA_GUI_DEBUG=1 安装的事件过滤器，记录任何窗口级控件的 Show 事件 + 类名/尺寸/位置/调用栈），run_gui 装配——正是它把真凶钉死（日志：`window-show cls=QToolButton size=(51,23)` + param_form:523 调用栈）。验证：同诊断复跑（真实鼠标点击 n6/n2/n5）后仅剩 MainWindow 的 Show，fx 切换零窗口事件。回归：test_gui_param_form 新增 test_fx_row_widgets_are_never_windows（按钮必须有父级、整棵表单不得含窗口控件）；ruff + FULL GATE PASSED（679 passed / 12 skipped）
2026-09-20 | gui-first-form-layout-lag | done | 修「第一次点 fx 指令小卡一下」（维护者报障）：cProfile 定位——首次选中复杂表单时 `QScrollArea.setWidget` 独占 247ms（真实平台实测 208~365ms，之后 7ms），是 Qt 对「复杂表单（分组区段 + 嵌套 QFormLayout + 多控件）首次布局/样式初始化」的一次性开销；与 fx 无关（用户第一次点恰好是 fx 节点），也与 setStyleSheet 无关（全局屏蔽后仍 469ms 实测排除）。修：新增 `MainWindow._prewarm_param_panel`——窗口显示后由 run_gui 用 `QTimer.singleShot(0, ...)` 在空闲时机以真实命令表单预热一次（用完即弃），把开销挪到用户交互之前；实测首次点击 208ms→45ms。注意：廉价预热（空 QLabel/最小 schema）无效（仍 296~391ms），必须用真实复杂表单。回归：test_gui_param_form 新增 test_prewarm_param_panel_is_safe_and_keeps_panel_usable；ruff + FULL GATE PASSED（680 passed / 12 skipped）
2026-09-20 | M24-debugger | S1 done | **断点契约（runtime + CLI）**：RunControl 打包「暂停开关 + 断点集合 + 已消费断点 + 单步预算」在递归执行链上传递（参数个数不变，类型由 asyncio.Event 换成 RunControl）；`stop_reason()` 在节点执行前统一判定「用户暂停 / 单步 / 命中断点」并返回原因；`PauseSignal` 带 reason；检查点新增**可选**字段 breakpoints/consumedBreakpoints/pauseReason/pausedAtNode（旧检查点缺省为空，向后兼容，`validate_checkpoint` 校验类型与唯一性）。关键设计：断点随检查点持久化（resume 是新进程、控制文件已被 reset，断点不能只放控制通道）；命中即计入 consumed，防「resume 后同断点反复命中」死循环；单步用「放行本节点 + 下个边界返回 step」实现（初版在放行时立刻 pause.set()，被同节点内的重试边界检查提前拦下导致节点根本没执行——已修）。CLI：`run --breakpoints a,b`、`resume --step`；`RunHandle` 新增 `request_step()` 与 breakpoints/pause_reason 只读属性（API v1 契约测试同步登记：新增均为 keyword-only 带默认值，向后兼容）。测试：新增 tests/unit/test_debugger_breakpoints.py 8 项（执行前停/事件与检查点记录/resume 消费断点/多断点第二个命中/单步只前进一节点/无断点回归/旧检查点兼容/CLI 解析）；FULL GATE PASSED
2026-09-20 | M24-debugger | S2+S3+S4 done + 收口 | **GUI 断点交互 + 单步 + 文档**（入口对标影刀）：画布编号栏**扩出最左断点列**（`_GUTTER_WIDTH` 46→58，行号区右移）——点行首切换断点、设断点的行画**红点**（`ROLE_BREAKPOINT` 数据驱动绘制，delegate 无状态）；视图新增 `breakpoint_toggled` 信号，app 维护 `_breakpoints` 集合并重刷标记（`_apply_breakpoint_flags`，结构变更/重开流程后自动重刷、删除节点连带剪除）；右键菜单加「添加/删除断点」（`_canvas_menu_state` 增 breakpoint/has_breakpoint，虚拟行不可设）。运行链路：`RunManager.start(breakpoints=)` 透传 `--breakpoints`；`RunManager.resume(step=)` 透传 `--step`；运行面板/浮窗按 `runPaused.reason` 区分显示「命中断点 <节点> / 单步完成 / 已暂停」，命中断点时复用跳转按钮定位到该节点。单步入口：工具栏 + 浮窗「单步」（仅 paused 态可用；`_step_run` 走同一 resume 通道带 `--step`）。文档：ADR 0005 增补「断点与单步（M24 增补）」、`docs/gui-run-control.md` 加断点/单步章（含 CLI 示例）。测试：新增 tests/contract/test_gui_breakpoints.py 7 项（热区几何/行首点击切换与标记/重开重刷与删除剪除/菜单可用性/真子进程断点运行命中并跳转/单步按钮态/单步只前进一节点）；M24 全切片 done、feature `debugger-breakpoints` 通过、project_state 推进 M25（运行历史浏览与回放，feature run-history-replay）。FULL GATE PASSED
2026-09-20 | M25-run-history | S1 done | **运行历史只读读取器 + CLI**：新增顶层零依赖模块 `src/rpa_core/run_history.py`（只 import stdlib——CLI/devserver/GUI 三方共用，且架构检查禁止 devserver import runtime，同 control_channel 的理由）：`list_runs(artifacts_root, limit)` 扫 `<run_id>/` 目录（含 result/events/checkpoint 任一即为运行），摘要含 runId/workflowId/status/起止/耗时/errorCode/resumable/pauseReason/pausedAtNode/completedSteps，按结束时间倒序（缺省回退目录 mtime）、limit=0 不限制；`read_run()` 详情额外带 inputs（取自 checkpoint.scopes.inputs）、breakpoints/consumedBreakpoints 与事件时间线；全程容错——损坏 result.json 记 status=unknown、坏事件行跳过、非运行目录忽略、缺失抛 RunNotFoundError。CLI 通道对齐：`rpa-core runs list [--limit] [--artifacts]` / `runs show <run_id>`（JSON 输出）。测试：新增 tests/unit/test_run_history.py 8 项（摘要与倒序/limit 与空根/损坏与非运行目录容错/详情 inputs+检查点调试字段+事件/坏行跳过/未知抛错/CLI list+show/CLI 缺失返回 2）；实机 `rpa-core runs list` 对真实 run_artifacts 输出正常。FULL GATE PASSED
2026-09-20 | M25-run-history | S2+S3 done + 收口 | **GUI 运行历史面板 + 文档**：工具栏「运行历史」→ 底部 dock（QTableWidget 5 列：时间/流程/状态/耗时/错误，时间倒序 limit 50）：查看时间线（复用 `_format_event` 渲染 events.jsonl，含输出值/暂停原因/失败详情）、跳到节点（事件行光标 → 画布定位，缺省暂停/失败节点）、**用同样输入再跑**（读历史 inputs → 按 workflow id 反查流程名 → `_open_named_flow` + `_start_run(name, inputs)`，明确是新 run 不是原地重放）、**继续/单步**（仅 paused+有检查点可用）。配套 `RunManager.resume_run(workflow_name, run_id, step=)`——恢复**不由本进程托管**的历史运行（GUI 重启后也能续），与 `resume` 的差别只是不需要先有 `_procs` 条目。文档：`docs/gui-run-control.md` 加「运行历史」章（含 CLI 同权说明）。测试：新增 tests/contract/test_gui_run_history.py 5 项（列表与按钮态/时间线渲染与跳转目标/再跑用历史输入/继续与单步分派/空态安全）；M25 全切片 done、feature `run-history-replay` 通过。FULL GATE PASSED
2026-09-20 | M27-workbench-home | S1 done | **宿主形态改两段式（ADR 0017）+ 工作台骨架**：维护者定向（运行历史/流程库单独界面、影刀式首页）——决策落 ADR 0017：① 工作台是**独立窗口**（`rpa-core gui` 默认打开），流程管理（列表/新建/打开，后续切片补复制/重命名/删除/导入导出）与全局运行历史（S2）在首页；② 编辑器保持**单窗口**（现有 MainWindow，画布/参数/运行控制/元素库/数据表格/变量 dock 不动）；③ **运行控制（暂停/继续/单步）只在编辑器**，工作台只「运行 + 看状态」，避免两处控制同一 run；④ **删除流程 = 提示后连同 elements/、data/ 与该流程运行历史一并删除**（提示列明资产数量、默认取消，S3 落地）；⑤ 多窗口编辑器推迟（窗口生命周期/RunManager 归属/脏关闭/测试基线成本高、收益未验证）。S1 交付：新增 `src/rpa_core/gui/home.py`（HomeWindow：流程表格「流程/最近运行/最近运行时间/元素数/修改时间」，按最近运行时间倒序；双击或「打开」→ 编辑器；「新建流程」命名对话框 + store 名称校验 + 建空流程后打开；空库引导文案）；`app.py` 新增 `open_editor_window`（编辑器**单例**，反复打开不叠窗）与 `run_gui` 分流（默认开工作台；`--workflow X` 仍直开编辑器，脚本与既有用法不变）；工作台与编辑器共用同一 `workflows_root`。测试：新增 tests/contract/test_gui_home.py 4 项（列表含运行状态与元素数并按时间倒序/打开与新建经注入 hook/空名与非法名被拒不落库/空态引导）；实机链路验证（工作台列出 test → 打开编辑器 → 单窗口复用）。FULL GATE PASSED
2026-09-20 | M27-workbench-home | S2 done | **运行历史迁入工作台（全局视图 + 按流程筛选 + 双击打开编辑器看时间线）**：工作台改为两页签（`QTabWidget`）——「流程库」+「运行历史」；运行历史页签复用 `run_history.list_runs(limit=0)` 全局列出（时间倒序），带流程筛选下拉（「全部流程」+ 各流程，保留选择）、刷新、「打开时间线」，双击/按钮 → 在工作台内**打开对应流程的编辑器并载入该次运行的时间线**（流程已不在库里时只给提示不打开）。编辑器侧把「渲染某次历史」抽成可按 run_id 调用的 `MainWindow.show_history_run(run_id)`（原 `_open_history_run` 取选中行后转调），`open_editor_window(..., history_run_id=)` 在打开流程后载入时间线——两窗口共用同一渲染与跳转逻辑，编辑器内既有历史 dock 保留（就地查看当前流程）。测试：test_gui_home.py +3（全局列出与筛选/双击带 run 打开编辑器/流程缺失给提示）；FULL GATE PASSED
2026-09-20 | M27-workbench-home | S3 done | **流程管理动作（复制/重命名/删除连带/导入导出）**：能力层 `WorkflowDirStore` 新增 `delete_flow(purge=)`（purge=True 整目录删除含 elements/、data/）、`rename_flow`、`copy_flow`、`export_flow`、`import_flow`（均复用名称校验、目标已存在则拒绝）；运行历史侧新增 `run_history.purge_runs(artifacts_root, workflow_id)`——**本模块唯一写操作**（判断归属依赖它对证据格式的了解：result.workflow_id / checkpoint.workflowId，放这里避免调用方重复实现漂移），逐目录 rmtree、单个删不掉不阻塞其它。工作台按钮补齐：复制（默认名 <name>_copy）、重命名、删除、导入（选 workflow.json 或目录）、导出（选目标目录）。**删除语义（ADR 0017 决策 4）**：确认框列出连带范围（元素资产 N 个 / 数据表格有·无 / 运行历史 M 条 + 「不可撤销」），**默认按钮是取消**，确认后才整目录删除 + purge_runs；**正在编辑器里打开的流程拒绝重命名/删除**（提示先保存并关闭/切换，避免丢未保存编辑）。测试：新增 tests/contract/test_gui_home_management.py 9 项（复制含资产与重名/非法名拒绝、重命名移动目录、purge 整目录、导出导入往返与重名拒绝、purge_runs 只删匹配流程、删除确认框列出连带范围且默认取消且取消不删、确认后流程资产+历史一并删除且不误伤其它流程、重命名/复制落库、编辑中拒绝并给出原因）；FULL GATE PASSED
2026-09-20 | M27-workbench-home | S4 done + 收口 | **工作台运行入口与状态联动 + M27 收口**：工作台「运行」按钮——对选中流程发起 `RunManager.start(name)`（不带断点，断点属编辑器调试能力）、运行中禁用按钮并提示「在编辑器中可暂停/单步」、800ms 轮询 status 到终态后刷新列表状态列并显示中文结果（paused 时提示可去编辑器继续）、已有运行进行中拒绝二次发起、`closeEvent` 停轮询并 `RunManager.close()` 释放子进程句柄、窗口重新激活时（且未在运行）自动刷新列表（编辑器保存/新建后切回即可见）。文档：`docs/gui-run-control.md` 加「两段式宿主：工作台 + 编辑器」章（首页职责/运行控制归属/删除连带/--workflow 兼容）。**M27 全切片 done**（S1 骨架 → S2 运行历史页签 → S3 管理动作含连带删除 → S4 运行入口），feature `gui-home-workbench` 通过；project_state 回到 M26（流程 inputs 声明编辑 UI，原计划下一项）为当前任务。测试：test_gui_home_management.py +3（运行入口发起并轮询到终态且按钮态正确/运行中拒绝二次发起/关闭释放 RunManager），合计工作台侧 19 项；FULL GATE PASSED
2026-09-20 | M27-workbench-home | polish | **影刀式交互细节（维护者反馈）**：① 首页运行——点「运行」后**首页收起**（`self.hide()`）+ 右下角弹出**运行浮窗**显示进度（复用 `RunFloatWindow`：取消/还原接线，暂停·继续·单步三按钮**隐藏**——控制权在编辑器，ADR 0017 决策 3）；浮窗进度从事件流取「已完成步数 + 当前步骤」；成功 2s 后自动还原首页（`_schedule_home_restore`），失败/暂停停留等手动「还原」（还原=关浮窗+show+刷新）；取消走 `RunManager.cancel` 等收口。② 打开流程——首页收起 + 编辑器 `showMaximized()`；**关闭编辑器后首页自动回来**（`_HOME_WINDOW` 模块引用 + 编辑器 `_shutdown_run_manager` 里 show/raise，两段式往返）。文档：`docs/gui-run-control.md` 补「影刀式交互细节」。测试：test_gui_home_management.py +4（运行收起首页并弹浮窗且控制按钮隐藏+取消派发/还原关浮窗/打开流程收起首页且编辑器 showMaximized/编辑器关闭回首页），工作台侧合计 23 项；FULL GATE PASSED

{
  "project": "rpa_core",
  "phase": "command-parity-extension-editor",
  "status": "active",
  "completed_milestone": "M37-ignore-before-unload",
  "active_milestone": "M38-command-matrix",
  "active_plan": ".harness/tasks/M38-command-matrix.md",
  "active_feature": null,
  "next_milestone": null,
  "later_milestones": [],
  "notes": "技术路线（ADR 0016）：GUI 为唯一主力形态，Web 编辑器退为可选、冻结演进；GUI 独立运行不需要 web 服务器。M28（元素自愈与执行前预检）、M29（浏览器命令参数漂移收口）已收口。当前 M30（桌面通道命令参数漂移收口，active）：开工第一步就推翻 M29 留在 BACKLOG 的归因——「桌面/数据通道分派不是 command == \"<id>\" 字面量形状」是错的。实际 desktop.py / desktop_win32.py 都是 command 字面量，python_worker.py 也是字面量（载体名是 invocation.command_id / invocation.inputs）。真实原因是当时 EXECUTOR_FILES 只登记了 browser. 前缀，其余通道被跳过、不是不适用。泛化载体名后四类通道全部可切片（78 条命令、跳过 0 条），一次挖出 12 条命令的参数漂移：timeoutMs 未消费 8 条（click/getText/input 两后端 + win32 hotkey/menuSelect）、click.simulateHuman/clickPosition 两后端、attachWindow.className（uia 侧）。口径沿用 M29（能兑现就实现、兑现不了就删），并补一条：能退让就退让、互斥就报错。后续任务：M26（流程 inputs 编辑 UI，唯一 passes=false 的既有 feature）、整页/元素截图、关闭标签页/终止进程、两套「可见」口径对齐。 M30 进度：S1（门禁扩到四类通道 75 checked/3 exempt/0 skipped）、S2（timeoutMs 变真等待，预算抬高上两层超时；删 hotkey/menuSelect 的 timeoutMs）、S3（click.simulateHuman/clickPosition 实装，两后端共用 plan_click_for_element；顺手修掉两个真 bug——click_input 没有 click_count、pywinauto 没有 key_down；新增 errors 声明门禁 check_error_contract.py）、S4（uia 侧 attachWindow 补 className 过滤 —— `_find_windows_by_title` 增 `class_name` 参数，exact 走 FindWindowW 的 lpClassName + GetClassNameW 复核、contains/regex 走枚举回调等值比较；口径「matchMode 只作用于 title，className 恒为等值」；顺手统一两处不对称——uia 的 title 原是 required 而 win32 不是、两侧都没有「一个筛选条件都不给」的前置守卫，现统一为「都可省 + 缺则 INVALID_INPUT」；KNOWN_GAPS 清零）done，剩 S5 收口（文档同步 + 全台账 + full gate）。S4 记录的取舍：exact 路径的 FindWindowW 只回第一个句柄，故同标题同类名的多窗口静默附着第一个、不报 ELEMENT_AMBIGUOUS（换 matchMode=contains 才有歧义检测）——有意为之，已写进文档与测试。 **M30 已全部收口（S1–S5 done，2026-09-21）**：106 项契约测试（test_desktop_params 13 + test_desktop_click_plan 64 + test_desktop_attach_window 29）；KNOWN_GAPS 清零；FULL GATE PASSED（925 passed / 2 xfailed / 12 skipped）。S5 清掉一处**面向维护者的过期结论**——`docs/element-mvp-boundaries.md` §3.4 仍写着 S1 已推翻的「桌面/数据通道不是字面量形状、静态切片不适用」（S1 只改了门禁 docstring 与 BACKLOG，漏了这份文档），已改写为四类通道处置表 + 与浏览器通道的两条口径差异。**教训：纠正错误结论时要搜全仓所有记录它的地方，不能只改眼前一处。** **M26 已立项为 active（2026-09-21）**——它是 feature_list 里最后一个 `passes=false` 的既有 feature（56 个 feature 中唯一未完成）。S1 已完成：新建 `model/inputs.py` 做声明规范化与校验（**两列** `name`+`json_default`，不引入 type/required/description——因为 `Workflow.inputs` 的真实形状是 `{名称: 默认值}` 扁平映射，改形状需要 ADR 级决定），并接入 `WorkflowCompiler.compile`使校验真的拦得住。关键设计：读取宽松（历史非法声明不该让文件打不开）、保存严格；名字规则`[A-Za-z_]\\w*` **不含点号**（点号是 `${...}` 路径分隔符，带点的名字会「声明在册但永远引用不上」）；默认值文本 `sort_keys=True` 规范化（否则键序抖动污染脏标记与 git diff）。67 项测试 + 负向验证 2 例。**M26 已全部收口（S1–S4 done，2026-09-21）**：三份测试共 115 项（能力层 67 + 对话框 29 + 联动 19），负向验证 4 处；新建 `docs/flow-inputs.md`。S2 建 `gui/inputs_dialog.py`（两列表格，校验在「确定」之前且不产出半成品；接缝 `app.flow_inputs_prompt` 由测试驱动出来——最初的函数内 import 让 monkeypatch 打不中，暴露了接缝缺失）；S3 的实质是**验证联动真的成立**而非新增接线——三个消费方本来就都读 `_workflow_meta[\"inputs\"]`，但「接线自动」与「接线正确」是两件事，S3 用跨层一致性测试（补全产出的 `inputs.<名>` 必须被 `compiler._REFERENCE` 匹配）把它证明了一次；并补了 M25 的边界：**声明不是运行输入的过滤器**（历史输入原样透传）。**feature_list 至此 56/56 全部 passes=true，无未完成 feature。****其他候选里程碑**：(a) M31 GUI 跨平台观感诊断（macOS vs Windows，诊断型，见 BACKLOG）——由维护者实测反馈「控件样式/字体大小/显示隐藏行为两端不一致」触发的**先诊断不写代码**，目的是把主观感受变成逐条可归因差异（[U]单位 / [F]字体 / [Q]皮肤 / [W]系统约束），用 [U]+[F]+[Q] 与 [W] 的比例判定「要不要迁 Tauri」，避免在缺前提材料时推翻刚做的 ADR 0016；(b) M26（已转 active，见上）；(c) 整页/元素截图；(d) 关闭标签页/终止我们拉起的浏览器进程；(e) 两套「可见」口径对齐。S3 未收：simulateHuman 归一化四端不一致（执行器 bool() vs 扩展 String(raw??\"\")!==\"false\"），现由 xfail(strict=True) 钉住，统一属独立切片。 **M32 已收口（S1 done，2026-09-21）**：浏览器收尾两条命令落地。**关标签页** `browser.closeTabs`——显式 `tabIds` / `all=true`（当前窗口全部）二选一，扩展侧新增 `tabs.closeMany` **逐项记账**（`closedTabIds`/`failedTabIds` 两个数组），不是 `Promise.all` 一把梭；关掉当前会话所属标签页时会话随之解绑（免得后续步骤拿着死 tabId 操作）。**终止浏览器** `browser.closeBrowser`——`scope` 参数对齐影刀形态：`launchedByUs`（**默认保守**，只杀本执行器拉起过的实例）/`byProcessName`（按名全杀，含用户自己开的窗口）。两者是**独立命令**而不是 `browser.close`的参数：后者是会话生命周期（effect=session、语义是解绑），前者是对用户浏览器的破坏性操作（effect=unsafe-write），风险等级与默认策略不同，混在一起会让「结束会话」带上关页面的杀伤力。**根因修复**：`launch_browser` 此前 fire-and-forget（Popen 完丢句柄），没有任何「我们启动过它」的记录——这正是该能力此前无法实现、只能退化成按名全杀的原因；现在 `_launch_target` 在**等到插件上线之后**记 `_launched_marks` 水位（失败的拉起不记，否则事后会去杀用户的浏览器）。保守默认 + 无记录时**不静默成功**（分开「本来就没这个浏览器」与「有但都不是我们开的」，后者报`EXECUTOR_FAILED` + `reason=no_launched_process` + 指向 `byProcessName` 出口）。**实现中修掉两个真 bug**：① `tasklist` 在中文 Windows 上输出 GBK，subprocess 默认按 UTF-8 解会抛 `UnicodeDecodeError`——且异常发生在**读线程**里，表现为 `stdout=None`、报错点离现场很远，现显式给 `encoding='mbcs'`；② `os.kill(pid, 0)` 在 Windows 上是 `TerminateProcess`、信号 0 直接抛 `WinError 87`（「信号 0 = 只探测」是 POSIX 语义），Windows 侧改用 `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` 判存活。**新门禁** `scripts/check_close_ops.mjs`（28 项，已进 `check_all.py`）：批量关闭的「逐项记账 / `all` 严格 `=== true` / 空 tabIds 不退化成全关」语义只有扩展侧能证明——Python 桩测的是接口形状，一个把 `Promise.all` 当成功的实现也能过 Python 测试，却会在真机上把「关了 2/3」报成「全关了」。**负向验证 3 例**（一把梭 / `all` 宽松判定 / 未声明错误码），其中 `all` 宽松那条第一次是**假绿灯**（桩里当前窗口恰好命中同一批标签），补了「空 tabIds」与「`all='yes'`」两个用例后才真正抓住——记这里：负向验证没红时先怀疑验证本身没打到。扩展版本 `0.3.3` → `0.4.0`（补 `windows` 权限）；`x-palette-order` 两条占 5/6、其余 +2 顺移。 **M34 已收口（2026-09-21）**：扩展宿主空闲自杀，根治「uv run 重装 venv 被 os error 5 拒绝访问」。根因：Windows 上 console script 是垫片+python.exe 两进程，真宿主随扩展断开退出但垫片不回收，一直锁着 .venv/Scripts/rpa-core-ext-host.exe。修复：宿主在「无客户端连接 且 静置超阈值」时主动 os._exit(0)（阈值默认 30 分钟，RPA_CORE_HOST_IDLE_EXIT_SECONDS=0/负 关闭；有客户端或扩展仍在发消息则不退）。实现中踩两个坑已写进任务单：① 合成脚本假绿灯证明「关 stdin fd 能唤醒阻塞读」，真宿主多线程+管道下完全失效（关的是宿主自己读端，写端在浏览器手里）；② 从监视线程调 shutdown() 在 server.close() 的 _PipeServer._lock 处死锁（accept_loop 攥锁阻塞在 WaitForSingleObject），改监视线程只置 _closed + os._exit。FULL GATE PASSED（1072 passed / 2 xfailed / 12 skipped）。【M35-closebrowser-kill-all】维护者定案「直接杀某个浏览器的所有进程」：browser.closeBrowser 删除 scope（launchedByUs/byProcessName）与 _launched_marks 水位，语义=按进程名全杀该类型全部进程（含用户自开窗口），执行命令即显式授权；保留 force/逐进程记账/等真退出/幂等 0 匹配成功；_list_browser_processes 的 startedAt 随唯一消费者（水位筛选）一并删除（Windows tasklist 本就取不到启动时间恒记 0，POSIX etimes 换算简化为 pid=,comm=）。errors 的 EXECUTOR_FAILED 声明保留（防御性声明合法，单向门禁不反向卡）。feature browser-teardown 标题同步。测试 -3 项（2 个 scope 专测 + 1 个参数化用例）；FULL GATE PASSED（收集实测 1083 项 = 1069 passed / 2 xfailed / 12 skipped，与 M34 的 1086 项差值恰为删除的 3 项）。【M36-test-input-leak】维护者报告「不同输入框莫名输入 hi」：M30 S2 的 desktop.input clipboard 契约用例只桩了 _find，剪贴板写入与 send_keys(\"^v\") 走真实全局路径——每跑一次套件清空一次剪贴板并向前台输入框粘贴 hi（SendInput 全局键击落焦点窗口）。修复：肇事用例全打桩（断言调度而非真实效果）+ conftest autouse 守卫 _block_global_input（钉死 pywinauto.keyboard.send_keys / desktop_win32.send_keys 早绑定名 / win32clipboard 写入口，Windows-only，RPA_DESKTOP_E2E=1 让位，用例内 monkeypatch LIFO 覆盖不冲突）；负向验证探针拦红。教训：打桩边界沿真实副作用面划，不沿参数传递面划（M30 S3 立了规矩 S2 没执行）。FULL GATE PASSED（1069 passed / 2 xfailed / 12 skipped，1083 项，与 M35 持平）。 **M37 已收口（2026-09-21）**：closeTabs 新增 ignoreBeforeUnload（默认 true 对齐影刀）——Python 显式才转发（None 不进 args），扩展 remove 前 MAIN world+injectImmediately 注入清 window.onbeforeunload，best-effort（注入失败不阻断；addEventListener 拦截清不掉、仍弹窗如实进 failedTabIds）；缺省语义单点兜底在扩展（!== false）。check_close_ops 29→37 项、三条负向验证全过。教训两条（详见 M37 任务文件）：同文件多编辑禁入同一并行批次（快照覆盖出过 NameError 与假绿灯各一次）；门禁正则必须切进被测 case 切片（全文件 world:/MAIN/ 被 page.call/eval 喂假绿）。FULL GATE PASSED（实测 1086 = 1083+3）。 **M38 已立项为 active（2026-09-22）**：指令测试策略（docs/command-testing-strategy.md）的落地第一片。维护者追加定案：**覆盖率静态校验进默认门禁**（只读用例表与 manifest、毫秒级；执行层全量矩阵仍按需 RPA_COMMAND_MATRIX=1）。S1 完成：公共假扩展（tests/commands/harness.py，只替换 _exchange）+ 浏览器通道用例表（32 条命令 / 180 个变体，每参数、每枚举值、min/max 两取值、每命令负路径）+ 驱动（断言真实下发的 op/args/timeoutSeconds、输出契约、effect 种类、错误码）+ 覆盖率校验器 6 条口径 + 收集隔离 + 危险面守卫。**实测发现两个死参数**（browser.closeTabs.browserType、browser.waitLoad.state：声明了但实现未消费，静态门禁因「通用读取」放行）已登记台账，处置待定。**两起事故**：①建表探针未打桩 browser.closeBrowser，真实杀掉本机 21 个浏览器进程（M36 同类，已加 conftest autouse 守卫钉死进程面/拉起面）；②矩阵用 pytest tmp_path 导致 atexit 批量删除被守卫拦下，159 个用例全绿而退出码为 1（改为共用临时目录）。验证：矩阵 180 passed / exit 0；校验器三向负向验证全红；实现级负向验证（删掉 click 的 simulateHuman 转发）精确 2 个用例红。剩余 S2 桌面 36 条、S3 数据/工作流 18 条、S4 L2 真机冒烟。 **M38 S1.2 已交付（2026-09-22）**：工作台第三个页签「指令测试」（`gui/command_matrix.py` + `home.py` 挂载 + `closeEvent` 连 `shutdown()`）——维护者要「一个页面做启动和展示」，落在 GUI 而非另起本地 Web 页（ADR 0016：GUI 唯一主力形态、devserver 冻结演进）。页面只做点火 + 展示：覆盖范围按命名空间读 `tests/commands/cases/*.json`（S2/S3 建表后自动出现，不用改页面；页面不读门禁的 PENDING_NAMESPACES 台账），结果不解析 pytest 输出而走被测方自己的 JSONL 钩子（`RPA_COMMAND_MATRIX_REPORT`，逐用例 append + flush，页面按字节偏移增量读、半行不消费）；`-v` 被 `addopts 的 -q` 抵消、`--junitxml` 只在会话结束落盘，两条候选的否决理由记在 conftest docstring；跑测试另起子进程（GUI 不承载 runtime），缺省不写报告。**本片最有价值的是抓到一条假绿灯**：摘掉 `_run_matrix` 的 `_timer.start()` 时白盒用例照样绿——①用例当时自己点火、注入点没落在被测分支上；②`_on_finished` 收尾还会 `_poll_report()` 一次，**收尾兜底掩盖了增量机制**，「结束后有行」永远证明不了轮询（轮询买的是「实时」）。改走真实入口 `_on_run_clicked`（只桩 argv 组装）+ 子进程写完第一行等测试放行，「进程还在跑时第一行已进表」成为可断言的中间态；三向负向验证全红且精确落在预期断言（其中摘 `timeout.connect` 那条在旧用例下是绿的，最能说明它现在真的承重）。教训：负向验证没红先怀疑验证本身没打到（M32、M30 S4 之后第三次出现，但形式更新：前两次是用例没覆盖到，这次是覆盖了别的路径 + 收尾逻辑掩盖）；推论——凡「收尾还会兜底做一次」的设计，增量机制必须靠中间态断言。测试 `tests/contract/test_gui_command_matrix.py` 16 项（纯函数 9 / 数据合同 2 / 装配 5，含 `shutdown` 收子进程防孤儿）。FULL GATE PASSED（1102 项：1088 passed / 2 xfailed / 12 skipped）。 **M38 S3 已交付（2026-09-22）**：数据/工作流通道 L1 用例表（`data` 17 + `workflow` 1 = 18 条命令 / 88 个变体），补的是**输出契约与错误分支**（M30 已确认这一通道零参数漂移）。**插桩点与前两片不同**：浏览器通道测「下发了什么 op/args」故桩在最底层 `_exchange`；数据命令本身是纯 Python 的文件/字符串操作，`PythonWorkerExecutor` **真起子进程**——真子进程就是真机，一次测到「inputs 决定的行为 / worker 回的 outputs 形状 / 磁盘上留下了什么」三面，打桩等于打掉最有价值的证据面（代价：每变体 ~0.6s 进程启动、全矩阵 64s，仍按需跑）。**抽公共驱动层** `tests/commands/matrix.py`（用例表加载 / `expect` 解释 / `{tmp}` 物化 / 落盘断言），两个驱动共用一套 `expect` 语义——S1.2 的假绿灯正是「同一件事两处口径」的变体，不赌第二次；`matrix_tmp_dir` 从驱动模块迁到 `tests/commands/conftest.py`。`expect` 新增 5 键：`outputPaths`（按 `Path` 比较、分隔符无关）、`outputsMatch`（正则）、`noEffects`（pure 命令**不产出** effect 记录）、`files`（`equals`/`contains`/`missing`/`jsonContains`）。**每个变体一个干净目录**（这一通道真写真删，用例之间不能共享状态）+ 可声明 `setup`（`files`/`dirs`/`table`）预置磁盘状态。**新增安全阀** `tests/commands/guard.py`：用例表是数据、数据会写错，而这条通道真的会删（`deletePath`+`recursive` 就是 `shutil.rmtree`）——执行任何命令前按**实现自己的解析规则**把路径类值解析成绝对路径，越出本变体目录即判失败；其契约测试放进**默认门禁**（`tests/contract/test_command_matrix_paths_guard.py` 3 项：正向不误杀 / 负向拦得住 / **接线**——源码断言安全阀出现在 `PythonWorkerExecutor(` 之前，即执行前；没人调用的纯函数只是注释）。每变体另断言 `active_process_count == 0`（不留 python 孤儿）。**实测固化的契约**：`limit` 的 items 传字符串按字符拆、`log` 的 level 传 `INFO` 原样透传、`getCell` 越界/列不存在**成功返回 null** 而 `deleteRow` 越界**报错**（读写不对称，有意）——前两条不是缺陷而是「schema 校验由 orchestrator 的 `Draft202012Validator` 统一负责」的证据（已带 notes 防后人当 bug 修）；`format` 占位符缺失报 `details.missing`（可诊断）；`exportCsv` 是 utf-8-sig（BOM）；`datetimeNow` 非法格式串是 SCRIPT_FAILED（不是静默降级）；`sleep` 小数秒那条同时断言**墙钟下界**（只断言 `sleptMs` 的话，一个「返回 `int(seconds*1000)` 但从不 sleep」的实现也会绿）。**一条实测纠正（小账）**：`writeText` 的 `../up.txt` 我按读代码推断成「归一化后仍在 workspace 内」，实测是 `CAPABILITY_DENIED`（`_within_workspace` 判的是**解析后的绝对路径**对 workspace）——**探针必须覆盖判据的两侧**，只测「合法一侧」会把推断当成契约写进表里。**验证**：矩阵 **268 passed / exit 0**（浏览器 180 + 数据 84 + 工作流 4）；带报告钩子重跑报告 268 case + 1 summary、`(command, variant)` 唯一组合 268、**无 command 缺失记录**；静态层 `check_command_matrix.py` `PASSED（50 条命令；未建表命名空间 1 个共 36 条，死参数台账 0）`——`PENDING_NAMESPACES` 只剩 desktop；**五向负向验证**（`.harness/spike/probe_data_matrix_negative.py`）全红且**精确落在预期变体**、逐字节还原（其中「用例表 `workspace` 越界」那条只让该变体红、输出里 `安全阀拦下` 在而 `用例断言失败` 不在——证明它在执行前就拦下了、根本没进命令层）。**第二笔小账**：安全阀的自检用例原先写在矩阵驱动里，nodeid 不含 `<命令>::<变体>`，于是页签进度条的分子（270）大于分母（268）——「一个用例 = 一个变体」是报告的硬不变量，自检用例已移进默认门禁的契约文件。FULL GATE PASSED（1105 项：1091 passed / 2 xfailed / 12 skipped）。剩余 S4 L2 真机冒烟。 **M38 S2.1 已交付（2026-09-22）**——维护者定案两句：S2 走**真桌面 fixture**（解掉「打桩绑定层 vs 真 fixture」的悬置口径）、先补「含桌面命令的流程中途暂停再继续」的真机证据（此前只有浏览器通道有，M21 macOS+Edge）。交付：fixture 加**非幂等**计数器（countButton 累加 / countLabel 只增不减——「零重跑」从推断变硬证据：重跑一次读数就是 2）；装配抽到 tests/e2e/desktop_fixture.py 两处共用；真机用例 tests/e2e/test_desktop_pause_resume.py（run 子进程 → 慢节点 keyIntervalMs=80 × 41 字 ≈ 3.7s 实测期间写暂停请求 → 收口 paused → resume 起**另一个子进程**（与 GUI「继续」同一条路）→ 三条断言：同一窗口 / 会话与元素都接回 / 零重跑，后者附事件级证据——续跑段 stepCompleted 集合恰等于暂停点之后的节点集）。**两处缺口都是实测逼出来的**：①续跑段 SESSION_NOT_FOUND（desktop.click、effects 空）——会话表没还原（窗口还在、句柄表是新的）；②补上会话后**同一个节点**换成 ELEMENT_NOT_FOUND——elementId→定位器映射也没还原；③补齐后 succeeded。修法：base.desktop_sessions_from_scopes + base.desktop_window_alive 两个共用纯函数 + 两后端各自的 restore_from_scopes；顺带定口径「定位器进 effect.details.locator 不进 outputs」（快照里资源绑定的权威位置一向是 resource+details，M21 的 tabId 就在那；outputs 是面向用户与表达式的公开面，塞进去等于升格成契约）。**与浏览器侧刻意不同**：browser 侧不校验标签页是否还在（校验要一次扩展往返，而「跑起来才发现页面被关了」与恢复期判定是同一种错误），桌面侧校验——本地微秒级，而 HWND 是 32 位句柄号**会被系统复用**，不校验可能把命令指到继承了同一句柄号的别人的窗口上；失效即不还原，让后续命令照旧报 SESSION_NOT_FOUND（错误码与浏览器侧一致）。验证：真机 E2E 2 passed / exit 0、UIA 全链路回归 3 passed、契约测试 tests/unit/test_desktop_session_restore.py 14 项进默认门禁、**负向验证 4 例**（只改写侧前缀字面量 / 只改读侧常量——两个方向都闭合各 2 红 / 摘掉 details.locator——1 红 / 临时停用 restore_from_scopes——E2E 红在续跑段，失败详情精确落在 nodeId=clickCount、commandId=desktop.click、effects 空，修复前状态被复现；均逐字节还原）。FULL GATE PASSED（退出码 0）。**教训**：第一版判据「输入框镜像到 resultText」是读代码推断——resultText 只在点提交时被赋值、直接读永远是初始值 Ready（实测当场红）；改成借 fixture 自己的提交处理程序当读侧探针后，那个未实测的假设（getText 对 UIA Edit 返回文本）也不再需要了。真机边界（如实记）：只跑 UIA 后端，win32 后端的还原只有契约测试、无真机用例（它认 title/class/controlId，fixture 控件是 UIA AutomationId，要另做靶子）。剩余：S2 的 36 条 L1 用例表本身（cases/desktop.json + desktop_win32.json + 驱动 + PENDING_NAMESPACES 删 desktop）。",
  "task_index": ".harness/tasks/BACKLOG.md",
  "scope": {
    "in": [
      "stable runtime errors",
      "executor cancellation cleanup",
      "bounded retry policy",
      "reliable run evidence",
      "effect contract",
      "Windows desktop executor validation",
      "legacy element data import",
      "checkpoint and recovery design"
    ],
    "out": [
      "FastAPI",
      "database",
      "React UI",
      "DSH plugin",
      "MCP",
      "scheduler",
      "installer",
      "distributed workers",
      "exactly-once guarantees",
      "online command generation"
    ]
  },
  "decision_gate": "Complete M1.1 before expanding command coverage; prove effect semantics and a second executor before durable recovery work."
}
